# 作業履歴 2024-12-03

## 概要

2024-12-03の作業内容をまとめています。

## コミット: d6d36d8c

### メッセージ

```
Merge branch 'take02' into take03
# Conflicts:
#	app/backend/api/src/test/java/com/example/sms/stepdefinitions/UC001Stepdefs.java
#	app/backend/api/src/test/java/com/example/sms/stepdefinitions/UC002StepDefs.java
#	app/backend/api/src/test/resources/application.properties
```

### 変更されたファイル


### 作業解説

#### 目的
システムの改善・機能拡張を行う。

#### 技術的な背景
プロジェクトの品質向上または機能拡張のための変更を実施。

#### 変更の影響範囲
影響を受けるレイヤ: 特定のレイヤに限定されない。変更は関連するテストおよびドキュメントへの波及を考慮する必要がある。

### 変更内容

```diff
commit d6d36d8c8310cb347345147c9d644af975d9f707
Merge: ab64cbe5 4e5672ad
Author: k2works <kakimomokuri@gmail.com>
Date:   Tue Dec 3 20:28:26 2024 +0900

    Merge branch 'take02' into take03
    
    # Conflicts:
    #       app/backend/api/src/test/java/com/example/sms/stepdefinitions/UC001Stepdefs.java
    #       app/backend/api/src/test/java/com/example/sms/stepdefinitions/UC002StepDefs.java
    #       app/backend/api/src/test/resources/application.properties


```

## コミット: 4e5672ad

### メッセージ

```
CI:アプリケーション
Remove Gradle testing steps from Vercel preview workflow

The Gradle testing and report generation steps within the api directory have been removed from the workflow. This streamlines the Vercel preview process by eliminating unnecessary tasks likely handled elsewhere.
```

### 変更されたファイル

- M	.github/workflows/vercel_preview.yml
- M	.github/workflows/vercel_productin.yml
- M	gulpfile.cjs
- M	package.json

### 作業解説

#### 目的
CI/CD パイプラインの改善により、デプロイメントプロセスを最適化する。

#### 技術的な背景
プロジェクトの品質向上または機能拡張のための変更を実施。

#### 変更の影響範囲
影響を受けるレイヤ: ビルド設定。変更は関連するテストおよびドキュメントへの波及を考慮する必要がある。

### 変更内容

```diff
commit 4e5672ad7f52d98df4063194b80afb389122400c
Author: k2works <kakimomokuri@gmail.com>
Date:   Tue Dec 3 20:00:28 2024 +0900

    CI:アプリケーション
    
    Remove Gradle testing steps from Vercel preview workflow
    
    The Gradle testing and report generation steps within the api directory have been removed from the workflow. This streamlines the Vercel preview process by eliminating unnecessary tasks likely handled elsewhere.

diff --git a/.github/workflows/vercel_preview.yml b/.github/workflows/vercel_preview.yml
index 7ae0f856..96872efd 100644
--- a/.github/workflows/vercel_preview.yml
+++ b/.github/workflows/vercel_preview.yml
@@ -42,12 +42,6 @@ jobs:
       - name: Grant execute permission for Gradle wrapper
         run: chmod +x ./gradlew
         working-directory: app/backend/api
-      - name: Test with Gradle in api directory
-        run: ./gradlew test
-        working-directory: app/backend/api
-      - name: Test Report with Gradle in api directory
-        run: ./gradlew allureReport
-        working-directory: app/backend/api
 
       - name: Install Vercel CLI
         run: npm install --global vercel@latest
@@ -55,5 +49,9 @@ jobs:
         run: vercel pull --yes --environment=preview --token=${{ secrets.VERCEL_TOKEN }}
       - name: Build Project Artifacts
         run: vercel build --token=${{ secrets.VERCEL_TOKEN }}
+      - name: Work round for Report Generation
+        run: npm run allure:build
+      - name: Work round for Report Generation
+        run: cp -r public/allure .vercel/output/static
       - name: Deploy Project Artifacts to Vercel
         run: vercel deploy --prebuilt --token=${{ secrets.VERCEL_TOKEN }}
\ No newline at end of file
diff --git a/.github/workflows/vercel_productin.yml b/.github/workflows/vercel_productin.yml
index 383eb618..5e4d6eb0 100644
--- a/.github/workflows/vercel_productin.yml
+++ b/.github/workflows/vercel_productin.yml
@@ -42,12 +42,6 @@ jobs:
       - name: Grant execute permission for Gradle wrapper
         run: chmod +x ./gradlew
         working-directory: app/backend/api
-      - name: Test with Gradle in api directory
-        run: ./gradlew test
-        working-directory: app/backend/api
-      - name: Test Report with Gradle in api directory
-        run: ./gradlew allureReport
-        working-directory: app/backend/api
 
       - name: Install Vercel CLI
         run: npm install --global vercel@latest
@@ -55,5 +49,9 @@ jobs:
         run: vercel pull --yes --environment=production --token=${{ secrets.VERCEL_TOKEN }}
       - name: Build Project Artifacts
         run: vercel build --prod --token=${{ secrets.VERCEL_TOKEN }}
+      - name: Work round for Report Generation
+        run: npm run allure:build
+      - name: Work round for Report Generation
+        run: cp -r public/allure .vercel/output/static
       - name: Deploy Project Artifacts to Vercel
         run: vercel deploy --prebuilt --prod --token=${{ secrets.VERCEL_TOKEN }}
diff --git a/gulpfile.cjs b/gulpfile.cjs
index 0ec91c04..a5267215 100644
--- a/gulpfile.cjs
+++ b/gulpfile.cjs
@@ -54,3 +54,5 @@ exports.dev = series(
 );
 
 exports.allure = custom.allureBuildTasks();
+
+exports.allureBuild = custom.allureGradleBuildTasks();
diff --git a/package.json b/package.json
index 4154fafe..3b4c7bf8 100644
--- a/package.json
+++ b/package.json
@@ -21,6 +21,7 @@
     "jig:db": "npx gulp jig_erd",
     "erd": "npx gulp erd",
     "allure": "npx gulp allure",
+    "allure:build": "npx gulp allureBuild",
     "db:schemaspy:mysql": "docker compose run --rm schemaspy_mysql",
     "db:schemaspy:postresql": "docker compose run --rm schemaspy_postgresql"
   },

```

## コミット: 28fcf461

### メッセージ

```
chore:ビルドタスク
Add build tasks for API and app in Gulp

Introduce new build tasks for the API and app within the Gulp custom tasks. These additions enable running 'gradlew build' for the API and 'npm install && npm run build' for the app, enhancing the build process. The changes streamline build automation by integrating these steps into existing Gulp task sequences.
```

### 変更されたファイル

- M	gulpfile.cjs
- M	ops/gulp/tasks/custom.js

### 作業解説

#### 目的
開発環境・ビルド設定の整備により、開発効率を向上させる。

#### 技術的な背景
開発ワークフローの効率化や依存関係の更新など、非機能的な改善を実施。

#### 変更の影響範囲
影響を受けるレイヤ: 特定のレイヤに限定されない。変更は関連するテストおよびドキュメントへの波及を考慮する必要がある。

### 変更内容

```diff
commit 28fcf4617d4a12b94c9d68804fc49d14dcac0c66
Author: k2works <kakimomokuri@gmail.com>
Date:   Tue Dec 3 19:54:15 2024 +0900

    chore:ビルドタスク
    
    Add build tasks for API and app in Gulp
    
    Introduce new build tasks for the API and app within the Gulp custom tasks. These additions enable running 'gradlew build' for the API and 'npm install && npm run build' for the app, enhancing the build process. The changes streamline build automation by integrating these steps into existing Gulp task sequences.

diff --git a/gulpfile.cjs b/gulpfile.cjs
index 55923a8b..0ec91c04 100644
--- a/gulpfile.cjs
+++ b/gulpfile.cjs
@@ -4,6 +4,7 @@ const custom = require('./ops/gulp/tasks/custom');
 
 const build = series(
     core.webpackBuildTasks(),
+    custom.appBuildTasks(),
     parallel(
         custom.assetsBuildTasks(),
         core.asciidoctorBuildTasks(),
diff --git a/ops/gulp/tasks/custom.js b/ops/gulp/tasks/custom.js
index 28787982..f6e1157f 100644
--- a/ops/gulp/tasks/custom.js
+++ b/ops/gulp/tasks/custom.js
@@ -19,6 +19,11 @@ const api = {
         const command = isWindows ? 'gradlew.bat bootRun' : './gradlew bootRun';
         return src(apiGradlewPath, { read: false })
             .pipe(exec(command, { cwd: apiCwd }));
+    },
+    build: () => {
+        const command = isWindows ? 'gradlew.bat build' : './gradlew build';
+        return src(apiGradlewPath, { read: false })
+            .pipe(exec(command, { cwd: apiCwd }));
     }
 }
 
@@ -27,6 +32,11 @@ const app = {
         const command = 'npm run dev';
         return src(apiGradlewPath, { read: false })
             .pipe(exec(command, { cwd: appCwd }));
+    },
+    build: () => {
+        const command = 'npm install && npm run build';
+        return src(apiGradlewPath, { read: false })
+            .pipe(exec(command, { cwd: appCwd }));
     }
 }
 
@@ -258,6 +268,11 @@ const jigErdBuildTasks = () => {
 
 exports.app = app;
 exports.api = api;
+
+const appBuildTasks = () => {
+    return series(app.build, api.build);
+}
+exports.appBuildTasks = appBuildTasks;
 exports.jig = jig;
 exports.jig_erd = jig_erd;
 exports.erd = erd;
@@ -271,6 +286,6 @@ const allureBuildTasks = () => {
 }
 exports.allureBuildTasks = allureBuildTasks;
 const allureGradleBuildTasks = () => {
-    return series(allure.build_gradle, allure.copy_build_gradle, allure.publish);
+    return series(api.build, allure.build_gradle, allure.copy_build_gradle, allure.publish);
 }
 exports.allureGradleBuildTasks = allureGradleBuildTasks;
\ No newline at end of file

```

## コミット: 27d7b7a3

### メッセージ

```
fix:受入テスト
Add application properties and update API URLs for tests

Introduce `application.properties` file for test configurations including database and server settings. Refactor test step definitions to use dynamic host and port variables, enhancing maintainability and consistency across tests.
```

### 変更されたファイル

- M	app/backend/api/src/test/java/com/example/sms/stepdefinitions/AuthStepdefs.java
- M	app/backend/api/src/test/java/com/example/sms/stepdefinitions/UserStepDefs.java
- A	app/backend/api/src/test/resources/application.properties

### 作業解説

#### 目的
バグ修正または不具合対応を行い、システムの安定性・信頼性を向上させる。

#### 技術的な背景
既存機能における問題点を特定し、適切な修正を実施。修正により、想定される使用シナリオでの正常動作を確保。

#### 変更の影響範囲
影響を受けるレイヤ: プレゼンテーション層、テスト。変更は関連するテストおよびドキュメントへの波及を考慮する必要がある。

### 変更内容

```diff
commit 27d7b7a3648ddf6f863b5bd4024cc319cf744a90
Author: k2works <kakimomokuri@gmail.com>
Date:   Tue Dec 3 19:38:46 2024 +0900

    fix:受入テスト
    
    Add application properties and update API URLs for tests
    
    Introduce `application.properties` file for test configurations including database and server settings. Refactor test step definitions to use dynamic host and port variables, enhancing maintainability and consistency across tests.

diff --git a/app/backend/api/src/test/java/com/example/sms/stepdefinitions/AuthStepdefs.java b/app/backend/api/src/test/java/com/example/sms/stepdefinitions/AuthStepdefs.java
index d20bc159..208e2072 100644
--- a/app/backend/api/src/test/java/com/example/sms/stepdefinitions/AuthStepdefs.java
+++ b/app/backend/api/src/test/java/com/example/sms/stepdefinitions/AuthStepdefs.java
@@ -12,6 +12,11 @@ import org.springframework.beans.factory.annotation.Autowired;
 import java.io.IOException;
 
 public class AuthStepdefs extends SpringAcceptanceTest {
+    private static final String PORT = "8079";
+    private static final String HOST = "http://localhost:" + PORT;
+    private static final String AUTH_API_URL = HOST + "/api/auth";
+    private static final String TEST_API_URL = HOST + "/api/test";
+
     @Autowired
     UserRepository userRepository;
 
@@ -24,7 +29,7 @@ public class AuthStepdefs extends SpringAcceptanceTest {
 
     @もし(": {string} でログインする")
     public void login(String user) {
-        String url = "http://localhost:8080/api/auth/signin";
+        String url = AUTH_API_URL + "/signin";
         if (user.equals("管理者権限")) {
             signin("U888888", "demo", url);
         } else {
@@ -35,9 +40,9 @@ public class AuthStepdefs extends SpringAcceptanceTest {
     @ならば(": {string} として認証される")
     public void authentication(String user) throws IOException {
         if (user.equals("管理者")) {
-            executeGet("http://localhost:8080/api/test/admin");
+            executeGet(TEST_API_URL + "/admin");
         } else {
-            executeGet("http://localhost:8080/api/test/user");
+            executeGet(TEST_API_URL + "/user");
         }
     }
 }
diff --git a/app/backend/api/src/test/java/com/example/sms/stepdefinitions/UserStepDefs.java b/app/backend/api/src/test/java/com/example/sms/stepdefinitions/UserStepDefs.java
index 6a69ccc0..8440cf0c 100644
--- a/app/backend/api/src/test/java/com/example/sms/stepdefinitions/UserStepDefs.java
+++ b/app/backend/api/src/test/java/com/example/sms/stepdefinitions/UserStepDefs.java
@@ -19,8 +19,10 @@ import java.util.List;
 import static org.junit.jupiter.api.Assertions.assertEquals;
 
 public class UserStepDefs extends SpringAcceptanceTest {
-    String AUTH_API_URL = "http://localhost:8080/api/auth";
-    String USER_API_URL = "http://localhost:8080/api/users";
+    private static final String PORT = "8079";
+    private static final String HOST = "http://localhost:" + PORT;
+    private static final String AUTH_API_URL = HOST + "/api/auth";
+    private static final String USER_API_URL = HOST + "/api/users";
 
     @前提(": {string} である")
     public void login(String user) {
diff --git a/app/backend/api/src/test/resources/application.properties b/app/backend/api/src/test/resources/application.properties
new file mode 100644
index 00000000..e80a95f4
--- /dev/null
+++ b/app/backend/api/src/test/resources/application.properties
@@ -0,0 +1,26 @@
+spring.application.name=sms
+spring.datasource.driver-class-name=org.h2.Driver
+spring.datasource.url=jdbc:h2:mem:app;MODE=PostgreSQL
+spring.datasource.username=sa
+spring.datasource.password=sa
+spring.h2.console.enabled=true
+spring.jpa.hibernate.ddl-auto=validate
+spring.jpa.properties.hibernate.forma_sql=true
+logging.level.org.hibernate.SQL=DEBUG
+logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
+spring.flyway.enabled=true
+spring.flyway.locations=classpath:/db/migration/h2
+# swagger-ui custom path
+springdoc.swagger-ui.path=/swagger-ui.html
+springdoc.packages-to-scan=com.example.sms.presentation.api
+springdoc.paths-to-match=/api/**
+# App Properties
+sms.jwtSecret=raukSSizGSvoHnsj7Q2HbH8OhBMRJd+DB/mVtc3qA8k=
+sms.jwtExpirationMs=86400000
+logging.level.org.mybatis=DEBUG
+logging.level.java.sql.PreparedStatement=TRACE
+logging.level.com.example.sms.infrastructure.datasource=TRACE
+# Jackson
+spring.jackson.date-format=yyyy-MM-dd'T'HH:mm:ss
+spring.jackson.time-zone=Asia/Tokyo
+server.port=8079
\ No newline at end of file

```

## コミット: 8dec413f

### メッセージ

```
Merge branch 'take03' into develop
```

### 変更されたファイル


### 作業解説

#### 目的
システムの改善・機能拡張を行う。

#### 技術的な背景
プロジェクトの品質向上または機能拡張のための変更を実施。

#### 変更の影響範囲
影響を受けるレイヤ: 特定のレイヤに限定されない。変更は関連するテストおよびドキュメントへの波及を考慮する必要がある。

### 変更内容

```diff
commit 8dec413f1dc39e38b79eba23868f2bae2a375cf2
Merge: 9ce950e9 ab64cbe5
Author: k2works <kakimomokuri@gmail.com>
Date:   Tue Dec 3 18:55:02 2024 +0900

    Merge branch 'take03' into develop


```

## コミット: ab64cbe5

### メッセージ

```
Merge branch 'take02' into take03
```

### 変更されたファイル


### 作業解説

#### 目的
システムの改善・機能拡張を行う。

#### 技術的な背景
プロジェクトの品質向上または機能拡張のための変更を実施。

#### 変更の影響範囲
影響を受けるレイヤ: 特定のレイヤに限定されない。変更は関連するテストおよびドキュメントへの波及を考慮する必要がある。

### 変更内容

```diff
commit ab64cbe52ff1cc7cd895ff15f2cfa992dced1320
Merge: 041b47e5 cc930056
Author: k2works <kakimomokuri@gmail.com>
Date:   Tue Dec 3 18:52:33 2024 +0900

    Merge branch 'take02' into take03


```

## コミット: cc930056

### メッセージ

```
CI:アプリケーション
Add environment variable setup to Vercel workflows

This commit introduces a step to set up environment variables in both Vercel frontend preview and production workflows. The added step configures the API URL for the respective environments by creating a `.env` file, ensuring that the application communicates with the correct API endpoint during the deployment process.
```

### 変更されたファイル

- M	.github/workflows/vercel_frontend_preview.yml
- M	.github/workflows/vercel_frontend_production.yml

### 作業解説

#### 目的
CI/CD パイプラインの改善により、デプロイメントプロセスを最適化する。

#### 技術的な背景
プロジェクトの品質向上または機能拡張のための変更を実施。

#### 変更の影響範囲
影響を受けるレイヤ: フロントエンド。変更は関連するテストおよびドキュメントへの波及を考慮する必要がある。

### 変更内容

```diff
commit cc930056635799f926a7bba3d7653f3aa3c8534a
Author: k2works <kakimomokuri@gmail.com>
Date:   Tue Dec 3 18:28:39 2024 +0900

    CI:アプリケーション
    
    Add environment variable setup to Vercel workflows
    
    This commit introduces a step to set up environment variables in both Vercel frontend preview and production workflows. The added step configures the API URL for the respective environments by creating a `.env` file, ensuring that the application communicates with the correct API endpoint during the deployment process.

diff --git a/.github/workflows/vercel_frontend_preview.yml b/.github/workflows/vercel_frontend_preview.yml
index 8ec7a766..b497a237 100644
--- a/.github/workflows/vercel_frontend_preview.yml
+++ b/.github/workflows/vercel_frontend_preview.yml
@@ -11,6 +11,10 @@ jobs:
     runs-on: ubuntu-latest
     steps:
       - uses: actions/checkout@v2
+      - name: Setup Environment Variables
+        run: |
+          echo VITE_APP_API_URL=${{ vars.DEV_APP_API_URL }} > .env
+        working-directory: app/frontend
       - name: Install Vercel CLI
         run: npm install --global vercel@latest
       - name: Pull Vercel Environment Information
diff --git a/.github/workflows/vercel_frontend_production.yml b/.github/workflows/vercel_frontend_production.yml
index 93cbe103..f474a2c8 100644
--- a/.github/workflows/vercel_frontend_production.yml
+++ b/.github/workflows/vercel_frontend_production.yml
@@ -11,6 +11,10 @@ jobs:
     runs-on: ubuntu-latest
     steps:
       - uses: actions/checkout@v2
+      - name: Setup Environment Variables
+        run: |
+          echo VITE_APP_API_URL=${{ vars.PRD_APP_API_URL }} > .env
+        working-directory: app/frontend
       - name: Install Vercel CLI
         run: npm install --global vercel@latest
       - name: Pull Vercel Environment Information

```

## コミット: 00a9edc3

### メッセージ

```
feat:アプリケーション
Refactor environment handling and update .gitignore

Centralize environment variable management by creating `env.ts` for production checks and API URL, replacing previous inline logic. Update TypeScript configuration to include Vite types, and adjust `.gitignore` to exclude environment files for improved security and consistency.
```

### 変更されたファイル

- M	app/frontend/.gitignore
- A	app/frontend/src/env.ts
- M	app/frontend/src/services/config.ts
- M	app/frontend/tsconfig.node.json

### 作業解説

#### 目的
新機能の実装により、システムの機能性を拡張する。

#### 技術的な背景
ユーザー要件または技術的要件に基づき、新機能を設計・実装。既存アーキテクチャとの整合性を保ちながら拡張。

#### 変更の影響範囲
影響を受けるレイヤ: フロントエンド、サービス層、ビルド設定。変更は関連するテストおよびドキュメントへの波及を考慮する必要がある。

### 変更内容

```diff
commit 00a9edc3bed8530cacec0ff673dd3db2172977ae
Author: k2works <kakimomokuri@gmail.com>
Date:   Tue Dec 3 18:19:19 2024 +0900

    feat:アプリケーション
    
    Refactor environment handling and update .gitignore
    
    Centralize environment variable management by creating `env.ts` for production checks and API URL, replacing previous inline logic. Update TypeScript configuration to include Vite types, and adjust `.gitignore` to exclude environment files for improved security and consistency.

diff --git a/app/frontend/.gitignore b/app/frontend/.gitignore
index 3a51a56f..bd9a1576 100644
--- a/app/frontend/.gitignore
+++ b/app/frontend/.gitignore
@@ -26,3 +26,6 @@ dist-ssr
 .vercel
 
 frontend/cypress/screenshots
+
+.env
+.env.*
diff --git a/app/frontend/src/env.ts b/app/frontend/src/env.ts
new file mode 100644
index 00000000..8a2d9b44
--- /dev/null
+++ b/app/frontend/src/env.ts
@@ -0,0 +1,11 @@
+const Env = (() => {
+    const isProduction = (): boolean => process.env.NODE_ENV === "production";
+    const apiUrl: string = import.meta.env.VITE_APP_API_URL;
+
+    return {
+        isProduction,
+        apiUrl
+    };
+})();
+
+export default Env;
\ No newline at end of file
diff --git a/app/frontend/src/services/config.ts b/app/frontend/src/services/config.ts
index 128b4f4a..5aa7aeba 100644
--- a/app/frontend/src/services/config.ts
+++ b/app/frontend/src/services/config.ts
@@ -1,8 +1,8 @@
+import Env from "../env.ts";
+
 const Config = () => {
     let config: { apiUrl: string, authHeader: string };
-
-    const isProduction = () => process.env.NODE_ENV === "production";
-    const getApiUrl = () => isProduction() ? "https://case-study-sales-api-50defd76b8c9.herokuapp.com/api" : "http://localhost:8080/api";
+    const getApiUrl = () => Env.isProduction() ? Env.apiUrl : "http://localhost:8080/api";
     const user = window.localStorage.getItem("user");
     if (user) {
         config = {apiUrl: getApiUrl(), authHeader: "Bearer " + JSON.parse(user).token};
diff --git a/app/frontend/tsconfig.node.json b/app/frontend/tsconfig.node.json
index 9f244752..4e35730e 100644
--- a/app/frontend/tsconfig.node.json
+++ b/app/frontend/tsconfig.node.json
@@ -16,7 +16,9 @@
     "strict": true,
     "noUnusedLocals": true,
     "noUnusedParameters": true,
-    "noFallthroughCasesInSwitch": true
+    "noFallthroughCasesInSwitch": true,
+
+    "types": ["vite/client"]
   },
   "include": [
     "vite.config.ts"

```

## コミット: 9ce950e9

### メッセージ

```
Merge branch 'take03' into develop
# Conflicts:
#	index.html
```

### 変更されたファイル


### 作業解説

#### 目的
システムの改善・機能拡張を行う。

#### 技術的な背景
プロジェクトの品質向上または機能拡張のための変更を実施。

#### 変更の影響範囲
影響を受けるレイヤ: 特定のレイヤに限定されない。変更は関連するテストおよびドキュメントへの波及を考慮する必要がある。

### 変更内容

```diff
commit 9ce950e9dcb55a724513f15321ab085ce6db2cab
Merge: 9d459681 041b47e5
Author: k2works <kakimomokuri@gmail.com>
Date:   Tue Dec 3 16:44:55 2024 +0900

    Merge branch 'take03' into develop
    
    # Conflicts:
    #       index.html


```

## コミット: 041b47e5

### メッセージ

```
fix:アプリケーション
Update version number in header to Ver0.2.0.

This change reflects the latest version of the Sales Management System on the webpage header, aligning it with recent updates and improvements. Ensure all references are consistent across documentation and interface.
```

### 変更されたファイル

- M	index.html

### 作業解説

#### 目的
バグ修正または不具合対応を行い、システムの安定性・信頼性を向上させる。

#### 技術的な背景
既存機能における問題点を特定し、適切な修正を実施。修正により、想定される使用シナリオでの正常動作を確保。

#### 変更の影響範囲
影響を受けるレイヤ: 特定のレイヤに限定されない。変更は関連するテストおよびドキュメントへの波及を考慮する必要がある。

### 変更内容

```diff
commit 041b47e520d91c3250f5ecc8deac131ef59b1f2e
Author: k2works <kakimomokuri@gmail.com>
Date:   Tue Dec 3 16:40:33 2024 +0900

    fix:アプリケーション
    
    Update version number in header to Ver0.2.0.
    
    This change reflects the latest version of the Sales Management System on the webpage header, aligning it with recent updates and improvements. Ensure all references are consistent across documentation and interface.

diff --git a/index.html b/index.html
index 3096ee57..f2db552d 100644
--- a/index.html
+++ b/index.html
@@ -15,7 +15,7 @@
 
 <body>
 <div id="header">
-  <h1>販売管理システム Ver0.1.0</h1>
+  <h1>販売管理システム Ver0.2.0</h1>
 </div>
 <div id="nav">
   <ul class="nav-sub-list">

```

## コミット: cb00fab1

### メッセージ

```
Merge branch 'take02' into take03
```

### 変更されたファイル


### 作業解説

#### 目的
システムの改善・機能拡張を行う。

#### 技術的な背景
プロジェクトの品質向上または機能拡張のための変更を実施。

#### 変更の影響範囲
影響を受けるレイヤ: 特定のレイヤに限定されない。変更は関連するテストおよびドキュメントへの波及を考慮する必要がある。

### 変更内容

```diff
commit cb00fab1922e6cc0711c6fd264fa73d59affe9c8
Merge: a293fc1f cc42666b
Author: k2works <kakimomokuri@gmail.com>
Date:   Tue Dec 3 16:39:04 2024 +0900

    Merge branch 'take02' into take03


```

## コミット: cc42666b

### メッセージ

```
docs:README
Add status badges for Heroku and Vercel workflows

Included badges for Heroku production and develop environments, as well as Vercel production and develop workflows in the README. Also corrected a typo in the Vercel production workflow file name for consistency.
```

### 変更されたファイル

- M	README.md

### 作業解説

#### 目的
ドキュメントの更新により、プロジェクトの理解しやすさを向上させる。

#### 技術的な背景
プロジェクトドキュメントの追加・更新により、知識の共有と将来の開発効率向上を図る。

#### 変更の影響範囲
影響を受けるレイヤ: 特定のレイヤに限定されない。変更は関連するテストおよびドキュメントへの波及を考慮する必要がある。

### 変更内容

```diff
commit cc42666b2d58e24ad02abf7271bf6994da8dff4d
Author: k2works <kakimomokuri@gmail.com>
Date:   Tue Dec 3 16:35:04 2024 +0900

    docs:README
    
    Add status badges for Heroku and Vercel workflows
    
    Included badges for Heroku production and develop environments, as well as Vercel production and develop workflows in the README. Also corrected a typo in the Vercel production workflow file name for consistency.

diff --git a/.github/workflows/vercel_frontend_productin.yml b/.github/workflows/vercel_frontend_production.yml
similarity index 100%
rename from .github/workflows/vercel_frontend_productin.yml
rename to .github/workflows/vercel_frontend_production.yml
diff --git a/README.md b/README.md
index 62acdfb5..749b0eaa 100644
--- a/README.md
+++ b/README.md
@@ -3,6 +3,13 @@
 [![Node.js CI](https://github.com/k2works/case-study-sales/actions/workflows/node.js.yml/badge.svg)](https://github.com/k2works/case-study-sales/actions/workflows/node.js.yml)
 [![Java CI](https://github.com/k2works/case-study-sales/actions/workflows/gradle.yml/badge.svg)](https://github.com/k2works/case-study-sales/actions/workflows/gradle.yml)
 
+[![Heroku Production](https://github.com/k2works/case-study-sales/actions/workflows/heroku_backend_production.yml/badge.svg)](https://github.com/k2works/case-study-sales/actions/workflows/heroku_backend_production.yml)
+[![Heroku Develop](https://github.com/k2works/case-study-sales/actions/workflows/heroku_backend_develop.yml/badge.svg)](https://github.com/k2works/case-study-sales/actions/workflows/heroku_backend_develop.yml)
+
+
+[![Vercel Production](https://github.com/k2works/case-study-sales/actions/workflows/vercel_frontend_production.yml/badge.svg)](https://github.com/k2works/case-study-sales/actions/workflows/vercel_frontend_production.yml)
+[![Vercel Develop](https://github.com/k2works/case-study-sales/actions/workflows/vercel_frontend_preview.yml/badge.svg)](https://github.com/k2works/case-study-sales/actions/workflows/vercel_frontend_preview.yml)
+
 ## 概要
 
 [ガイドライン](./docs/slides/PITCHME.md)

```

## コミット: e2b48612

### メッセージ

```
CI:プロジェクト
Update GitHub Actions and improve directory preparation

Add Java setup to GitHub Actions workflow for better integration with Gradle, granting execute permission for the Gradle wrapper. Modify `prepareDirectories` to accept customizable paths and update related functions to support this, ensuring better directory management in custom Gulp tasks. Also, remove the redundant command in the "test" script of `package.json`.
```

### 変更されたファイル

- M	.github/workflows/node.js.yml
- M	ops/gulp/tasks/custom.js
- M	package.json

### 作業解説

#### 目的
CI/CD パイプラインの改善により、デプロイメントプロセスを最適化する。

#### 技術的な背景
プロジェクトの品質向上または機能拡張のための変更を実施。

#### 変更の影響範囲
影響を受けるレイヤ: ビルド設定。変更は関連するテストおよびドキュメントへの波及を考慮する必要がある。

### 変更内容

```diff
commit e2b486129695eb6f7c2e7123b4cc2b7c155587db
Author: k2works <kakimomokuri@gmail.com>
Date:   Tue Dec 3 16:29:31 2024 +0900

    CI:プロジェクト
    
    Update GitHub Actions and improve directory preparation
    
    Add Java setup to GitHub Actions workflow for better integration with Gradle, granting execute permission for the Gradle wrapper. Modify `prepareDirectories` to accept customizable paths and update related functions to support this, ensuring better directory management in custom Gulp tasks. Also, remove the redundant command in the "test" script of `package.json`.

diff --git a/.github/workflows/node.js.yml b/.github/workflows/node.js.yml
index 259fb27d..d0bb1269 100644
--- a/.github/workflows/node.js.yml
+++ b/.github/workflows/node.js.yml
@@ -21,6 +21,16 @@ jobs:
 
     steps:
     - uses: actions/checkout@v3
+
+    - name: Setup JDK
+      uses: actions/setup-java@v3
+      with:
+        java-version: '21'
+        distribution: 'temurin'
+    - name: Grant execute permission for Gradle wrapper
+      run: chmod +x ./gradlew
+      working-directory: app/backend/api
+
     - name: Use Node.js ${{ matrix.node-version }}
       uses: actions/setup-node@v3
       with:
diff --git a/ops/gulp/tasks/custom.js b/ops/gulp/tasks/custom.js
index 8cfb5933..28787982 100644
--- a/ops/gulp/tasks/custom.js
+++ b/ops/gulp/tasks/custom.js
@@ -196,21 +196,23 @@ const assets = {
         cb();
     },
 };
-const prepareDirectories = async () => {
-    await fs.mkdirs('./ops/docker/allure/allure-reports');
+const prepareDirectories = async (path) => {
+    await fs.mkdirs(path);
 }
 const allure = {
     build: () => {
-         console.log("See osp/docker/allure/allure-reports");
-         runExecCommand('docker-compose run --rm allure');
+        console.log("See osp/docker/allure/allure-reports");
+        runExecCommand('docker-compose run --rm allure');
     },
     build_gradle: () => {
         const command = isWindows ? 'gradlew.bat allureReport' : './gradlew allureReport';
         return src(apiGradlewPath, { read: false })
             .pipe(exec(command, { cwd: apiCwd }));
     },
-    copy_build_gradle: () => {
-        return src(path.join(apiCwd, 'build/reports/allure-report/**'))
+    copy_build_gradle: async () => {
+        const sourcePath = path.join(apiCwd, 'build/reports/allure-report');
+        await prepareDirectories(sourcePath);
+        return src(sourcePath + '/**')
             .pipe(dest('./ops/docker/allure/allure-reports'));
     },
     clean: async (cb) => {
@@ -224,7 +226,7 @@ const allure = {
             .pipe(dest('./ops/docker/allure/allure-results'));
     },
     publish: async () => {
-        await prepareDirectories();
+        await prepareDirectories('./ops/docker/allure/allure-reports');
         return src('./ops/docker/allure/allure-reports/**')
             .pipe(dest('./public/allure'));
     }
diff --git a/package.json b/package.json
index 397e9ba6..4154fafe 100644
--- a/package.json
+++ b/package.json
@@ -6,7 +6,7 @@
   "scripts": {
     "start": "npx gulp",
     "dev": "npx gulp dev",
-    "test": "npm-run-all build",
+    "test": "",
     "build": "npx gulp build",
     "build:slides": "npx gulp slides",
     "build:docs": "npx gulp buildDocs",

```

## コミット: 1e09f0ff

### メッセージ

```
CI:Apiデプロイ
Add GitHub Action for Heroku backend deployment

Introduce a new GitHub workflow to automate the deployment of the backend API to Heroku. This setup uses specific secrets for authentication and targets the appropriate application directory. The workflow is triggered on pushes to all branches except main.
```

### 変更されたファイル

- A	.github/workflows/heroku_backend_develop.yml
- A	.github/workflows/heroku_backend_production.yml

### 作業解説

#### 目的
CI/CD パイプラインの改善により、デプロイメントプロセスを最適化する。

#### 技術的な背景
プロジェクトの品質向上または機能拡張のための変更を実施。

#### 変更の影響範囲
影響を受けるレイヤ: 特定のレイヤに限定されない。変更は関連するテストおよびドキュメントへの波及を考慮する必要がある。

### 変更内容

```diff
commit 1e09f0ffb5395c3b1b48bdf9867ef9a8f9a8e2fd
Author: k2works <kakimomokuri@gmail.com>
Date:   Tue Dec 3 15:59:12 2024 +0900

    CI:Apiデプロイ
    
    Add GitHub Action for Heroku backend deployment
    
    Introduce a new GitHub workflow to automate the deployment of the backend API to Heroku. This setup uses specific secrets for authentication and targets the appropriate application directory. The workflow is triggered on pushes to all branches except main.

diff --git a/.github/workflows/heroku_backend_develop.yml b/.github/workflows/heroku_backend_develop.yml
new file mode 100644
index 00000000..ba6d0cd5
--- /dev/null
+++ b/.github/workflows/heroku_backend_develop.yml
@@ -0,0 +1,18 @@
+name: Heroku Backend Develop Deployment
+
+on:
+  push:
+    branches-ignore:
+      - main
+
+jobs:
+  build:
+    runs-on: ubuntu-22.04
+    steps:
+      - uses: actions/checkout@v2
+      - uses: akhileshns/heroku-deploy@v3.13.15
+        with:
+          heroku_api_key: ${{secrets.HEROKU_API_KEY}}
+          heroku_app_name: ${{secrets.HEROKU_APP_DEVELOP_NAME}}
+          heroku_email: ${{secrets.HEROKU_EMAIL}}
+          appdir: "app/backend/api" # <- This will point to the api folder in your project
diff --git a/.github/workflows/heroku_backend_production.yml b/.github/workflows/heroku_backend_production.yml
new file mode 100644
index 00000000..3f7cf228
--- /dev/null
+++ b/.github/workflows/heroku_backend_production.yml
@@ -0,0 +1,18 @@
+name: Heroku Backend Production Deployment
+
+on:
+  push:
+    branches:
+      - main
+
+jobs:
+  build:
+    runs-on: ubuntu-22.04
+    steps:
+      - uses: actions/checkout@v2
+      - uses: akhileshns/heroku-deploy@v3.13.15
+        with:
+          heroku_api_key: ${{secrets.HEROKU_API_KEY}}
+          heroku_app_name: ${{secrets.HEROKU_APP_NAME}}
+          heroku_email: ${{secrets.HEROKU_EMAIL}}
+          appdir: "app/backend/api" # <- This will point to the api folder in your project

```

## コミット: 9d459681

### メッセージ

```
CI:プロジェクト
Add Java setup and grant execute permission for Gradle

Integrate JDK setup with Temurin distribution and Java 21 version in the Node.js GitHub Actions workflow. Also, grant execute permission to the Gradle wrapper to enable its execution during automated processes.
```

### 変更されたファイル

- M	package.json

### 作業解説

#### 目的
CI/CD パイプラインの改善により、デプロイメントプロセスを最適化する。

#### 技術的な背景
プロジェクトの品質向上または機能拡張のための変更を実施。

#### 変更の影響範囲
影響を受けるレイヤ: ビルド設定。変更は関連するテストおよびドキュメントへの波及を考慮する必要がある。

### 変更内容

```diff
commit 9d45968127bde24b0a91464c25bb56bc9817df83
Author: k2works <kakimomokuri@gmail.com>
Date:   Tue Dec 3 15:47:45 2024 +0900

    CI:プロジェクト
    
    Add Java setup and grant execute permission for Gradle
    
    Integrate JDK setup with Temurin distribution and Java 21 version in the Node.js GitHub Actions workflow. Also, grant execute permission to the Gradle wrapper to enable its execution during automated processes.

diff --git a/package.json b/package.json
index 397e9ba6..4154fafe 100644
--- a/package.json
+++ b/package.json
@@ -6,7 +6,7 @@
   "scripts": {
     "start": "npx gulp",
     "dev": "npx gulp dev",
-    "test": "npm-run-all build",
+    "test": "",
     "build": "npx gulp build",
     "build:slides": "npx gulp slides",
     "build:docs": "npx gulp buildDocs",

```

## コミット: b04d5423

### メッセージ

```
CI:プロジェクト
Add Java setup and grant execute permission for Gradle

Integrate JDK setup with Temurin distribution and Java 21 version in the Node.js GitHub Actions workflow. Also, grant execute permission to the Gradle wrapper to enable its execution during automated processes.
```

### 変更されたファイル

- M	.github/workflows/node.js.yml
- M	ops/gulp/tasks/custom.js

### 作業解説

#### 目的
CI/CD パイプラインの改善により、デプロイメントプロセスを最適化する。

#### 技術的な背景
プロジェクトの品質向上または機能拡張のための変更を実施。

#### 変更の影響範囲
影響を受けるレイヤ: 特定のレイヤに限定されない。変更は関連するテストおよびドキュメントへの波及を考慮する必要がある。

### 変更内容

```diff
commit b04d542309c45571082c022930987a4598172425
Author: k2works <kakimomokuri@gmail.com>
Date:   Tue Dec 3 15:09:58 2024 +0900

    CI:プロジェクト
    
    Add Java setup and grant execute permission for Gradle
    
    Integrate JDK setup with Temurin distribution and Java 21 version in the Node.js GitHub Actions workflow. Also, grant execute permission to the Gradle wrapper to enable its execution during automated processes.

diff --git a/.github/workflows/node.js.yml b/.github/workflows/node.js.yml
index 259fb27d..d0bb1269 100644
--- a/.github/workflows/node.js.yml
+++ b/.github/workflows/node.js.yml
@@ -21,6 +21,16 @@ jobs:
 
     steps:
     - uses: actions/checkout@v3
+
+    - name: Setup JDK
+      uses: actions/setup-java@v3
+      with:
+        java-version: '21'
+        distribution: 'temurin'
+    - name: Grant execute permission for Gradle wrapper
+      run: chmod +x ./gradlew
+      working-directory: app/backend/api
+
     - name: Use Node.js ${{ matrix.node-version }}
       uses: actions/setup-node@v3
       with:
diff --git a/ops/gulp/tasks/custom.js b/ops/gulp/tasks/custom.js
index b61c0cdd..d669b045 100644
--- a/ops/gulp/tasks/custom.js
+++ b/ops/gulp/tasks/custom.js
@@ -202,8 +202,8 @@ const assets = {
         cb();
     },
 };
-const prepareDirectories = async () => {
-    await fs.mkdirs('./ops/docker/allure/allure-reports');
+const prepareDirectories = async (path) => {
+    await fs.mkdirs(path);
 }
 const allure = {
     build: () => {
@@ -215,8 +215,10 @@ const allure = {
         return src(apiGradlewPath, { read: false })
             .pipe(exec(command, { cwd: apiCwd }));
     },
-    copy_build_gradle: () => {
-        return src(path.join(apiCwd, 'build/reports/allure-report/**'))
+    copy_build_gradle: async () => {
+        const sourcePath = path.join(apiCwd, 'build/reports/allure-report');
+        await prepareDirectories(sourcePath);
+        return src(sourcePath + '/**')
             .pipe(dest('./ops/docker/allure/allure-reports'));
     },
     clean: async (cb) => {
@@ -230,7 +232,7 @@ const allure = {
             .pipe(dest('./ops/docker/allure/allure-results'));
     },
     publish: async () => {
-        await prepareDirectories();
+        await prepareDirectories('./ops/docker/allure/allure-reports');
         return src('./ops/docker/allure/allure-reports/**')
             .pipe(dest('./public/allure'));
     }

```

## コミット: 55cb7a40

### メッセージ

```
Merge branch 'take03' into develop
# Conflicts:
#	index.html
```

### 変更されたファイル


### 作業解説

#### 目的
システムの改善・機能拡張を行う。

#### 技術的な背景
プロジェクトの品質向上または機能拡張のための変更を実施。

#### 変更の影響範囲
影響を受けるレイヤ: 特定のレイヤに限定されない。変更は関連するテストおよびドキュメントへの波及を考慮する必要がある。

### 変更内容

```diff
commit 55cb7a40a76fb7fd119eb4e22b6d9210410de509
Merge: 809aa4c3 a293fc1f
Author: k2works <kakimomokuri@gmail.com>
Date:   Tue Dec 3 14:54:27 2024 +0900

    Merge branch 'take03' into develop
    
    # Conflicts:
    #       index.html


```

## コミット: a293fc1f

### メッセージ

```
fix:ドキュメント
Update navigation links to include 'index.html' paths

Corrected the URLs for several navigation items by appending 'index.html' to their paths, ensuring they now point to the intended landing pages. This change affects links for JIG, JIG-ERD, and ERD PostgreSQL, improving accessibility and user navigation.
```

### 変更されたファイル

- M	index.html

### 作業解説

#### 目的
バグ修正または不具合対応を行い、システムの安定性・信頼性を向上させる。

#### 技術的な背景
既存機能における問題点を特定し、適切な修正を実施。修正により、想定される使用シナリオでの正常動作を確保。

#### 変更の影響範囲
影響を受けるレイヤ: 特定のレイヤに限定されない。変更は関連するテストおよびドキュメントへの波及を考慮する必要がある。

### 変更内容

```diff
commit a293fc1fecac290780ffb48d668c000b73ac09d9
Author: k2works <kakimomokuri@gmail.com>
Date:   Tue Dec 3 14:44:45 2024 +0900

    fix:ドキュメント
    
    Update navigation links to include 'index.html' paths
    
    Corrected the URLs for several navigation items by appending 'index.html' to their paths, ensuring they now point to the intended landing pages. This change affects links for JIG, JIG-ERD, and ERD PostgreSQL, improving accessibility and user navigation.

diff --git a/index.html b/index.html
index 834d33df..3096ee57 100644
--- a/index.html
+++ b/index.html
@@ -25,10 +25,10 @@
     <li class="nav-sub-item"><a href="docs" target="_blank">ドキュメント</a></li>
     <li class="nav-sub-item"><a href="docs/adr/export.html" target="_blank">ADR</a></li>
     <li class="nav-sub-item"><a href="assets/release/index.html" target="_blank">リリース</a></li>
-    <li class="nav-sub-item"><a href="jig" target="_blank">JIG</a></li>
-    <li class="nav-sub-item"><a href="jig-erd" target="_blank">JIG-ERD</a></li>
+    <li class="nav-sub-item"><a href="jig/index.html" target="_blank">JIG</a></li>
+    <li class="nav-sub-item"><a href="jig-erd/index.html" target="_blank">JIG-ERD</a></li>
     <li class="nav-sub-item"><a href="allure/allureReport/index.html" target="_blank">Allure Report</a></li>
-    <li class="nav-sub-item"><a href="erd/postgresql" target="_blank">ERD Postgresql</a></li>
+    <li class="nav-sub-item"><a href="erd/postgresql/index.html" target="_blank">ERD Postgresql</a></li>
     <li class="nav-sub-item"><a href="http://localhost:8080/swagger-ui/index.html" target="_blank">SwaggerUI</a></li>
     <li class="nav-sub-item"><a href="http://localhost:8080/h2-console" target="_blank">H2 DataBase</a></li>
     <li class="nav-sub-item"><a href="https://plantuml.com/ja/" target="_blank">PlantUML</a></li>

```

## コミット: 392977ce

### メッセージ

```
CI:プロジェクト
Add Vercel preview deployment workflow

This workflow automates deployment of preview environments using Vercel for branches other than main. It installs the Vercel CLI, retrieves environment information, and builds and deploys project artifacts to Vercel. This setup facilitates easier testing of changes before merging.
```

### 変更されたファイル

- M	index.html

### 作業解説

#### 目的
CI/CD パイプラインの改善により、デプロイメントプロセスを最適化する。

#### 技術的な背景
プロジェクトの品質向上または機能拡張のための変更を実施。

#### 変更の影響範囲
影響を受けるレイヤ: 特定のレイヤに限定されない。変更は関連するテストおよびドキュメントへの波及を考慮する必要がある。

### 変更内容

```diff
commit 392977ce7a745ad07510cc2d639f662966c9c3fa
Author: k2works <kakimomokuri@gmail.com>
Date:   Tue Dec 3 14:33:17 2024 +0900

    CI:プロジェクト
    
    Add Vercel preview deployment workflow
    
    This workflow automates deployment of preview environments using Vercel for branches other than main. It installs the Vercel CLI, retrieves environment information, and builds and deploys project artifacts to Vercel. This setup facilitates easier testing of changes before merging.

diff --git a/index.html b/index.html
index 4e46759f..834d33df 100644
--- a/index.html
+++ b/index.html
@@ -24,14 +24,13 @@
     <li class="nav-sub-item"><a href="docs/slides" target="_blank">ガイド</a></li>
     <li class="nav-sub-item"><a href="docs" target="_blank">ドキュメント</a></li>
     <li class="nav-sub-item"><a href="docs/adr/export.html" target="_blank">ADR</a></li>
-    <li class="nav-sub-item"><a href="assets/release/index.html" target="_blank">JIG</a></li>
     <li class="nav-sub-item"><a href="assets/release/index.html" target="_blank">リリース</a></li>
     <li class="nav-sub-item"><a href="jig" target="_blank">JIG</a></li>
     <li class="nav-sub-item"><a href="jig-erd" target="_blank">JIG-ERD</a></li>
+    <li class="nav-sub-item"><a href="allure/allureReport/index.html" target="_blank">Allure Report</a></li>
     <li class="nav-sub-item"><a href="erd/postgresql" target="_blank">ERD Postgresql</a></li>
     <li class="nav-sub-item"><a href="http://localhost:8080/swagger-ui/index.html" target="_blank">SwaggerUI</a></li>
     <li class="nav-sub-item"><a href="http://localhost:8080/h2-console" target="_blank">H2 DataBase</a></li>
-    <li class="nav-sub-item"><a href="allure/allureReport/index.html" target="_blank">Allure Report</a></li>
     <li class="nav-sub-item"><a href="https://plantuml.com/ja/" target="_blank">PlantUML</a></li>
     <li class="nav-sub-item"><a href="https://docs.asciidoctor.org/asciidoc/latest/" target="_blank">AsciiDoc</a></li>
     <li class="nav-sub-item"><a href="https://mermaid.js.org/intro/" target="_blank">Mermaid</a></li>

```

## コミット: a7ada0d3

### メッセージ

```
Merge branch 'take02' into take03
# Conflicts:
#	gulpfile.cjs
#	index.html
```

### 変更されたファイル


### 作業解説

#### 目的
システムの改善・機能拡張を行う。

#### 技術的な背景
プロジェクトの品質向上または機能拡張のための変更を実施。

#### 変更の影響範囲
影響を受けるレイヤ: 特定のレイヤに限定されない。変更は関連するテストおよびドキュメントへの波及を考慮する必要がある。

### 変更内容

```diff
commit a7ada0d30606228f912ffb2c7b0ae20cad72bd54
Merge: e6d3a480 18c5b9e6
Author: k2works <kakimomokuri@gmail.com>
Date:   Tue Dec 3 14:20:42 2024 +0900

    Merge branch 'take02' into take03
    
    # Conflicts:
    #       gulpfile.cjs
    #       index.html

diff --cc index.html
index 1cd71308,43a199bf..4e46759f
--- a/index.html
+++ b/index.html
@@@ -24,13 -24,8 +24,14 @@@
      <li class="nav-sub-item"><a href="docs/slides" target="_blank">ガイド</a></li>
      <li class="nav-sub-item"><a href="docs" target="_blank">ドキュメント</a></li>
      <li class="nav-sub-item"><a href="docs/adr/export.html" target="_blank">ADR</a></li>
+     <li class="nav-sub-item"><a href="assets/release/index.html" target="_blank">JIG</a></li>
 +    <li class="nav-sub-item"><a href="assets/release/index.html" target="_blank">リリース</a></li>
 +    <li class="nav-sub-item"><a href="jig" target="_blank">JIG</a></li>
 +    <li class="nav-sub-item"><a href="jig-erd" target="_blank">JIG-ERD</a></li>
 +    <li class="nav-sub-item"><a href="erd/postgresql" target="_blank">ERD Postgresql</a></li>
 +    <li class="nav-sub-item"><a href="http://localhost:8080/swagger-ui/index.html" target="_blank">SwaggerUI</a></li>
 +    <li class="nav-sub-item"><a href="http://localhost:8080/h2-console" target="_blank">H2 DataBase</a></li>
-     <li class="nav-sub-item"><a href="allure/latest/index.html" target="_blank">Allure Report</a></li>
+     <li class="nav-sub-item"><a href="allure/allureReport/index.html" target="_blank">Allure Report</a></li>
      <li class="nav-sub-item"><a href="https://plantuml.com/ja/" target="_blank">PlantUML</a></li>
      <li class="nav-sub-item"><a href="https://docs.asciidoctor.org/asciidoc/latest/" target="_blank">AsciiDoc</a></li>
      <li class="nav-sub-item"><a href="https://mermaid.js.org/intro/" target="_blank">Mermaid</a></li>

```

## コミット: 18c5b9e6

### メッセージ

```
CI:プロジェクト
Add Vercel preview deployment workflow

This workflow automates deployment of preview environments using Vercel for branches other than main. It installs the Vercel CLI, retrieves environment information, and builds and deploys project artifacts to Vercel. This setup facilitates easier testing of changes before merging.
```

### 変更されたファイル

- A	.github/workflows/vercel_frontend_preview.yml
- A	.github/workflows/vercel_frontend_productin.yml
- A	.github/workflows/vercel_preview.yml
- A	.github/workflows/vercel_productin.yml
- M	gulpfile.cjs
- M	index.html
- M	ops/docker/postgres/init/1_create.sql
- M	ops/gulp/tasks/custom.js
- M	package.json

### 作業解説

#### 目的
CI/CD パイプラインの改善により、デプロイメントプロセスを最適化する。

#### 技術的な背景
プロジェクトの品質向上または機能拡張のための変更を実施。

#### 変更の影響範囲
影響を受けるレイヤ: フロントエンド、データベース、ビルド設定。変更は関連するテストおよびドキュメントへの波及を考慮する必要がある。

### 変更内容

```diff
commit 18c5b9e6c68ed6b5be0821ce368d33df33f881ba
Author: k2works <kakimomokuri@gmail.com>
Date:   Mon Dec 2 19:02:05 2024 +0900

    CI:プロジェクト
    
    Add Vercel preview deployment workflow
    
    This workflow automates deployment of preview environments using Vercel for branches other than main. It installs the Vercel CLI, retrieves environment information, and builds and deploys project artifacts to Vercel. This setup facilitates easier testing of changes before merging.

diff --git a/.github/workflows/vercel_frontend_preview.yml b/.github/workflows/vercel_frontend_preview.yml
new file mode 100644
index 00000000..8ec7a766
--- /dev/null
+++ b/.github/workflows/vercel_frontend_preview.yml
@@ -0,0 +1,24 @@
+name: Vercel FrontEnd Preview Deployment
+env:
+  VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
+  VERCEL_PROJECT_ID: ${{ secrets.VERCEL_FRONTEND_PROJECT_ID }}
+on:
+  push:
+    branches-ignore:
+      - main
+jobs:
+  Deploy-Preview:
+    runs-on: ubuntu-latest
+    steps:
+      - uses: actions/checkout@v2
+      - name: Install Vercel CLI
+        run: npm install --global vercel@latest
+      - name: Pull Vercel Environment Information
+        run: vercel pull --yes --environment=preview --token=${{ secrets.VERCEL_TOKEN }}
+        working-directory: app/frontend
+      - name: Build Project Artifacts
+        run: vercel build --token=${{ secrets.VERCEL_TOKEN }}
+        working-directory: app/frontend
+      - name: Deploy Project Artifacts to Vercel
+        run: vercel deploy --prebuilt --token=${{ secrets.VERCEL_TOKEN }}
+        working-directory: app/frontend
diff --git a/.github/workflows/vercel_frontend_productin.yml b/.github/workflows/vercel_frontend_productin.yml
new file mode 100644
index 00000000..93cbe103
--- /dev/null
+++ b/.github/workflows/vercel_frontend_productin.yml
@@ -0,0 +1,24 @@
+name: Vercel FrontEnd Production Deployment
+env:
+  VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
+  VERCEL_PROJECT_ID: ${{ secrets.VERCEL_FRONTEND_PROJECT_ID }}
+on:
+  push:
+    branches:
+      - main
+jobs:
+  Deploy-Production:
+    runs-on: ubuntu-latest
+    steps:
+      - uses: actions/checkout@v2
+      - name: Install Vercel CLI
+        run: npm install --global vercel@latest
+      - name: Pull Vercel Environment Information
+        run: vercel pull --yes --environment=production --token=${{ secrets.VERCEL_TOKEN }}
+        working-directory: app/frontend
+      - name: Build Project Artifacts
+        run: vercel build --prod --token=${{ secrets.VERCEL_TOKEN }}
+        working-directory: app/frontend
+      - name: Deploy Project Artifacts to Vercel
+        run: vercel deploy --prebuilt --prod --token=${{ secrets.VERCEL_TOKEN }}
+        working-directory: app/frontend
diff --git a/.github/workflows/vercel_preview.yml b/.github/workflows/vercel_preview.yml
new file mode 100644
index 00000000..7ae0f856
--- /dev/null
+++ b/.github/workflows/vercel_preview.yml
@@ -0,0 +1,59 @@
+name: Vercel Preview Deployment
+env:
+  VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
+  VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
+on:
+  push:
+    branches-ignore:
+      - main
+jobs:
+  Deploy-Preview:
+    runs-on: ubuntu-latest
+    steps:
+      - uses: actions/checkout@v2
+
+      - name: Start Database Services
+        uses: hoverkraft-tech/compose-action@v2.0.1
+        with:
+          compose-file: "./docker-compose.yml"
+          services: |
+            db_postgresql
+
+      - name: Install Prisma
+        run: npm install
+        working-directory: ./db
+      - name: Setup DB
+        run: |
+          echo DATABASE_URL="postgresql://root:password@localhost:5432/mydb?schema=public" > .env
+          npx prisma migrate dev
+        working-directory: ./db
+      - name: Run tests
+        run: npm test
+        working-directory: ./db
+
+      - name: Setup Graphviz
+        uses: tlylt/install-graphviz@main
+
+      - name: Setup JDK
+        uses: actions/setup-java@v3
+        with:
+          java-version: '21'
+          distribution: 'temurin'
+      - name: Grant execute permission for Gradle wrapper
+        run: chmod +x ./gradlew
+        working-directory: app/backend/api
+      - name: Test with Gradle in api directory
+        run: ./gradlew test
+        working-directory: app/backend/api
+      - name: Test Report with Gradle in api directory
+        run: ./gradlew allureReport
+        working-directory: app/backend/api
+
+      - name: Install Vercel CLI
+        run: npm install --global vercel@latest
+      - name: Pull Vercel Environment Information
+        run: vercel pull --yes --environment=preview --token=${{ secrets.VERCEL_TOKEN }}
+      - name: Build Project Artifacts
+        run: vercel build --token=${{ secrets.VERCEL_TOKEN }}
+      - name: Deploy Project Artifacts to Vercel
+        run: vercel deploy --prebuilt --token=${{ secrets.VERCEL_TOKEN }}
\ No newline at end of file
diff --git a/.github/workflows/vercel_productin.yml b/.github/workflows/vercel_productin.yml
new file mode 100644
index 00000000..383eb618
--- /dev/null
+++ b/.github/workflows/vercel_productin.yml
@@ -0,0 +1,59 @@
+name: Vercel Production Deployment
+env:
+  VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
+  VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
+on:
+  push:
+    branches:
+      - main
+jobs:
+  Deploy-Production:
+    runs-on: ubuntu-latest
+    steps:
+      - uses: actions/checkout@v2
+
+      - name: Start Database Services
+        uses: hoverkraft-tech/compose-action@v2.0.1
+        with:
+          compose-file: "./docker-compose.yml"
+          services: |
+            db_postgresql
+
+      - name: Install Prisma
+        run: npm install
+        working-directory: ./db
+      - name: Setup DB
+        run: |
+          echo DATABASE_URL="postgresql://root:password@localhost:5432/mydb?schema=public" > .env
+          npx prisma migrate dev
+        working-directory: ./db
+      - name: Run tests
+        run: npm test
+        working-directory: ./db
+
+      - name: Setup Graphviz
+        uses: tlylt/install-graphviz@main
+
+      - name: Setup JDK
+        uses: actions/setup-java@v3
+        with:
+          java-version: '21'
+          distribution: 'temurin'
+      - name: Grant execute permission for Gradle wrapper
+        run: chmod +x ./gradlew
+        working-directory: app/backend/api
+      - name: Test with Gradle in api directory
+        run: ./gradlew test
+        working-directory: app/backend/api
+      - name: Test Report with Gradle in api directory
+        run: ./gradlew allureReport
+        working-directory: app/backend/api
+
+      - name: Install Vercel CLI
+        run: npm install --global vercel@latest
+      - name: Pull Vercel Environment Information
+        run: vercel pull --yes --environment=production --token=${{ secrets.VERCEL_TOKEN }}
+      - name: Build Project Artifacts
+        run: vercel build --prod --token=${{ secrets.VERCEL_TOKEN }}
+      - name: Deploy Project Artifacts to Vercel
+        run: vercel deploy --prebuilt --prod --token=${{ secrets.VERCEL_TOKEN }}
diff --git a/gulpfile.cjs b/gulpfile.cjs
index 01550149..55923a8b 100644
--- a/gulpfile.cjs
+++ b/gulpfile.cjs
@@ -2,30 +2,28 @@ const { series, parallel } = require('gulp');
 const core = require('./ops/gulp/tasks/core');
 const custom = require('./ops/gulp/tasks/custom');
 
-exports.default = series(
+const build = series(
     core.webpackBuildTasks(),
     parallel(
         custom.assetsBuildTasks(),
         core.asciidoctorBuildTasks(),
         core.marpBuildTasks(),
         core.adrBuildTasks(),
+        custom.jigBuildTasks(),
+        custom.jigErdBuildTasks(),
+        custom.erdBuildTasks(),
+        custom.allureGradleBuildTasks(),
     ),
+);
+exports.build = build;
+
+exports.default = series(
     series(
         parallel(core.webpack.server, core.asciidoctor.server),
         parallel(core.webpack.watch, core.asciidoctor.watch, core.marp.watch, core.adr.watch),
     ),
 );
 
-exports.build = series(
-    core.webpackBuildTasks(),
-    parallel(
-        custom.assetsBuildTasks(),
-        core.asciidoctorBuildTasks(),
-        core.marpBuildTasks(),
-        core.adrBuildTasks(),
-    )
-);
-
 exports.docs = series(
     parallel(custom.assetsBuildTasks(), core.asciidoctorBuildTasks(), core.marpBuildTasks()),
     core.adrBuildTasks(),
@@ -48,16 +46,7 @@ exports.buildDocs = series(
 );
 
 exports.dev = series(
-    core.webpackBuildTasks(),
-    parallel(
-        custom.assetsBuildTasks(),
-        core.asciidoctorBuildTasks(),
-        core.marpBuildTasks()
-    ),
-    custom.jigBuildTasks(),
-    custom.jigErdBuildTasks(),
-    custom.erdBuildTasks(),
-    custom.allure.publish,
+    build,
     parallel(core.webpack.server, core.asciidoctor.server),
     parallel(core.webpack.watch, core.asciidoctor.watch, core.marp.watch),
     parallel(custom.app.dev, custom.api.dev),
diff --git a/index.html b/index.html
index 115abf22..43a199bf 100644
--- a/index.html
+++ b/index.html
@@ -25,7 +25,7 @@
     <li class="nav-sub-item"><a href="docs" target="_blank">ドキュメント</a></li>
     <li class="nav-sub-item"><a href="docs/adr/export.html" target="_blank">ADR</a></li>
     <li class="nav-sub-item"><a href="assets/release/index.html" target="_blank">JIG</a></li>
-    <li class="nav-sub-item"><a href="allure/latest/index.html" target="_blank">Allure Report</a></li>
+    <li class="nav-sub-item"><a href="allure/allureReport/index.html" target="_blank">Allure Report</a></li>
     <li class="nav-sub-item"><a href="https://plantuml.com/ja/" target="_blank">PlantUML</a></li>
     <li class="nav-sub-item"><a href="https://docs.asciidoctor.org/asciidoc/latest/" target="_blank">AsciiDoc</a></li>
     <li class="nav-sub-item"><a href="https://mermaid.js.org/intro/" target="_blank">Mermaid</a></li>
diff --git a/ops/docker/postgres/init/1_create.sql b/ops/docker/postgres/init/1_create.sql
index 982b6e92..3060760e 100644
--- a/ops/docker/postgres/init/1_create.sql
+++ b/ops/docker/postgres/init/1_create.sql
@@ -1,7 +1,7 @@
 -- Create databases if they don't exist
-DROP DATABASE IF EXISTS mydb;
+-- DROP DATABASE IF EXISTS mydb;
 
-CREATE DATABASE mydb;
+-- CREATE DATABASE mydb;
 
 -- Connect to database
 \c mydb;
diff --git a/ops/gulp/tasks/custom.js b/ops/gulp/tasks/custom.js
index 121e3abc..8cfb5933 100644
--- a/ops/gulp/tasks/custom.js
+++ b/ops/gulp/tasks/custom.js
@@ -196,12 +196,23 @@ const assets = {
         cb();
     },
 };
-
+const prepareDirectories = async () => {
+    await fs.mkdirs('./ops/docker/allure/allure-reports');
+}
 const allure = {
     build: () => {
          console.log("See osp/docker/allure/allure-reports");
          runExecCommand('docker-compose run --rm allure');
     },
+    build_gradle: () => {
+        const command = isWindows ? 'gradlew.bat allureReport' : './gradlew allureReport';
+        return src(apiGradlewPath, { read: false })
+            .pipe(exec(command, { cwd: apiCwd }));
+    },
+    copy_build_gradle: () => {
+        return src(path.join(apiCwd, 'build/reports/allure-report/**'))
+            .pipe(dest('./ops/docker/allure/allure-reports'));
+    },
     clean: async (cb) => {
         await fs.remove('./ops/docker/allure');
         cb();
@@ -212,7 +223,8 @@ const allure = {
             .pipe(src('./app/frontend/allure-results/**'))
             .pipe(dest('./ops/docker/allure/allure-results'));
     },
-    publish: () => {
+    publish: async () => {
+        await prepareDirectories();
         return src('./ops/docker/allure/allure-reports/**')
             .pipe(dest('./public/allure'));
     }
@@ -227,7 +239,8 @@ exports.assetsBuildTasks = assetsBuildTasks;
 const erdBuildTasks = () => {
     return series(
         parallel(
-            erd.buildMySQL, erd.buildPostgresql
+            //erd.buildMySQL,
+            erd.buildPostgresql
         ),
         erd.copy
     );
@@ -254,4 +267,8 @@ exports.allure = allure;
 const allureBuildTasks = () => {
     return series(allure.clean, allure.copy, allure.build);
 }
-exports.allureBuildTasks = allureBuildTasks;
\ No newline at end of file
+exports.allureBuildTasks = allureBuildTasks;
+const allureGradleBuildTasks = () => {
+    return series(allure.build_gradle, allure.copy_build_gradle, allure.publish);
+}
+exports.allureGradleBuildTasks = allureGradleBuildTasks;
\ No newline at end of file
diff --git a/package.json b/package.json
index 634bd18b..397e9ba6 100644
--- a/package.json
+++ b/package.json
@@ -21,8 +21,8 @@
     "jig:db": "npx gulp jig_erd",
     "erd": "npx gulp erd",
     "allure": "npx gulp allure",
-    "db:schemaspy:mysql": "docker-compose run --rm schemaspy_mysql",
-    "db:schemaspy:postresql": "docker-compose run --rm schemaspy_postgresql"
+    "db:schemaspy:mysql": "docker compose run --rm schemaspy_mysql",
+    "db:schemaspy:postresql": "docker compose run --rm schemaspy_postgresql"
   },
   "author": "",
   "license": "ISC",

```

