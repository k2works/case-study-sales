# 作業履歴 2025-04-09

## 概要

2025-04-09の作業内容をまとめています。

## コミット: db0e825f

### メッセージ

```
docs:出荷/売上管理
Complete pending features for shipment and sales modules

Implemented shipment and sales aggregation logic, completed respective screens, and finalized execution history support. These updates enhance functionality and ensure readiness for shipment and sales workflows.
```

### 変更されたファイル

- M	index.js

### 作業解説

#### 目的
ドキュメントの更新により、プロジェクトの理解しやすさを向上させる。

#### 技術的な背景
プロジェクトドキュメントの追加・更新により、知識の共有と将来の開発効率向上を図る。

#### 変更の影響範囲
影響を受けるレイヤ: 特定のレイヤに限定されない。変更は関連するテストおよびドキュメントへの波及を考慮する必要がある。

### 変更内容

```diff
commit db0e825f6085f8aa3f27a442d4ce72fe81c2e631
Author: k2works <kakimomokuri@gmail.com>
Date:   Wed Apr 9 15:49:35 2025 +0900

    docs:出荷/売上管理
    
    Complete pending features for shipment and sales modules
    
    Implemented shipment and sales aggregation logic, completed respective screens, and finalized execution history support. These updates enhance functionality and ensure readiness for shipment and sales workflows.

diff --git a/index.js b/index.js
index ff5eb840..98189c78 100644
--- a/index.js
+++ b/index.js
@@ -80,14 +80,13 @@ const contents = `
     - [x] 出荷編集を実装する  
     - [x] 出荷指示を実装する
     - [x] 出荷ルールを実装する
-    - [ ] 出荷ドメインロジックを実装する
   - [x] 出荷コントローラを実装する
     - [x] 出荷一覧を実装する
     - [x] 出荷詳細を実装する
     - [x] 出荷検索を実装する
     - [x] 出荷編集を実装する  
     - [x] 出荷指示を実装する
-    - [ ] 出荷ドメインロジックを実装する
+  - [ ] 出荷ドメインロジックを実装する
 - [ ] 売上APIを作成する
   - [x] 売上データアクセスを実装する
   - [x] 売上サービスを実装する
@@ -97,7 +96,7 @@ const contents = `
      - [x] 売上登録を実装する
      - [x] 売上編集を実装する
      - [x] 売上削除を実装する
-     - [ ] 売上集計を実装する
+     - [x] 売上集計を実装する
   - [x] 売上コントローラを実装する
      - [x] 売上一覧を実装する
      - [x] 売上詳細を実装する
@@ -105,22 +104,25 @@ const contents = `
      - [x] 売上登録を実装する
      - [x] 売上編集を実装する
      - [x] 売上削除を実装する
-     - [ ] 売上集計を実装する
-- [ ] 出荷画面を作成する
+     - [x] 売上集計を実装する
+  - [ ] 売上ドメインロジックを実装する
+- [x] 出荷画面を作成する
      - [x] 出荷一覧画面を作成する
      - [x] 出荷詳細画面を作成する
      - [x] 出荷検索画面を作成する
      - [x] 出荷編集画面を作成する
      - [x] 出荷ルール確認画面を作成する
      - [x] 出荷指示画面を作成する
-- [ ] 売上画面を作成する
+- [x] 売上画面を作成する
      - [x] 売上一覧画面を作成する
      - [x] 売上詳細画面を作成する
      - [x] 売上検索画面を作成する
      - [x] 売上登録画面を作成する
      - [x] 売上編集画面を作成する
-     - [ ] 売上集計画面を作成する
- - [ ] 実行履歴対応
+     - [x] 売上集計画面を作成する
+ - [x] 実行履歴対応
+     - [x] 出荷コントローラ
+     - [x] 売上コントローラ
 
 `;
 

```

## コミット: 4d34d1de

### メッセージ

```
fix:出荷/売上管理
Refactor navigation structure to adjust sub-item order.

Reorganized the navigation menu to ensure consistent grouping and order of sub-items. Added a new sub-item for shipping ("指示") while maintaining non-production specific sub-menus. This improves clarity and navigation flow in the application.
```

### 変更されたファイル

- M	app/frontend/src/views/application/Navigation.tsx

### 作業解説

#### 目的
バグ修正または不具合対応を行い、システムの安定性・信頼性を向上させる。

#### 技術的な背景
既存機能における問題点を特定し、適切な修正を実施。修正により、想定される使用シナリオでの正常動作を確保。

#### 変更の影響範囲
影響を受けるレイヤ: フロントエンド。変更は関連するテストおよびドキュメントへの波及を考慮する必要がある。

### 変更内容

```diff
commit 4d34d1de6948dfb3b0e3854b821606ec8437b89f
Author: k2works <kakimomokuri@gmail.com>
Date:   Wed Apr 9 15:49:20 2025 +0900

    fix:出荷/売上管理
    
    Refactor navigation structure to adjust sub-item order.
    
    Reorganized the navigation menu to ensure consistent grouping and order of sub-items. Added a new sub-item for shipping ("指示") while maintaining non-production specific sub-menus. This improves clarity and navigation flow in the application.

diff --git a/app/frontend/src/views/application/Navigation.tsx b/app/frontend/src/views/application/Navigation.tsx
index e2801a12..a58f861f 100644
--- a/app/frontend/src/views/application/Navigation.tsx
+++ b/app/frontend/src/views/application/Navigation.tsx
@@ -122,18 +122,19 @@ const NaveItems: React.FC = () => {
                                     <SubNavItem id="side-nav-product-nav" to="/sales-order-rule">ルール</SubNavItem>
                                 </ul>
                             )}
-                            <SubNavItem id="side-nav-sales-nav" to="/sales">売上</SubNavItem>
+                            <SubNavItem id="side-nav-shipping-nav" to="/shipping">出荷</SubNavItem>
                             {!Env.isProduction() && (
                                 <ul className="nav-sub-list">
-                                    <SubNavItem id="side-nav-sales-nav" to="/sales-list">一覧</SubNavItem>
-                                    <SubNavItem id="side-nav-sales-nav" to="/sales-summary">集計</SubNavItem>
+                                    <SubNavItem id="side-nav-shipping-nav" to="/shipping-list">一覧</SubNavItem>
+                                    <SubNavItem id="side-nav-shipping-nav" to="/shipping-rule">ルール</SubNavItem>
+                                    <SubNavItem id="side-nav-shipping-nav" to="/shipping-order">指示</SubNavItem>
                                 </ul>
                             )}
-                            <SubNavItem id="side-nav-shipping-nav" to="/shipping">出荷</SubNavItem>
+                            <SubNavItem id="side-nav-sales-nav" to="/sales">売上</SubNavItem>
                             {!Env.isProduction() && (
                                 <ul className="nav-sub-list">
-                                    <SubNavItem id="side-nav-shipping-nav" to="/shipping-list">一覧</SubNavItem>
-                                    <SubNavItem id="side-nav-shipping-nav" to="/shipping-rule">ルール</SubNavItem>
+                                    <SubNavItem id="side-nav-sales-nav" to="/sales-list">一覧</SubNavItem>
+                                    <SubNavItem id="side-nav-sales-nav" to="/sales-summary">集計</SubNavItem>
                                 </ul>
                             )}
                         </ul>

```

## コミット: 205f8df3

### メッセージ

```
docs:出荷/売上管理
Update SalesList class documentation

Renamed the class description from "販売一覧" to "売上一覧" to improve terminology consistency. No functional changes were introduced.
```

### 変更されたファイル

- M	app/backend/api/src/main/java/com/example/sms/domain/model/sales/SalesList.java

### 作業解説

#### 目的
ドキュメントの更新により、プロジェクトの理解しやすさを向上させる。

#### 技術的な背景
プロジェクトドキュメントの追加・更新により、知識の共有と将来の開発効率向上を図る。

#### 変更の影響範囲
影響を受けるレイヤ: ドメイン層、プレゼンテーション層。変更は関連するテストおよびドキュメントへの波及を考慮する必要がある。

### 変更内容

```diff
commit 205f8df32b9141f73d55a64d219ecd7230a8ff09
Author: k2works <kakimomokuri@gmail.com>
Date:   Wed Apr 9 15:28:45 2025 +0900

    docs:出荷/売上管理
    
    Update SalesList class documentation
    
    Renamed the class description from "販売一覧" to "売上一覧" to improve terminology consistency. No functional changes were introduced.

diff --git a/app/backend/api/src/main/java/com/example/sms/domain/model/sales/SalesList.java b/app/backend/api/src/main/java/com/example/sms/domain/model/sales/SalesList.java
index f8c1a535..9cdb2e4d 100644
--- a/app/backend/api/src/main/java/com/example/sms/domain/model/sales/SalesList.java
+++ b/app/backend/api/src/main/java/com/example/sms/domain/model/sales/SalesList.java
@@ -5,7 +5,7 @@ import java.util.Collections;
 import java.util.List;
 
 /**
- * 販売一覧
+ * 売上一覧
  */
 public class SalesList {
     private final List<Sales> value;

```

## コミット: 3ce815f4

### メッセージ

```
feat:出荷/売上管理
Clear sales data before aggregation

Added a call to delete all sales data in the SalesService's aggregate method to ensure a clean state before aggregating. Updated the test case to reflect the reduced expected result size due to the data clearing.
```

### 変更されたファイル

- M	app/backend/api/src/main/java/com/example/sms/service/sales/SalesService.java
- M	app/backend/api/src/test/java/com/example/sms/service/sales/SalesServiceTest.java

### 作業解説

#### 目的
新機能の実装により、システムの機能性を拡張する。

#### 技術的な背景
ユーザー要件または技術的要件に基づき、新機能を設計・実装。既存アーキテクチャとの整合性を保ちながら拡張。

#### 変更の影響範囲
影響を受けるレイヤ: サービス層、プレゼンテーション層、テスト。変更は関連するテストおよびドキュメントへの波及を考慮する必要がある。

### 変更内容

```diff
commit 3ce815f427afdbcbddfe6d6b1ec63d65670de0fd
Author: k2works <kakimomokuri@gmail.com>
Date:   Wed Apr 9 15:23:13 2025 +0900

    feat:出荷/売上管理
    
    Clear sales data before aggregation
    
    Added a call to delete all sales data in the SalesService's aggregate method to ensure a clean state before aggregating. Updated the test case to reflect the reduced expected result size due to the data clearing.

diff --git a/app/backend/api/src/main/java/com/example/sms/service/sales/SalesService.java b/app/backend/api/src/main/java/com/example/sms/service/sales/SalesService.java
index e5835bf7..efd4040b 100644
--- a/app/backend/api/src/main/java/com/example/sms/service/sales/SalesService.java
+++ b/app/backend/api/src/main/java/com/example/sms/service/sales/SalesService.java
@@ -82,6 +82,8 @@ public class SalesService {
      * 売上集計
      */
     public void aggregate() {
+        salesRepository.deleteAll();
+
         ShippingList  shippingList = shippingRepository.selectAllComplete();
 
         List<String> orderNumberList = shippingList.asList().stream()
diff --git a/app/backend/api/src/test/java/com/example/sms/service/sales/SalesServiceTest.java b/app/backend/api/src/test/java/com/example/sms/service/sales/SalesServiceTest.java
index 9fdfc6e1..ad2615fc 100644
--- a/app/backend/api/src/test/java/com/example/sms/service/sales/SalesServiceTest.java
+++ b/app/backend/api/src/test/java/com/example/sms/service/sales/SalesServiceTest.java
@@ -221,7 +221,7 @@ class SalesServiceTest {
             salesService.aggregate();
 
             SalesList result = salesService.selectAll();
-            assertEquals(6, result.asList().size());
+            assertEquals(3, result.asList().size());
         }
     }
 }
\ No newline at end of file

```

## コミット: ac7b3a98

### メッセージ

```
fix:出荷/売上管理
Update sales calculation to use order quantity instead of sales amount

Replaced the use of `salesAmount` with `orderQuantity` in `SalesService` to correctly reflect the intended value in calculations. Adjusted test cases in `SalesServiceTest` accordingly to align with the updated logic.
```

### 変更されたファイル

- M	app/backend/api/src/main/java/com/example/sms/service/sales/SalesService.java
- M	app/backend/api/src/test/java/com/example/sms/service/sales/SalesServiceTest.java

### 作業解説

#### 目的
バグ修正または不具合対応を行い、システムの安定性・信頼性を向上させる。

#### 技術的な背景
既存機能における問題点を特定し、適切な修正を実施。修正により、想定される使用シナリオでの正常動作を確保。

#### 変更の影響範囲
影響を受けるレイヤ: サービス層、プレゼンテーション層、テスト。変更は関連するテストおよびドキュメントへの波及を考慮する必要がある。

### 変更内容

```diff
commit ac7b3a988a15181646f532da92095436a3125037
Author: k2works <kakimomokuri@gmail.com>
Date:   Wed Apr 9 15:10:14 2025 +0900

    fix:出荷/売上管理
    
    Update sales calculation to use order quantity instead of sales amount
    
    Replaced the use of `salesAmount` with `orderQuantity` in `SalesService` to correctly reflect the intended value in calculations. Adjusted test cases in `SalesServiceTest` accordingly to align with the updated logic.

diff --git a/app/backend/api/src/main/java/com/example/sms/service/sales/SalesService.java b/app/backend/api/src/main/java/com/example/sms/service/sales/SalesService.java
index b2fdce81..e5835bf7 100644
--- a/app/backend/api/src/main/java/com/example/sms/service/sales/SalesService.java
+++ b/app/backend/api/src/main/java/com/example/sms/service/sales/SalesService.java
@@ -103,7 +103,7 @@ public class SalesService {
                             shipping.getProductCode().getValue(),
                             shipping.getProductName(),
                             shipping.getSalesUnitPrice().getAmount(),
-                            shipping.getSalesAmount().getValue().getAmount(),
+                            shipping.getOrderQuantity().getAmount(),
                             shipping.getShippedQuantity().getAmount(),
                             shipping.getDiscountAmount().getAmount(),
                             null,
diff --git a/app/backend/api/src/test/java/com/example/sms/service/sales/SalesServiceTest.java b/app/backend/api/src/test/java/com/example/sms/service/sales/SalesServiceTest.java
index 5d8e9b41..9fdfc6e1 100644
--- a/app/backend/api/src/test/java/com/example/sms/service/sales/SalesServiceTest.java
+++ b/app/backend/api/src/test/java/com/example/sms/service/sales/SalesServiceTest.java
@@ -181,7 +181,7 @@ class SalesServiceTest {
                             "99999999",
                             "商品1",
                             1000,
-                            10000,
+                            10,
                             10,
                             10,
                             null,

```

## コミット: 182f3b6a

### メッセージ

```
feat:出荷/売上管理
Add audit annotations for sales operations

Introduced @AuditAnnotation to track create, update, delete, and aggregate operations in SalesApiController. Updated ApplicationExecutionProcessType with corresponding sales-related entries to support auditing.
```

### 変更されたファイル

- M	app/backend/api/src/main/java/com/example/sms/domain/model/system/audit/ApplicationExecutionProcessType.java
- M	app/backend/api/src/main/java/com/example/sms/presentation/api/sales/SalesApiController.java

### 作業解説

#### 目的
新機能の実装により、システムの機能性を拡張する。

#### 技術的な背景
ユーザー要件または技術的要件に基づき、新機能を設計・実装。既存アーキテクチャとの整合性を保ちながら拡張。

#### 変更の影響範囲
影響を受けるレイヤ: ドメイン層、プレゼンテーション層。変更は関連するテストおよびドキュメントへの波及を考慮する必要がある。

### 変更内容

```diff
commit 182f3b6a849543223de3f7b8f0fa270ff39f2500
Author: k2works <kakimomokuri@gmail.com>
Date:   Wed Apr 9 14:55:48 2025 +0900

    feat:出荷/売上管理
    
    Add audit annotations for sales operations
    
    Introduced @AuditAnnotation to track create, update, delete, and aggregate operations in SalesApiController. Updated ApplicationExecutionProcessType with corresponding sales-related entries to support auditing.

diff --git a/app/backend/api/src/main/java/com/example/sms/domain/model/system/audit/ApplicationExecutionProcessType.java b/app/backend/api/src/main/java/com/example/sms/domain/model/system/audit/ApplicationExecutionProcessType.java
index b3b9f508..950cf3a5 100644
--- a/app/backend/api/src/main/java/com/example/sms/domain/model/system/audit/ApplicationExecutionProcessType.java
+++ b/app/backend/api/src/main/java/com/example/sms/domain/model/system/audit/ApplicationExecutionProcessType.java
@@ -46,6 +46,10 @@ public enum ApplicationExecutionProcessType {
     出荷登録("出荷登録", "0037"),
     出荷更新("出荷更新", "0038"),
     出荷削除("出荷削除", "0039"),
+    売上登録("売上登録", "0040"),
+    売上更新("売上更新", "0041"),
+    売上削除("売上削除", "0042"),
+    売上集計("売上集計", "0043"),
     その他("その他", "9999"),
     データダウンロード("データダウンロード", "9001");
 
diff --git a/app/backend/api/src/main/java/com/example/sms/presentation/api/sales/SalesApiController.java b/app/backend/api/src/main/java/com/example/sms/presentation/api/sales/SalesApiController.java
index f0b2e376..dcbc8ff0 100644
--- a/app/backend/api/src/main/java/com/example/sms/presentation/api/sales/SalesApiController.java
+++ b/app/backend/api/src/main/java/com/example/sms/presentation/api/sales/SalesApiController.java
@@ -1,6 +1,8 @@
 package com.example.sms.presentation.api.sales;
 
 import com.example.sms.domain.model.sales.Sales;
+import com.example.sms.domain.model.system.audit.ApplicationExecutionHistoryType;
+import com.example.sms.domain.model.system.audit.ApplicationExecutionProcessType;
 import com.example.sms.presentation.Message;
 import com.example.sms.presentation.PageNation;
 import com.example.sms.presentation.api.system.auth.payload.response.MessageResponse;
@@ -8,6 +10,7 @@ import com.example.sms.service.BusinessException;
 import com.example.sms.service.PageNationService;
 import com.example.sms.service.sales.SalesCriteria;
 import com.example.sms.service.sales.SalesService;
+import com.example.sms.service.system.audit.AuditAnnotation;
 import com.github.pagehelper.PageInfo;
 import io.swagger.v3.oas.annotations.Operation;
 import io.swagger.v3.oas.annotations.tags.Tag;
@@ -63,6 +66,7 @@ public class SalesApiController {
 
     @Operation(summary = "売上を登録する", description = "新しい売上データを登録します。")
     @PostMapping
+    @AuditAnnotation(process = ApplicationExecutionProcessType.売上登録, type = ApplicationExecutionHistoryType.同期)
     public ResponseEntity<?> create(@RequestBody SalesResource resource) {
         try {
             Sales sales = SalesResourceDTOMapper.convertToEntity(resource);
@@ -78,6 +82,7 @@ public class SalesApiController {
 
     @Operation(summary = "売上情報を更新する", description = "指定された売上データを更新します。")
     @PutMapping("/{salesNumber}")
+    @AuditAnnotation(process = ApplicationExecutionProcessType.売上更新, type = ApplicationExecutionHistoryType.同期)
     public ResponseEntity<?> update(@PathVariable String salesNumber, @RequestBody SalesResource resource) {
         try {
             resource.setSalesNumber(salesNumber);
@@ -94,6 +99,7 @@ public class SalesApiController {
 
     @Operation(summary = "売上を削除する", description = "指定された売上データを削除します。")
     @DeleteMapping("/{salesNumber}")
+    @AuditAnnotation(process = ApplicationExecutionProcessType.売上削除, type = ApplicationExecutionHistoryType.同期)
     public ResponseEntity<?> delete(@PathVariable String salesNumber) {
         try {
             Sales sales = salesService.find(salesNumber);
@@ -126,6 +132,7 @@ public class SalesApiController {
 
     @Operation(summary = "売上を集計する", description = "売上を集計します。")
     @PostMapping("/aggregate")
+    @AuditAnnotation(process = ApplicationExecutionProcessType.売上集計, type = ApplicationExecutionHistoryType.同期)
     public ResponseEntity<?> aggregate(@RequestBody SalesResource resource) {
         try {
             salesService.aggregate();
@@ -134,4 +141,4 @@ public class SalesApiController {
             return ResponseEntity.badRequest().body(new MessageResponse(e.getMessage()));
         }
     }
-}
\ No newline at end of file
+}

```

## コミット: f32f043d

### メッセージ

```
feat:出荷/売上管理
implement sales aggregation functionality

Revised SalesAggregateContainer to include sales data fetching and error handling, enhancing usability. Added `aggregate` service function and updated type definitions to support new aggregation needs.
```

### 変更されたファイル

- A	app/frontend/src/components/sales/sales/aggregate/SalesAggregateCollection.tsx
- M	app/frontend/src/components/sales/sales/aggregate/SalesAggregateContainer.tsx
- M	app/frontend/src/services/sales/sales.ts
- A	app/frontend/src/views/sales/sales/aggregate/SalesAggregateCollection.tsx

### 作業解説

#### 目的
新機能の実装により、システムの機能性を拡張する。

#### 技術的な背景
ユーザー要件または技術的要件に基づき、新機能を設計・実装。既存アーキテクチャとの整合性を保ちながら拡張。

#### 変更の影響範囲
影響を受けるレイヤ: フロントエンド、サービス層。変更は関連するテストおよびドキュメントへの波及を考慮する必要がある。

### 変更内容

```diff
commit f32f043da45478da5deccbb69f74c584785a8e72
Author: k2works <kakimomokuri@gmail.com>
Date:   Wed Apr 9 14:46:06 2025 +0900

    feat:出荷/売上管理
    
    implement sales aggregation functionality
    
    Revised SalesAggregateContainer to include sales data fetching and error handling, enhancing usability. Added `aggregate` service function and updated type definitions to support new aggregation needs.

diff --git a/app/frontend/src/components/sales/sales/aggregate/SalesAggregateCollection.tsx b/app/frontend/src/components/sales/sales/aggregate/SalesAggregateCollection.tsx
new file mode 100644
index 00000000..6d4c7741
--- /dev/null
+++ b/app/frontend/src/components/sales/sales/aggregate/SalesAggregateCollection.tsx
@@ -0,0 +1,31 @@
+import {useState} from "react";
+import {SalesAggregateResponse} from "../../../../services/sales/sales";
+import {useSalesContext} from "../../../../providers/sales/Sales";
+import {SalesAggregateCollectionView} from "../../../../views/sales/sales/aggregate/SalesAggregateCollection";
+
+export const SalesAggregateCollection: React.FC = () => {
+    const [aggregateResults, setAggregateResults] = useState<SalesAggregateResponse[]>([]);
+    const {salesService} = useSalesContext();
+
+    const handleExecuteAggregate = async () => {
+        try {
+            const result = await salesService.aggregate();
+            setAggregateResults(prevResults => [...prevResults, result]);
+        } catch (error) {
+            const errorMessage = error instanceof Error ? error.message : '不明なエラーが発生しました';
+            console.error('Aggregate failed:', errorMessage);
+        }
+    };
+
+    const handleDeleteAggregateResult = (index: number) => {
+        setAggregateResults(prevResults => prevResults.filter((_, i) => i !== index));
+    };
+
+    return (
+        <SalesAggregateCollectionView
+            aggregateHeaderItems={{handleExecuteAggregate}}
+            aggregateResults={aggregateResults}
+            handleDeleteAggregateResult={handleDeleteAggregateResult}
+        />
+    );
+};
\ No newline at end of file
diff --git a/app/frontend/src/components/sales/sales/aggregate/SalesAggregateContainer.tsx b/app/frontend/src/components/sales/sales/aggregate/SalesAggregateContainer.tsx
index 171f8511..d582fde8 100644
--- a/app/frontend/src/components/sales/sales/aggregate/SalesAggregateContainer.tsx
+++ b/app/frontend/src/components/sales/sales/aggregate/SalesAggregateContainer.tsx
@@ -1,18 +1,44 @@
-import React from "react";
+import { useEffect } from "react";
+import { showErrorMessage } from "../../../application/utils";
+import LoadingIndicator from "../../../../views/application/LoadingIndicatior";
+import { SalesProvider, useSalesContext } from "../../../../providers/sales/Sales";
+import { SalesAggregateCollection } from "./SalesAggregateCollection";
 
 export const SalesAggregateContainer: React.FC = () => {
+    const Content: React.FC = () => {
+        const {
+            loading,
+            setError,
+            fetchSales,
+        } = useSalesContext();
+
+        useEffect(() => {
+            (async () => {
+                try {
+                    await Promise.all([
+                        fetchSales.load(),
+                    ]);
+                } catch (error) {
+                    const errorMessage = error instanceof Error ? error.message : '不明なエラーが発生しました';
+                    showErrorMessage(`売上情報の取得に失敗しました: ${errorMessage}`, setError);
+                }
+            })();
+        }, []);
+
+        return (
+            <>
+                {loading ? (
+                    <LoadingIndicator/>
+                ) : (
+                    <SalesAggregateCollection/>
+                )}
+            </>
+        );
+    };
+
     return (
-        <div className="single-view-object-container">
-            <div className="single-view-header">
-                <div className="single-view-header-item">
-                    <h1 className="single-view-title">売上集計</h1>
-                </div>
-            </div>
-            <div className="single-view-container">
-                <div className="single-view-content">
-                    <p>売上集計機能は実装中です。</p>
-                </div>
-            </div>
-        </div>
+        <SalesProvider>
+            <Content />
+        </SalesProvider>
     );
-};
\ No newline at end of file
+};
diff --git a/app/frontend/src/services/sales/sales.ts b/app/frontend/src/services/sales/sales.ts
index dd00541b..46c64ecb 100644
--- a/app/frontend/src/services/sales/sales.ts
+++ b/app/frontend/src/services/sales/sales.ts
@@ -8,6 +8,12 @@ import {
     mapToSalesResource
 } from "../../models/sales/sales";
 
+// 売上集計結果の型定義
+export interface SalesAggregateResponse {
+    message: string;
+    details: string[];
+}
+
 export interface SalesServiceType {
     select: (page?: number, pageSize?: number) => Promise<SalesPageInfoType>;
     find: (salesNumber: string) => Promise<SalesType>;
@@ -15,6 +21,7 @@ export interface SalesServiceType {
     update: (sales: SalesType) => Promise<void>;
     destroy: (salesNumber: string) => Promise<void>;
     search: (criteria: SalesCriteriaType, page?: number, pageSize?: number) => Promise<SalesPageInfoType>;
+    aggregate: () => Promise<SalesAggregateResponse>;
 }
 
 export const SalesService = () => {
@@ -51,9 +58,9 @@ export const SalesService = () => {
         await apiUtils.fetchDelete<void>(url);
     };
 
-    const aggregate = async (): Promise<void> => {
+    const aggregate = async (): Promise<SalesAggregateResponse> => {
         const url = `${endPoint}/aggregate`;
-        await apiUtils.fetchPost<void>(url, null);
+        return await apiUtils.fetchPost<SalesAggregateResponse>(url, {});
     };
 
     return {
@@ -65,4 +72,4 @@ export const SalesService = () => {
         search,
         aggregate
     };
-}
\ No newline at end of file
+}
diff --git a/app/frontend/src/views/sales/sales/aggregate/SalesAggregateCollection.tsx b/app/frontend/src/views/sales/sales/aggregate/SalesAggregateCollection.tsx
new file mode 100644
index 00000000..ba0b26a4
--- /dev/null
+++ b/app/frontend/src/views/sales/sales/aggregate/SalesAggregateCollection.tsx
@@ -0,0 +1,71 @@
+import React from "react";
+import {Message} from "../../../../components/application/Message.tsx";
+import {SalesAggregateResponse} from "../../../../services/sales/sales.ts";
+
+interface SalesAggregateCollectionViewProps {
+    aggregateHeaderItems: {
+        handleExecuteAggregate: () => void;
+    };
+    aggregateResults: SalesAggregateResponse[];
+    handleDeleteAggregateResult: (index: number) => void;
+}
+
+export const SalesAggregateCollectionView: React.FC<SalesAggregateCollectionViewProps> = ({
+                                                                                              aggregateHeaderItems: { handleExecuteAggregate },
+                                                                                              aggregateResults,
+                                                                                              handleDeleteAggregateResult,
+                                                                                          }) => {
+    return (
+        <div className="collection-view-object-container">
+            <div className="collection-view-container">
+                <div className="collection-view-header">
+                    <div className="single-view-header-item">
+                        <h1 className="single-view-title">売上集計</h1>
+                    </div>
+                </div>
+                <div className="collection-view-content">
+                    <div className="button-container">
+                        <button className="action-button" onClick={handleExecuteAggregate} id="execute">
+                            実行
+                        </button>
+                    </div>
+                    <div className="collection-object-container">
+                        <ul className="collection-object-list">
+                            {aggregateResults.map((result, index) => (
+                                <div key={index} className="upload-result-item">
+                                    <div className="check-result-message">
+                                        <Message error={null} message={result.message}/>
+                                        {result.details && result.details.length > 0 && (
+                                            <div className="upload-result-details">
+                                                {result.details.map((detail, detailIndex) => (
+                                                    <div key={detailIndex} className="upload-result-detail">
+                                                        {Object.entries(detail).map(([key, value]) => (
+                                                            <div key={key} className="detail-item">
+                                                                <span className="detail-key">{key}:</span>
+                                                                <span className="detail-value">
+                                                                  {typeof value === 'object' && value !== null
+                                                                      ? JSON.stringify(value)
+                                                                      : String(value)}
+                                                                </span>
+                                                            </div>
+                                                        ))}
+                                                    </div>
+                                                ))}
+                                            </div>
+                                        )}
+                                    </div>
+                                    <button
+                                        className="action-button"
+                                        onClick={() => handleDeleteAggregateResult(index)}
+                                    >
+                                        x
+                                    </button>
+                                </div>
+                            ))}
+                        </ul>
+                    </div>
+                </div>
+            </div>
+        </div>
+    );
+};
\ No newline at end of file

```

## コミット: 596cf229

### メッセージ

```
refactor:出荷/売上管理
Rename SalesSummaryContainer to SalesAggregateContainer

Updated component name and related imports for improved clarity and consistency with its purpose. Additionally, added an `aggregate` service function to support new functionality.
```

### 変更されたファイル

- M	app/frontend/src/components/application/RouteConfig.tsx
- M	app/frontend/src/components/sales/sales/SalesTabContainer.tsx
- M	app/frontend/src/services/sales/sales.ts

### 作業解説

#### 目的
コード構造の改善により、保守性・可読性を向上させる。

#### 技術的な背景
既存コードの動作を変更せずに、内部構造を改善。SOLID 原則やクリーンアーキテクチャの考え方に基づくリファクタリング。

#### 変更の影響範囲
影響を受けるレイヤ: フロントエンド、サービス層。変更は関連するテストおよびドキュメントへの波及を考慮する必要がある。

### 変更内容

```diff
commit 596cf229e1da3f8e7d058da71c54769f3070b3e5
Author: k2works <kakimomokuri@gmail.com>
Date:   Wed Apr 9 14:36:21 2025 +0900

    refactor:出荷/売上管理
    
    Rename SalesSummaryContainer to SalesAggregateContainer
    
    Updated component name and related imports for improved clarity and consistency with its purpose. Additionally, added an `aggregate` service function to support new functionality.

diff --git a/app/frontend/src/components/application/RouteConfig.tsx b/app/frontend/src/components/application/RouteConfig.tsx
index 92b6cb35..4c0307d8 100644
--- a/app/frontend/src/components/application/RouteConfig.tsx
+++ b/app/frontend/src/components/application/RouteConfig.tsx
@@ -28,7 +28,7 @@ import {SalesOrderUploadContainer} from "../sales/sales_order/upload/SalesOrderU
 import {SalesOrderRuleContainer} from "../sales/sales_order/rule/SalesOrderRuleContainer.tsx";
 import {SalesTabContainer} from "../sales/sales/SalesTabContainer.tsx";
 import {SalesContainer} from "../sales/sales/list/SalesContainer.tsx";
-import {SalesSummaryContainer} from "../sales/sales/summary/SalesSummaryContainer.tsx";
+import {SalesAggregateContainer} from "../sales/sales/aggregate/SalesAggregateContainer.tsx";
 import {ShippingTabContainer} from "../sales/shipping/ShippingTabContainer.tsx";
 import {ShippingContainer} from "../sales/shipping/list/ShippingContainer.tsx";
 import {ShippingRuleContainer} from "../sales/shipping/rule/ShippingRuleContainer.tsx";
@@ -134,7 +134,7 @@ export const RouteConfig: React.FC = () => {
     const SalesSummaryPage = () => {
         return (
             <SiteLayout>
-                <SalesSummaryContainer/>
+                <SalesAggregateContainer/>
             </SiteLayout>
         )
     }
diff --git a/app/frontend/src/components/sales/sales/SalesTabContainer.tsx b/app/frontend/src/components/sales/sales/SalesTabContainer.tsx
index 638de07b..3b8f1317 100644
--- a/app/frontend/src/components/sales/sales/SalesTabContainer.tsx
+++ b/app/frontend/src/components/sales/sales/SalesTabContainer.tsx
@@ -1,7 +1,7 @@
 import {useTab} from "../../application/hooks";
 import {SiteLayout} from "../../../views/SiteLayout";
 import {SalesContainer} from "./list/SalesContainer";
-import {SalesSummaryContainer} from "./summary/SalesSummaryContainer";
+import {SalesAggregateContainer} from "./aggregate/SalesAggregateContainer.tsx";
 
 export const SalesTabContainer: React.FC = () => {
     const {
@@ -22,7 +22,7 @@ export const SalesTabContainer: React.FC = () => {
                     <SalesContainer/>
                 </TabPanel>
                 <TabPanel>
-                    <SalesSummaryContainer/>
+                    <SalesAggregateContainer/>
                 </TabPanel>
             </Tabs>
         </SiteLayout>
diff --git a/app/frontend/src/components/sales/sales/summary/SalesSummaryContainer.tsx b/app/frontend/src/components/sales/sales/aggregate/SalesAggregateContainer.tsx
similarity index 90%
rename from app/frontend/src/components/sales/sales/summary/SalesSummaryContainer.tsx
rename to app/frontend/src/components/sales/sales/aggregate/SalesAggregateContainer.tsx
index dc55e3f1..171f8511 100644
--- a/app/frontend/src/components/sales/sales/summary/SalesSummaryContainer.tsx
+++ b/app/frontend/src/components/sales/sales/aggregate/SalesAggregateContainer.tsx
@@ -1,6 +1,6 @@
 import React from "react";
 
-export const SalesSummaryContainer: React.FC = () => {
+export const SalesAggregateContainer: React.FC = () => {
     return (
         <div className="single-view-object-container">
             <div className="single-view-header">
diff --git a/app/frontend/src/services/sales/sales.ts b/app/frontend/src/services/sales/sales.ts
index 09e7358e..dd00541b 100644
--- a/app/frontend/src/services/sales/sales.ts
+++ b/app/frontend/src/services/sales/sales.ts
@@ -51,12 +51,18 @@ export const SalesService = () => {
         await apiUtils.fetchDelete<void>(url);
     };
 
+    const aggregate = async (): Promise<void> => {
+        const url = `${endPoint}/aggregate`;
+        await apiUtils.fetchPost<void>(url, null);
+    };
+
     return {
         select,
         find,
         create,
         update,
         destroy,
-        search
+        search,
+        aggregate
     };
 }
\ No newline at end of file

```

## コミット: 8b1e26c4

### メッセージ

```
refactor:出荷/売上管理
Refactor: Move shipping service to sales directory

Relocated the shipping service files from "services/shipping" to "services/sales/shipping" for improved directory structure and consistency. Updated all relevant import paths to reflect the new location.
```

### 変更されたファイル

- M	app/frontend/src/components/sales/shipping/rule/ShippingRuleCollection.tsx
- M	app/frontend/src/providers/sales/Shipping.tsx
- M	app/frontend/src/views/sales/shipping/rule/ShippingRuleCollection.tsx

### 作業解説

#### 目的
コード構造の改善により、保守性・可読性を向上させる。

#### 技術的な背景
既存コードの動作を変更せずに、内部構造を改善。SOLID 原則やクリーンアーキテクチャの考え方に基づくリファクタリング。

#### 変更の影響範囲
影響を受けるレイヤ: フロントエンド。変更は関連するテストおよびドキュメントへの波及を考慮する必要がある。

### 変更内容

```diff
commit 8b1e26c4e531a6bc6a71ec4c5a16e9b2e792f82c
Author: k2works <kakimomokuri@gmail.com>
Date:   Wed Apr 9 14:13:49 2025 +0900

    refactor:出荷/売上管理
    
    Refactor: Move shipping service to sales directory
    
    Relocated the shipping service files from "services/shipping" to "services/sales/shipping" for improved directory structure and consistency. Updated all relevant import paths to reflect the new location.

diff --git a/app/frontend/src/components/sales/shipping/rule/ShippingRuleCollection.tsx b/app/frontend/src/components/sales/shipping/rule/ShippingRuleCollection.tsx
index 24d05bfa..8784f7c7 100644
--- a/app/frontend/src/components/sales/shipping/rule/ShippingRuleCollection.tsx
+++ b/app/frontend/src/components/sales/shipping/rule/ShippingRuleCollection.tsx
@@ -1,5 +1,5 @@
 import {useState} from "react";
-import {ShippingRuleCheckResponse} from "../../../../services/shipping/shipping";
+import {ShippingRuleCheckResponse} from "../../../../services/sales/shipping";
 import {useShippingContext} from "../../../../providers/sales/Shipping";
 import {ShippingRuleCollectionView} from "../../../../views/sales/shipping/rule/ShippingRuleCollection";
 
diff --git a/app/frontend/src/providers/sales/Shipping.tsx b/app/frontend/src/providers/sales/Shipping.tsx
index 958b1520..37f4589b 100644
--- a/app/frontend/src/providers/sales/Shipping.tsx
+++ b/app/frontend/src/providers/sales/Shipping.tsx
@@ -3,7 +3,7 @@ import { PageNationType, usePageNation } from "../../views/application/PageNatio
 import { ShippingCriteriaType, ShippingType, initialShipping, initialShippingCriteria } from "../../models/sales/shipping";
 import { useModal } from "../../components/application/hooks.ts";
 import { useMessage } from "../../components/application/Message.tsx";
-import { ShippingService, ShippingServiceType } from "../../services/shipping/shipping";
+import { ShippingService, ShippingServiceType } from "../../services/sales/shipping";
 import { showErrorMessage } from "../../components/application/utils.ts";
 
 type ShippingContextType = {
diff --git a/app/frontend/src/services/shipping/shipping.ts b/app/frontend/src/services/sales/shipping.ts
similarity index 100%
rename from app/frontend/src/services/shipping/shipping.ts
rename to app/frontend/src/services/sales/shipping.ts
diff --git a/app/frontend/src/views/sales/shipping/rule/ShippingRuleCollection.tsx b/app/frontend/src/views/sales/shipping/rule/ShippingRuleCollection.tsx
index 1c9d7689..c63b6cf9 100644
--- a/app/frontend/src/views/sales/shipping/rule/ShippingRuleCollection.tsx
+++ b/app/frontend/src/views/sales/shipping/rule/ShippingRuleCollection.tsx
@@ -1,6 +1,6 @@
 import React from "react";
 import {Message} from "../../../../components/application/Message.tsx";
-import {ShippingRuleCheckResponse} from "../../../../services/shipping/shipping.ts";
+import {ShippingRuleCheckResponse} from "../../../../services/sales/shipping.ts";
 
 interface ShippingRuleCollectionViewProps {
     ruleHeaderItems: {

```

## コミット: f1933dcf

### メッセージ

```
feat(WIP):出荷/売上管理
Add sales aggregation functionality

Introduced an endpoint to aggregate sales along with the necessary backend logic, message properties updates, and supporting tests. Updated `SalesService` method and added a new test scenario for verifying aggregation behavior.
```

### 変更されたファイル

- M	app/backend/api/src/main/java/com/example/sms/presentation/api/sales/SalesApiController.java
- M	app/backend/api/src/main/java/com/example/sms/service/sales/SalesService.java
- M	app/backend/api/src/main/resources/messages.properties
- M	app/backend/api/src/main/resources/messages_ja.properties
- M	app/backend/api/src/test/java/com/example/sms/service/sales/SalesServiceTest.java
- M	app/backend/api/src/test/java/com/example/sms/stepdefinitions/UC017StepDefs.java
- M	app/backend/api/src/test/resources/features/sales/Sales.feature

### 作業解説

#### 目的
新機能の実装により、システムの機能性を拡張する。

#### 技術的な背景
ユーザー要件または技術的要件に基づき、新機能を設計・実装。既存アーキテクチャとの整合性を保ちながら拡張。

#### 変更の影響範囲
影響を受けるレイヤ: プレゼンテーション層、サービス層、テスト。変更は関連するテストおよびドキュメントへの波及を考慮する必要がある。

### 変更内容

```diff
commit f1933dcf70c3f1b96b7510effe769f5434f171c3
Author: k2works <kakimomokuri@gmail.com>
Date:   Wed Apr 9 14:09:56 2025 +0900

    feat(WIP):出荷/売上管理
    
    Add sales aggregation functionality
    
    Introduced an endpoint to aggregate sales along with the necessary backend logic, message properties updates, and supporting tests. Updated `SalesService` method and added a new test scenario for verifying aggregation behavior.

diff --git a/app/backend/api/src/main/java/com/example/sms/presentation/api/sales/SalesApiController.java b/app/backend/api/src/main/java/com/example/sms/presentation/api/sales/SalesApiController.java
index bc5f45f8..f0b2e376 100644
--- a/app/backend/api/src/main/java/com/example/sms/presentation/api/sales/SalesApiController.java
+++ b/app/backend/api/src/main/java/com/example/sms/presentation/api/sales/SalesApiController.java
@@ -123,4 +123,15 @@ public class SalesApiController {
             return ResponseEntity.badRequest().body(new MessageResponse(e.getMessage()));
         }
     }
+
+    @Operation(summary = "売上を集計する", description = "売上を集計します。")
+    @PostMapping("/aggregate")
+    public ResponseEntity<?> aggregate(@RequestBody SalesResource resource) {
+        try {
+            salesService.aggregate();
+            return ResponseEntity.ok(new MessageResponse(message.getMessage("success.sales.aggregated")));
+        } catch (BusinessException | IllegalArgumentException e) {
+            return ResponseEntity.badRequest().body(new MessageResponse(e.getMessage()));
+        }
+    }
 }
\ No newline at end of file
diff --git a/app/backend/api/src/main/java/com/example/sms/service/sales/SalesService.java b/app/backend/api/src/main/java/com/example/sms/service/sales/SalesService.java
index e7c278ec..b2fdce81 100644
--- a/app/backend/api/src/main/java/com/example/sms/service/sales/SalesService.java
+++ b/app/backend/api/src/main/java/com/example/sms/service/sales/SalesService.java
@@ -81,7 +81,7 @@ public class SalesService {
     /**
      * 売上集計
      */
-    public void aggregateSales() {
+    public void aggregate() {
         ShippingList  shippingList = shippingRepository.selectAllComplete();
 
         List<String> orderNumberList = shippingList.asList().stream()
diff --git a/app/backend/api/src/main/resources/messages.properties b/app/backend/api/src/main/resources/messages.properties
index dbf8bc60..162f4fdc 100644
--- a/app/backend/api/src/main/resources/messages.properties
+++ b/app/backend/api/src/main/resources/messages.properties
@@ -68,6 +68,7 @@ success.sales.updated=Sales update success
 success.sales.deleted=Sales delete success
 error.sales.already.exist=Sales already exist
 error.sales.not.exist=Sales not found
+success.sales.aggregated=Sales aggregated success
 success.shipping.saved=Shipping saved success
 success.shipping-order.exec=ShippingOrder execute success
 error.shipping.not.exist=Shipping not found
diff --git a/app/backend/api/src/main/resources/messages_ja.properties b/app/backend/api/src/main/resources/messages_ja.properties
index 655a273d..234d27c2 100644
--- a/app/backend/api/src/main/resources/messages_ja.properties
+++ b/app/backend/api/src/main/resources/messages_ja.properties
@@ -66,6 +66,7 @@ error.sales-order.check=\u53D7\u6CE8\u30EB\u30FC\u30EB\u30C1\u30A7\u30C3\u30AF\u
 success.sales.registered=\u58F2\u4E0A\u3092\u767B\u9332\u3057\u307E\u3057\u305F
 success.sales.updated=\u58F2\u4E0A\u3092\u66F4\u65B0\u3057\u307E\u3057\u305F
 success.sales.deleted=\u58F2\u4E0A\u3092\u524A\u9664\u3057\u307E\u3057\u305F
+success.sales.aggregated=\u58F2\u4E0A\u3092\u96C6\u8A08\u3057\u307E\u3057\u305F
 error.sales.already.exist=\u767B\u9332\u6E08\u307F\u306E\u58F2\u4E0A\u3067\u3059
 error.sales.not.exist=\u8A72\u5F53\u3059\u308B\u58F2\u4E0A\u304C\u5B58\u5728\u3057\u307E\u305B\u3093
 success.shipping.saved=\u51FA\u8377\u3092\u4FDD\u5B58\u3057\u307E\u3057\u305F
diff --git a/app/backend/api/src/test/java/com/example/sms/service/sales/SalesServiceTest.java b/app/backend/api/src/test/java/com/example/sms/service/sales/SalesServiceTest.java
index 898bec63..5d8e9b41 100644
--- a/app/backend/api/src/test/java/com/example/sms/service/sales/SalesServiceTest.java
+++ b/app/backend/api/src/test/java/com/example/sms/service/sales/SalesServiceTest.java
@@ -206,7 +206,7 @@ class SalesServiceTest {
                     salesLines
             );
 
-            salesService.aggregateSales();
+            salesService.aggregate();
             Sales actual = salesService.find(salesNumber);
 
             assertNotNull(actual);
@@ -217,8 +217,8 @@ class SalesServiceTest {
         @Test
         @DisplayName("2回目の集計で売上が重複しない")
         void shouldNotDuplicateSalesOnSecondAggregation() {
-            salesService.aggregateSales();
-            salesService.aggregateSales();
+            salesService.aggregate();
+            salesService.aggregate();
 
             SalesList result = salesService.selectAll();
             assertEquals(6, result.asList().size());
diff --git a/app/backend/api/src/test/java/com/example/sms/stepdefinitions/UC017StepDefs.java b/app/backend/api/src/test/java/com/example/sms/stepdefinitions/UC017StepDefs.java
index 0affca11..be3500e0 100644
--- a/app/backend/api/src/test/java/com/example/sms/stepdefinitions/UC017StepDefs.java
+++ b/app/backend/api/src/test/java/com/example/sms/stepdefinitions/UC017StepDefs.java
@@ -150,6 +150,18 @@ public class UC017StepDefs extends SpringAcceptanceTest {
         executePost(SALES_API_URL, json);
     }
 
+    @もし(":UC017 売上集計を実行した")
+    public void aggregate() throws JsonProcessingException {
+        String url = SALES_API_URL + "/aggregate";
+
+        SalesResource sales = getSalesResource("1000000", "2023-10-01T00:00:00Z", "D0001", "C0001", "E0001", "10000");
+
+        ObjectMapper objectMapper = new ObjectMapper();
+        objectMapper.registerModule(new JavaTimeModule());
+        String json = objectMapper.writeValueAsString(sales);
+        executePost(url, json);
+    }
+
     @かつ(":UC017 売上番号 {string} の売上明細を更新する \\(製品コード {string} 製品名 {string} 売上数量 {string})")
     public void updateSalesLine(String salesNumber, String productCode, String productName, String amount) throws JsonProcessingException {
         String url = SALES_API_URL + "/" + salesNumber;
@@ -294,6 +306,5 @@ public class UC017StepDefs extends SpringAcceptanceTest {
                 .discountAmount(Integer.parseInt(entry.get("割引額")))
                 .build();
     }
-
 }
 
diff --git a/app/backend/api/src/test/resources/features/sales/Sales.feature b/app/backend/api/src/test/resources/features/sales/Sales.feature
index b90980e8..22f1ccf5 100644
--- a/app/backend/api/src/test/resources/features/sales/Sales.feature
+++ b/app/backend/api/src/test/resources/features/sales/Sales.feature
@@ -65,3 +65,7 @@
       | 2000000010 | 1       | 20202002   | カップ麺   | 300      | 20       | 200    |
     かつ:UC017 売上番号 "2000000010" 製品コード "20202002" の売上明細を削除する
     ならば:UC017 "売上を更新しました" が表示される
+
+  シナリオ: 売上げを集計する
+    もし:UC017 売上集計を実行した
+    ならば:UC017 "売上を集計しました" が表示される

```

## コミット: f770c702

### メッセージ

```
feat(WIP):出荷/売上管理
Add sales aggregation functionality to SalesService

Implemented the `aggregateSales` method in `SalesService` to calculate sales data based on shipping information. Added a corresponding unit test to verify the functionality, ensuring proper sales data aggregation and validation.
```

### 変更されたファイル

- M	app/backend/api/src/main/java/com/example/sms/service/sales/SalesService.java
- M	app/backend/api/src/test/java/com/example/sms/TestDataFactory.java
- M	app/backend/api/src/test/java/com/example/sms/TestDataFactoryImpl.java
- M	app/backend/api/src/test/java/com/example/sms/service/sales/SalesServiceTest.java

### 作業解説

#### 目的
新機能の実装により、システムの機能性を拡張する。

#### 技術的な背景
ユーザー要件または技術的要件に基づき、新機能を設計・実装。既存アーキテクチャとの整合性を保ちながら拡張。

#### 変更の影響範囲
影響を受けるレイヤ: サービス層、プレゼンテーション層、テスト。変更は関連するテストおよびドキュメントへの波及を考慮する必要がある。

### 変更内容

```diff
commit f770c70259f8115d37cfc790887917bd6eb54419
Author: k2works <kakimomokuri@gmail.com>
Date:   Wed Apr 9 13:33:32 2025 +0900

    feat(WIP):出荷/売上管理
    
    Add sales aggregation functionality to SalesService
    
    Implemented the `aggregateSales` method in `SalesService` to calculate sales data based on shipping information. Added a corresponding unit test to verify the functionality, ensuring proper sales data aggregation and validation.

diff --git a/app/backend/api/src/main/java/com/example/sms/service/sales/SalesService.java b/app/backend/api/src/main/java/com/example/sms/service/sales/SalesService.java
index 1abdeb5d..e7c278ec 100644
--- a/app/backend/api/src/main/java/com/example/sms/service/sales/SalesService.java
+++ b/app/backend/api/src/main/java/com/example/sms/service/sales/SalesService.java
@@ -1,11 +1,19 @@
 package com.example.sms.service.sales;
 
 import com.example.sms.domain.model.sales.Sales;
+import com.example.sms.domain.model.sales.SalesLine;
 import com.example.sms.domain.model.sales.SalesList;
+import com.example.sms.domain.model.shipping.Shipping;
+import com.example.sms.domain.model.shipping.ShippingList;
+import com.example.sms.service.shipping.ShippingRepository;
 import com.github.pagehelper.PageInfo;
 import jakarta.transaction.Transactional;
 import org.springframework.stereotype.Service;
 
+import java.util.ArrayList;
+import java.util.List;
+import java.util.Objects;
+
 /**
  * 売上サービス
  */
@@ -14,9 +22,11 @@ import org.springframework.stereotype.Service;
 public class SalesService {
 
     private final SalesRepository salesRepository;
+    private final ShippingRepository shippingRepository;
 
-    public SalesService(SalesRepository salesRepository) {
+    public SalesService(SalesRepository salesRepository, ShippingRepository shippingRepository) {
         this.salesRepository = salesRepository;
+        this.shippingRepository = shippingRepository;
     }
 
     /**
@@ -68,4 +78,59 @@ public class SalesService {
         return salesRepository.searchWithPageInfo(criteria);
     }
 
+    /**
+     * 売上集計
+     */
+    public void aggregateSales() {
+        ShippingList  shippingList = shippingRepository.selectAllComplete();
+
+        List<String> orderNumberList = shippingList.asList().stream()
+                .map(shipping -> shipping.getOrderNumber().getValue())
+                .distinct()
+                .toList();
+
+        List<Sales> salesList = new ArrayList<>();
+
+        orderNumberList.forEach(orderNumber -> {
+            List<Shipping> shippingListByOrderNumber = shippingList.asList().stream()
+                    .filter(shipping -> Objects.equals(shipping.getOrderNumber().getValue(), orderNumber))
+                    .toList();
+
+            List<SalesLine> salesLines = shippingListByOrderNumber.stream()
+                    .map(shipping -> SalesLine.of(
+                            shipping.getOrderNumber().getValue(),
+                            shipping.getOrderLineNumber(),
+                            shipping.getProductCode().getValue(),
+                            shipping.getProductName(),
+                            shipping.getSalesUnitPrice().getAmount(),
+                            shipping.getSalesAmount().getValue().getAmount(),
+                            shipping.getShippedQuantity().getAmount(),
+                            shipping.getDiscountAmount().getAmount(),
+                            null,
+                            null,
+                            null,
+                            shipping.getDeliveryDate().getValue()
+                    ))
+                    .toList();
+
+            Shipping shipping = shippingListByOrderNumber.getFirst();
+            Sales sales = Sales.of(
+                    shipping.getOrderNumber().getValue(),
+                    shipping.getOrderNumber().getValue(),
+                    shipping.getDeliveryDate().getValue(),
+                    null,
+                    shipping.getDepartmentCode().getValue(),
+                    shipping.getDepartmentStartDate(),
+                    shipping.getCustomerCode().getCode().getValue(),
+                    shipping.getEmployeeCode().getValue(),
+                    null,
+                    null,
+                    shipping.getRemarks(),
+                    salesLines
+            );
+            salesList.add(sales);
+        });
+
+        salesList.forEach(salesRepository::save);
+    }
 }
\ No newline at end of file
diff --git a/app/backend/api/src/test/java/com/example/sms/TestDataFactory.java b/app/backend/api/src/test/java/com/example/sms/TestDataFactory.java
index 39a06410..40836385 100644
--- a/app/backend/api/src/test/java/com/example/sms/TestDataFactory.java
+++ b/app/backend/api/src/test/java/com/example/sms/TestDataFactory.java
@@ -39,11 +39,11 @@ public interface TestDataFactory {
 
     void setUpForShippingRuleCheckService();
 
+    void setUpForSalesService();
+
     MultipartFile createSalesOrderFile();
 
     MultipartFile createSalesOrderInvalidFile();
 
     MultipartFile createSalesOrderCheckRuleFile();
-
-    void setUpForSalesService();
 }
diff --git a/app/backend/api/src/test/java/com/example/sms/TestDataFactoryImpl.java b/app/backend/api/src/test/java/com/example/sms/TestDataFactoryImpl.java
index b9122a80..88662f70 100644
--- a/app/backend/api/src/test/java/com/example/sms/TestDataFactoryImpl.java
+++ b/app/backend/api/src/test/java/com/example/sms/TestDataFactoryImpl.java
@@ -415,6 +415,9 @@ public class TestDataFactoryImpl implements TestDataFactory {
                 9    // 入荷リードタイム
         ));
 
+        // 出荷データの準備
+        setUpForShippingService();
+
         // 売上データの削除
         salesRepository.deleteAll();
 
diff --git a/app/backend/api/src/test/java/com/example/sms/service/sales/SalesServiceTest.java b/app/backend/api/src/test/java/com/example/sms/service/sales/SalesServiceTest.java
index f62f7647..898bec63 100644
--- a/app/backend/api/src/test/java/com/example/sms/service/sales/SalesServiceTest.java
+++ b/app/backend/api/src/test/java/com/example/sms/service/sales/SalesServiceTest.java
@@ -20,6 +20,7 @@ import org.springframework.beans.factory.annotation.Autowired;
 
 import java.time.LocalDateTime;
 import java.util.List;
+import java.util.stream.IntStream;
 
 import static org.junit.jupiter.api.Assertions.*;
 
@@ -168,5 +169,59 @@ class SalesServiceTest {
             assertEquals(1, result.getList().size());
             assertEquals(departmentCode, result.getList().get(0).getDepartmentCode());
         }
+
+        @Test
+        @DisplayName("売上を集計できる")
+        void shouldAggregateSales() {
+            String salesNumber = "1000000001";
+            List<SalesLine> salesLines = IntStream.range(1, 4)
+                    .mapToObj(lineNumber -> SalesLine.of(
+                            salesNumber,
+                            lineNumber,
+                            "99999999",
+                            "商品1",
+                            1000,
+                            10000,
+                            10,
+                            10,
+                            null,
+                            null,
+                            null,
+                            LocalDateTime.of(2021, 1, 1, 0, 0)
+                    ))
+                    .toList();
+
+            Sales expected = Sales.of(
+                    salesNumber,
+                    "1000000001",
+                    LocalDateTime.of(2021, 1, 1, 0, 0),
+                    null,
+                    "10000",
+                    LocalDateTime.of(2021, 1, 1, 0, 0),
+                    "001",
+                    "EMP001",
+                    null,
+                    null,
+                    "備考",
+                    salesLines
+            );
+
+            salesService.aggregateSales();
+            Sales actual = salesService.find(salesNumber);
+
+            assertNotNull(actual);
+            assertEquals(3, actual.getSalesLines().size());
+            assertEquals(expected, actual);
+        }
+
+        @Test
+        @DisplayName("2回目の集計で売上が重複しない")
+        void shouldNotDuplicateSalesOnSecondAggregation() {
+            salesService.aggregateSales();
+            salesService.aggregateSales();
+
+            SalesList result = salesService.selectAll();
+            assertEquals(6, result.asList().size());
+        }
     }
 }
\ No newline at end of file

```

## コミット: 25562c0e

### メッセージ

```
feat(WIP):出荷/売上管理
Set default completionFlag to false and pass search criteria.

Updated the completionFlag default value to false in shipping model to ensure proper handling of falsy values. Adjusted fetchShippings call to include searchShippingCriteria for more precise data retrieval.
```

### 変更されたファイル

- M	app/frontend/src/components/sales/shipping/order/ShippingOrderContainer.tsx
- M	app/frontend/src/models/sales/shipping.ts

### 作業解説

#### 目的
新機能の実装により、システムの機能性を拡張する。

#### 技術的な背景
ユーザー要件または技術的要件に基づき、新機能を設計・実装。既存アーキテクチャとの整合性を保ちながら拡張。

#### 変更の影響範囲
影響を受けるレイヤ: フロントエンド。変更は関連するテストおよびドキュメントへの波及を考慮する必要がある。

### 変更内容

```diff
commit 25562c0e5ec7c048e117e61830a2ac820b9236dd
Author: k2works <kakimomokuri@gmail.com>
Date:   Wed Apr 9 12:09:39 2025 +0900

    feat(WIP):出荷/売上管理
    
    Set default completionFlag to false and pass search criteria.
    
    Updated the completionFlag default value to false in shipping model to ensure proper handling of falsy values. Adjusted fetchShippings call to include searchShippingCriteria for more precise data retrieval.

diff --git a/app/frontend/src/components/sales/shipping/order/ShippingOrderContainer.tsx b/app/frontend/src/components/sales/shipping/order/ShippingOrderContainer.tsx
index f936848a..e7310ba7 100644
--- a/app/frontend/src/components/sales/shipping/order/ShippingOrderContainer.tsx
+++ b/app/frontend/src/components/sales/shipping/order/ShippingOrderContainer.tsx
@@ -14,6 +14,7 @@ export const ShippingOrderContainer: React.FC = () => {
             loading,
             setError,
             fetchShippings,
+            searchShippingCriteria
         } = useShippingContext();
 
         const {
@@ -36,7 +37,7 @@ export const ShippingOrderContainer: React.FC = () => {
             (async () => {
                 try {
                     await Promise.all([
-                        await fetchShippings.load(),
+                        await fetchShippings.load(1, searchShippingCriteria),
                         fetchDepartments.load(),
                         fetchEmployees.load(),
                         fetchCustomers.load(),
diff --git a/app/frontend/src/models/sales/shipping.ts b/app/frontend/src/models/sales/shipping.ts
index ce9972b4..c732a85b 100644
--- a/app/frontend/src/models/sales/shipping.ts
+++ b/app/frontend/src/models/sales/shipping.ts
@@ -103,7 +103,7 @@ export const mapToShippingCriteriaResource = (criteria: ShippingCriteriaType) =>
         productCode: criteria.productCode || null,
         productName: criteria.productName || null,
         deliveryDate: criteria.deliveryDate || null,
-        completionFlag: criteria.completionFlag || null
+        completionFlag: criteria.completionFlag || false
     };
 };
 

```

## コミット: 44ab8356

### メッセージ

```
feat(WIP):出荷/売上管理
Filter shippings by completion flag in data source.

Added logic to filter shipping records based on the completion flag in `ShippingDataSource`. This ensures that only records matching the specified flag are included in the results. The change improves query flexibility based on specific criteria.
```

### 変更されたファイル

- M	app/backend/api/src/main/java/com/example/sms/infrastructure/datasource/shipping/ShippingDataSource.java

### 作業解説

#### 目的
新機能の実装により、システムの機能性を拡張する。

#### 技術的な背景
ユーザー要件または技術的要件に基づき、新機能を設計・実装。既存アーキテクチャとの整合性を保ちながら拡張。

#### 変更の影響範囲
影響を受けるレイヤ: プレゼンテーション層、インフラストラクチャ層。変更は関連するテストおよびドキュメントへの波及を考慮する必要がある。

### 変更内容

```diff
commit 44ab8356131f6104625189d8dfe9c7757e56b558
Author: k2works <kakimomokuri@gmail.com>
Date:   Wed Apr 9 12:09:12 2025 +0900

    feat(WIP):出荷/売上管理
    
    Filter shippings by completion flag in data source.
    
    Added logic to filter shipping records based on the completion flag in `ShippingDataSource`. This ensures that only records matching the specified flag are included in the results. The change improves query flexibility based on specific criteria.

diff --git a/app/backend/api/src/main/java/com/example/sms/infrastructure/datasource/shipping/ShippingDataSource.java b/app/backend/api/src/main/java/com/example/sms/infrastructure/datasource/shipping/ShippingDataSource.java
index 35cbec80..6b88b942 100644
--- a/app/backend/api/src/main/java/com/example/sms/infrastructure/datasource/shipping/ShippingDataSource.java
+++ b/app/backend/api/src/main/java/com/example/sms/infrastructure/datasource/shipping/ShippingDataSource.java
@@ -225,6 +225,10 @@ public class ShippingDataSource implements ShippingRepository {
                 }
             }
         }
+        if (criteria.getCompletionFlag() != null) {
+            CompletionFlag checkFlag = CompletionFlag.of(criteria.getCompletionFlag() ? 1 : 0);
+            shippings.removeIf(shipping -> !shipping.getCompletionFlag().equals(checkFlag));
+        }
 
         return new PageInfo<>(shippings);
     }
@@ -246,6 +250,10 @@ public class ShippingDataSource implements ShippingRepository {
                 }
             }
         }
+        if (criteria.getCompletionFlag() != null) {
+            CompletionFlag checkFlag = CompletionFlag.of(criteria.getCompletionFlag() ? 1 : 0);
+            shippings.removeIf(shipping -> !shipping.getCompletionFlag().equals(checkFlag));
+        }
 
         return shippingEntityMapper.mapToShippingList(shippings);
     }

```

