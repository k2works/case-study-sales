# 作業履歴 2025-02-28

## 概要

2025-02-28の作業内容をまとめています。

## コミット: 560392dc

### メッセージ

```
chore:Wiki.js追加
Add Wiki.js and PlantUML services to docker-compose

This commit introduces two new services: Wiki.js and PlantUML. Wiki.js is configured to use a MySQL database, while PlantUML provides diagram generation capabilities. These additions enhance the functionality of the application stack.
```

### 変更されたファイル

- M	docker-compose.yml

### 作業解説

#### 目的
開発環境・ビルド設定の整備により、開発効率を向上させる。

#### 技術的な背景
開発ワークフローの効率化や依存関係の更新など、非機能的な改善を実施。

#### 変更の影響範囲
影響を受けるレイヤ: 特定のレイヤに限定されない。変更は関連するテストおよびドキュメントへの波及を考慮する必要がある。

### 変更内容

```diff
commit 560392dc9d2eddb2448cdc5338644947ec76b366
Author: k2works <kakimomokuri@gmail.com>
Date:   Fri Feb 28 15:33:50 2025 +0900

    chore:Wiki.js追加
    
    Add Wiki.js and PlantUML services to docker-compose
    
    This commit introduces two new services: Wiki.js and PlantUML. Wiki.js is configured to use a MySQL database, while PlantUML provides diagram generation capabilities. These additions enhance the functionality of the application stack.

diff --git a/docker-compose.yml b/docker-compose.yml
index 188ac75c..c1a1340a 100644
--- a/docker-compose.yml
+++ b/docker-compose.yml
@@ -104,6 +104,30 @@ services:
     ports:
       - "5252:5252"
 
+  wiki:
+    image: ghcr.io/requarks/wiki:2
+    environment:
+      DB_TYPE: mysql
+      DB_HOST: db_mysql
+      DB_PORT: 3306
+      DB_USER: user
+      DB_PASS: password
+      DB_NAME: mydb
+    ports:
+      - "80:3000"
+    links:
+      - db_mysql
+      - plantuml
+
+  plantuml:
+    image: plantuml/plantuml-server:jetty
+    container_name: plantuml
+    hostname: plantuml
+    environment:
+      BASE_URL: plantuml
+    ports:
+      - "9999:8080"
+
 volumes:
   mysql8-data:
   postgresql-data:

```

