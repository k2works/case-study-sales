# 作業履歴 2024-12-23

## 概要

2024-12-23の作業内容をまとめています。

## コミット: 64d16d5a

### メッセージ

```
refactor:パッケージの整理
Restructure view folder organization for audit and download.

Moved audit and download-related view files into dedicated subfolders to improve folder structure and maintainability. Updated import paths across components to reflect the new locations. This change enhances logical grouping and better organization of the codebase.
```

### 変更されたファイル

- M	app/frontend/src/components/system/Audit.tsx
- M	app/frontend/src/components/system/Download.tsx

### 変更内容

```diff
commit 64d16d5a4c2893c4bcd2539d1253ef7351be367a
Author: k2works <kakimomokuri@gmail.com>
Date:   Mon Dec 23 19:19:47 2024 +0900

    refactor:パッケージの整理
    
    Restructure view folder organization for audit and download.
    
    Moved audit and download-related view files into dedicated subfolders to improve folder structure and maintainability. Updated import paths across components to reflect the new locations. This change enhances logical grouping and better organization of the codebase.

diff --git a/app/frontend/src/components/system/Audit.tsx b/app/frontend/src/components/system/Audit.tsx
index c408d137..9b3dc740 100644
--- a/app/frontend/src/components/system/Audit.tsx
+++ b/app/frontend/src/components/system/Audit.tsx
@@ -6,9 +6,9 @@ import {useAudit, useFetchAudits} from "./hooks.ts";
 import {AuditType, SearchAuditConditionType} from "../../models/audit.ts";
 import {SiteLayout} from "../../views/SiteLayout.tsx";
 import LoadingIndicator from "../../views/application/LoadingIndicatior.tsx";
-import {AuditCollectionView} from "../../views/system/AuditCollection.tsx";
-import {AuditSingleView} from "../../views/system/AuditSingle.tsx";
-import {AuditSearchSingleView} from "../../views/system/AuditSearch.tsx";
+import {AuditCollectionView} from "../../views/system/audit/AuditCollection.tsx";
+import {AuditSingleView} from "../../views/system/audit/AuditSingle.tsx";
+import {AuditSearchSingleView} from "../../views/system/audit/AuditSearch.tsx";
 
 export const Audit: React.FC = () => {
     const Content: React.FC = () => {
diff --git a/app/frontend/src/components/system/Download.tsx b/app/frontend/src/components/system/Download.tsx
index e1ca9c8f..2038e3de 100644
--- a/app/frontend/src/components/system/Download.tsx
+++ b/app/frontend/src/components/system/Download.tsx
@@ -4,7 +4,7 @@ import {DownloadService} from "../../services/download.ts";
 import {DownloadTarget} from "../../models/download.ts";
 import {SiteLayout} from "../../views/SiteLayout.tsx";
 import LoadingIndicator from "../../views/application/LoadingIndicatior.tsx";
-import {SingleView} from "../../views/system/DownloadSingle.tsx";
+import {SingleView} from "../../views/system/download/DownloadSingle.tsx";
 import {getKeyBySymbol, getSymbolByValue} from "../../models/utils.ts";
 
 export const Download: React.FC = () => {
diff --git a/app/frontend/src/views/system/AuditCollection.tsx b/app/frontend/src/views/system/audit/AuditCollection.tsx
similarity index 96%
rename from app/frontend/src/views/system/AuditCollection.tsx
rename to app/frontend/src/views/system/audit/AuditCollection.tsx
index ed122aef..c270a21a 100644
--- a/app/frontend/src/views/system/AuditCollection.tsx
+++ b/app/frontend/src/views/system/audit/AuditCollection.tsx
@@ -1,11 +1,11 @@
 import React from "react";
-import {PageNation, PageNationType} from "../application/PageNation.tsx";
-import {Message} from "../../components/application/Message.tsx";
+import {PageNation, PageNationType} from "../../application/PageNation.tsx";
+import {Message} from "../../../components/application/Message.tsx";
 import {
     AuditType,
     SearchAuditConditionType
-} from "../../models/audit.ts";
-import {convertToDateTimeInputFormat} from "../../components/application/utils.ts";
+} from "../../../models/audit.ts";
+import {convertToDateTimeInputFormat} from "../../../components/application/utils.ts";
 
 interface SearchProps {
     searchAuditCondition: SearchAuditConditionType;
diff --git a/app/frontend/src/views/system/AuditSearch.tsx b/app/frontend/src/views/system/audit/AuditSearch.tsx
similarity index 97%
rename from app/frontend/src/views/system/AuditSearch.tsx
rename to app/frontend/src/views/system/audit/AuditSearch.tsx
index 7b30cfa6..6c7d8a7e 100644
--- a/app/frontend/src/views/system/AuditSearch.tsx
+++ b/app/frontend/src/views/system/audit/AuditSearch.tsx
@@ -4,8 +4,8 @@ import {
     ApplicationExecutionProcessFlag,
     ApplicationExecutionProcessType,
     SearchAuditConditionType
-} from "../../models/audit.ts";
-import {FormSelect, SingleViewHeaderItem} from "../Common.tsx";
+} from "../../../models/audit.ts";
+import {FormSelect, SingleViewHeaderItem} from "../../Common.tsx";
 
 interface FormProps {
     condition: SearchAuditConditionType,
diff --git a/app/frontend/src/views/system/AuditSingle.tsx b/app/frontend/src/views/system/audit/AuditSingle.tsx
similarity index 95%
rename from app/frontend/src/views/system/AuditSingle.tsx
rename to app/frontend/src/views/system/audit/AuditSingle.tsx
index a27726f1..bb12b4ba 100644
--- a/app/frontend/src/views/system/AuditSingle.tsx
+++ b/app/frontend/src/views/system/audit/AuditSingle.tsx
@@ -1,8 +1,8 @@
 import React from "react";
-import {Message} from "../../components/application/Message.tsx";
-import {FormInput, FormTextarea, SingleViewHeaderItem} from "../Common.tsx";
-import {AuditType} from "../../models/audit.ts";
-import {convertToDateTimeInputFormat} from "../../components/application/utils.ts";
+import {Message} from "../../../components/application/Message.tsx";
+import {FormInput, FormTextarea, SingleViewHeaderItem} from "../../Common.tsx";
+import {AuditType} from "../../../models/audit.ts";
+import {convertToDateTimeInputFormat} from "../../../components/application/utils.ts";
 
 interface HeaderProps {
     title: string;
diff --git a/app/frontend/src/views/system/DownloadSingle.tsx b/app/frontend/src/views/system/download/DownloadSingle.tsx
similarity index 95%
rename from app/frontend/src/views/system/DownloadSingle.tsx
rename to app/frontend/src/views/system/download/DownloadSingle.tsx
index 852c76dc..e963ea63 100644
--- a/app/frontend/src/views/system/DownloadSingle.tsx
+++ b/app/frontend/src/views/system/download/DownloadSingle.tsx
@@ -1,7 +1,7 @@
-import {DownloadTarget} from "../../models/download.ts";
+import {DownloadTarget} from "../../../models/download.ts";
 import React from "react";
-import {Message} from "../../components/application/Message.tsx";
-import {getKeyBySymbol} from "../../models/utils.ts";
+import {Message} from "../../../components/application/Message.tsx";
+import {getKeyBySymbol} from "../../../models/utils.ts";
 
 interface FormProps {
     selectedTarget: DownloadTarget | null;

```

## コミット: 35f1a67e

### メッセージ

```
Merge branch 'take03' into develop
```

### 変更されたファイル


### 変更内容

```diff
commit 35f1a67e90923c7b975d3297fe0e0c7bb27010b9
Merge: ea7fbcaf 756f4c60
Author: k2works <kakimomokuri@gmail.com>
Date:   Mon Dec 23 19:14:28 2024 +0900

    Merge branch 'take03' into develop


```

## コミット: 756f4c60

### メッセージ

```
refactor:パッケージの整理
Refactor user view folder structure for better organization

Moved UserSelect.tsx into a 'user' subfolder to enhance clarity and maintainability. Adjusted related import paths in all affected files to reflect the updated directory structure.
```

### 変更されたファイル

- M	app/frontend/src/components/master/Employee.tsx

### 変更内容

```diff
commit 756f4c60788408b8f86164a7d2b3a16a80947442
Author: k2works <kakimomokuri@gmail.com>
Date:   Mon Dec 23 19:14:08 2024 +0900

    refactor:パッケージの整理
    
    Refactor user view folder structure for better organization
    
    Moved UserSelect.tsx into a 'user' subfolder to enhance clarity and maintainability. Adjusted related import paths in all affected files to reflect the updated directory structure.

diff --git a/app/frontend/src/components/master/Employee.tsx b/app/frontend/src/components/master/Employee.tsx
index b43f9c15..41dbcec5 100644
--- a/app/frontend/src/components/master/Employee.tsx
+++ b/app/frontend/src/components/master/Employee.tsx
@@ -10,7 +10,7 @@ import {SiteLayout} from "../../views/SiteLayout.tsx";
 import LoadingIndicator from "../../views/application/LoadingIndicatior.tsx";
 import {useFetchUsers, useUser} from "../system/hooks.ts";
 import {DepartmentCollectionSelectView, DepartmentSelectView} from "../../views/master/department/DepartmentSelect.tsx";
-import {UserCollectionSelectView, UserSelectView} from "../../views/system/UserSelect.tsx";
+import {UserCollectionSelectView, UserSelectView} from "../../views/system/user/UserSelect.tsx";
 import {EmployeeCollectionView} from "../../views/master/employee/EmployeeCollection.tsx";
 import {EmployeeSingleView} from "../../views/master/employee/EmployeeSingle.tsx";
 
diff --git a/app/frontend/src/views/system/UserSelect.tsx b/app/frontend/src/views/system/user/UserSelect.tsx
similarity index 97%
rename from app/frontend/src/views/system/UserSelect.tsx
rename to app/frontend/src/views/system/user/UserSelect.tsx
index 08727374..d14daf15 100644
--- a/app/frontend/src/views/system/UserSelect.tsx
+++ b/app/frontend/src/views/system/user/UserSelect.tsx
@@ -1,7 +1,7 @@
 import React from 'react';
 import {FaTimes} from 'react-icons/fa';
-import {PageNation} from '../application/PageNation.tsx';
-import {UserAccountType} from "../../models";
+import {PageNation} from '../../application/PageNation.tsx';
+import {UserAccountType} from "../../../models";
 
 interface UserSelectProps {
     handleSelect: () => void;

```

## コミット: 0d0fd0d7

### メッセージ

```
Merge branch 'take02' into take03
# Conflicts:
#	app/frontend/src/components/application/RouteConfig.tsx
#	app/frontend/src/components/system/Auth.tsx
#	app/frontend/src/components/system/User.tsx
#	app/frontend/src/views/system/user/UserCollection.tsx
```

### 変更されたファイル


### 変更内容

```diff
commit 0d0fd0d761c711ec78a970e289d404f25d106095
Merge: 6a79e1fd 1cbef0b8
Author: k2works <kakimomokuri@gmail.com>
Date:   Mon Dec 23 19:09:02 2024 +0900

    Merge branch 'take02' into take03
    
    # Conflicts:
    #       app/frontend/src/components/application/RouteConfig.tsx
    #       app/frontend/src/components/system/Auth.tsx
    #       app/frontend/src/components/system/User.tsx
    #       app/frontend/src/views/system/user/UserCollection.tsx

diff --cc app/frontend/src/components/application/RouteConfig.tsx
index bfd1be85,552154ba..eb225be0
--- a/app/frontend/src/components/application/RouteConfig.tsx
+++ b/app/frontend/src/components/application/RouteConfig.tsx
@@@ -1,36 -1,13 +1,35 @@@
  import {Route, Routes} from "react-router-dom";
  import React from "react";
 -import {RouteAuthGuard} from "./RouteAuthGuard.tsx";
 +import {RouteAuthGuard} from "./RouteAuthGuard";
- import {Login} from "../system/Login.tsx";
- import {Logout} from "../system/Logout.tsx";
++import {Home} from "./Home.tsx";
  import {User} from "../system/User.tsx";
 -import {Home} from "../../views/application/Home.tsx";
+ import {NotFound} from "../../views/application/NotFound.tsx";
  import {RoleType} from "../../models";
+ import {Login, Logout} from "../system/Auth.tsx";
 +import {Department} from "../master/Department.tsx";
 +import {Employee} from "../master/Employee.tsx";
- import {NotFound} from "../../views/application/NotFound.tsx";
- import {Home} from "./Home.tsx";
 +import {Product} from "../master/Product.tsx";
 +import {SiteLayout} from "../../views/SiteLayout.tsx";
 +import {ProductCategory} from "../master/ProductCategory.tsx";
 +import {ProductItem} from "../master/ProductItem.tsx";
  
  export const RouteConfig: React.FC = () => {
 +    const ProductCategoryPage = () => {
 +        return (
 +            <SiteLayout>
 +                <ProductCategory/>
 +            </SiteLayout>
 +        );
 +    }
 +
 +    const ProductDetailPage = () => {
 +        return (
 +            <SiteLayout>
 +                <ProductItem/>
 +            </SiteLayout>
 +        );
 +    }
 +
      return (
          <>
              <Routes>
diff --cc app/frontend/src/components/system/User.tsx
index 700e055e,9680325b..3e9f4f41
--- a/app/frontend/src/components/system/User.tsx
+++ b/app/frontend/src/components/system/User.tsx
@@@ -1,13 -1,13 +1,13 @@@
 -import {useMessage} from "../application/Message.tsx";
 -import {useModal} from "../application/hooks.ts";
 -import {useFetchUsers, useUser} from "./hooks.ts";
 +import React, {useEffect, useState} from "react";
- import {UserAccountType} from "../../models";
- import {useMessage} from "../application/Message";
 +import {useModal} from "../application/hooks";
 +import {useFetchUsers, useUser} from "./hooks";
  import {usePageNation} from "../../views/application/PageNation.tsx";
 +import LoadingIndicator from "../../views/application/LoadingIndicatior.tsx";
++import {useMessage} from "../application/Message";
  import {SiteLayout} from "../../views/SiteLayout.tsx";
- import {UserCollectionView} from "../../views/system/UserCollection.tsx";
- import {UserSingleView} from "../../views/system/UserSingle.tsx";
++import {UserAccountType} from "../../models";
+ import {UserCollectionView} from "../../views/system/user/UserCollection.tsx";
+ import {UserSingleView} from "../../views/system/user/UserSingle.tsx";
 -import React, {useEffect, useState} from "react";
 -import LoadingIndicator from "../../views/application/LoadingIndicatior.tsx";
 -import {UserAccountType} from "../../models";
  
  export const User: React.FC = () => {
      const Content: React.FC = () => {

```

## コミット: 1cbef0b8

### メッセージ

```
refactor:パッケージの整理
Refactor folder structure for user and auth views

Moved User and Auth-related view components into dedicated subfolders ('user' and 'auth') to improve organization and maintainability. Updated import paths accordingly across the affected files to align with the new structure.
```

### 変更されたファイル

- M	app/frontend/src/components/system/Auth.tsx
- M	app/frontend/src/components/system/User.tsx

### 変更内容

```diff
commit 1cbef0b82d97e6ca3f8270a3cf07b10d99319c26
Author: k2works <kakimomokuri@gmail.com>
Date:   Mon Dec 23 19:02:29 2024 +0900

    refactor:パッケージの整理
    
    Refactor folder structure for user and auth views
    
    Moved User and Auth-related view components into dedicated subfolders ('user' and 'auth') to improve organization and maintainability. Updated import paths accordingly across the affected files to align with the new structure.

diff --git a/app/frontend/src/components/system/Auth.tsx b/app/frontend/src/components/system/Auth.tsx
index e6ef3f6d..600c7248 100644
--- a/app/frontend/src/components/system/Auth.tsx
+++ b/app/frontend/src/components/system/Auth.tsx
@@ -2,7 +2,7 @@ import React, {useEffect, useState} from "react";
 import {useLocation, useNavigate} from "react-router-dom";
 import {AuthUserContextType, useAuthUserContext} from "../../providers/AuthUser.tsx";
 import AuthService from "../../services/auth.ts";
-import {LoginSingleView} from "../../views/system/Login.tsx";
+import {LoginSingleView} from "../../views/system/auth/Login.tsx";
 import {CustomLocation, DataType, RoleType, UserType} from "../../models";
 
 const DEFAULT_USER_ID = "U000003";
diff --git a/app/frontend/src/components/system/User.tsx b/app/frontend/src/components/system/User.tsx
index 126aba09..9680325b 100644
--- a/app/frontend/src/components/system/User.tsx
+++ b/app/frontend/src/components/system/User.tsx
@@ -3,8 +3,8 @@ import {useModal} from "../application/hooks.ts";
 import {useFetchUsers, useUser} from "./hooks.ts";
 import {usePageNation} from "../../views/application/PageNation.tsx";
 import {SiteLayout} from "../../views/SiteLayout.tsx";
-import {UserCollectionView} from "../../views/system/UserCollection.tsx";
-import {UserSingleView} from "../../views/system/UserSingle.tsx";
+import {UserCollectionView} from "../../views/system/user/UserCollection.tsx";
+import {UserSingleView} from "../../views/system/user/UserSingle.tsx";
 import React, {useEffect, useState} from "react";
 import LoadingIndicator from "../../views/application/LoadingIndicatior.tsx";
 import {UserAccountType} from "../../models";
diff --git a/app/frontend/src/views/system/Login.tsx b/app/frontend/src/views/system/auth/Login.tsx
similarity index 100%
rename from app/frontend/src/views/system/Login.tsx
rename to app/frontend/src/views/system/auth/Login.tsx
diff --git a/app/frontend/src/views/system/UserCollection.tsx b/app/frontend/src/views/system/user/UserCollection.tsx
similarity index 96%
rename from app/frontend/src/views/system/UserCollection.tsx
rename to app/frontend/src/views/system/user/UserCollection.tsx
index f0433208..31207bec 100644
--- a/app/frontend/src/views/system/UserCollection.tsx
+++ b/app/frontend/src/views/system/user/UserCollection.tsx
@@ -1,7 +1,7 @@
 import React from "react";
-import {PageNation, PageNationType} from "../application/PageNation.tsx";
-import {Message} from "../../components/application/Message.tsx";
-import {UserAccountType} from "../../models";
+import {PageNation, PageNationType} from "../../application/PageNation.tsx";
+import {Message} from "../../../components/application/Message.tsx";
+import {UserAccountType} from "../../../models";
 
 interface SearchBarProps {
     searchValue: string;
diff --git a/app/frontend/src/views/system/UserSingle.tsx b/app/frontend/src/views/system/user/UserSingle.tsx
similarity index 96%
rename from app/frontend/src/views/system/UserSingle.tsx
rename to app/frontend/src/views/system/user/UserSingle.tsx
index 87e94a53..4abccc94 100644
--- a/app/frontend/src/views/system/UserSingle.tsx
+++ b/app/frontend/src/views/system/user/UserSingle.tsx
@@ -1,7 +1,7 @@
 import React from "react";
-import {RoleNameEnumType, UserAccountType} from "../../models";
-import {Message} from "../../components/application/Message.tsx";
-import {FormInput, FormSelect, SingleViewHeaderActions, SingleViewHeaderItem} from "../Common.tsx";
+import {RoleNameEnumType, UserAccountType} from "../../../models";
+import {Message} from "../../../components/application/Message.tsx";
+import {FormInput, FormSelect, SingleViewHeaderActions, SingleViewHeaderItem} from "../../Common.tsx";
 
 
 interface HeaderProps {

```

## コミット: cc4abcfa

### メッセージ

```
refactor:関数のインライン化
Refactor login and logout into a unified Auth component

Consolidated `Login` and `Logout` into a single `Auth` component for better maintainability and structure. Updated route configuration to reference the new `Auth` component. This improves code organization and eliminates redundancy.
```

### 変更されたファイル

- M	app/frontend/src/components/application/RouteConfig.tsx
- D	app/frontend/src/components/system/Logout.tsx

### 変更内容

```diff
commit cc4abcfaa93213eb50297dc150fd27d4986a5365
Author: k2works <kakimomokuri@gmail.com>
Date:   Mon Dec 23 19:00:10 2024 +0900

    refactor:関数のインライン化
    
    Refactor login and logout into a unified Auth component
    
    Consolidated `Login` and `Logout` into a single `Auth` component for better maintainability and structure. Updated route configuration to reference the new `Auth` component. This improves code organization and eliminates redundancy.

diff --git a/app/frontend/src/components/application/RouteConfig.tsx b/app/frontend/src/components/application/RouteConfig.tsx
index 496396c6..552154ba 100644
--- a/app/frontend/src/components/application/RouteConfig.tsx
+++ b/app/frontend/src/components/application/RouteConfig.tsx
@@ -1,12 +1,11 @@
 import {Route, Routes} from "react-router-dom";
 import React from "react";
 import {RouteAuthGuard} from "./RouteAuthGuard.tsx";
-import {Login} from "../system/Login.tsx";
-import {Logout} from "../system/Logout.tsx";
 import {User} from "../system/User.tsx";
 import {Home} from "../../views/application/Home.tsx";
 import {NotFound} from "../../views/application/NotFound.tsx";
 import {RoleType} from "../../models";
+import {Login, Logout} from "../system/Auth.tsx";
 
 export const RouteConfig: React.FC = () => {
     return (
diff --git a/app/frontend/src/components/system/Login.tsx b/app/frontend/src/components/system/Auth.tsx
similarity index 85%
rename from app/frontend/src/components/system/Login.tsx
rename to app/frontend/src/components/system/Auth.tsx
index e15169e7..e6ef3f6d 100644
--- a/app/frontend/src/components/system/Login.tsx
+++ b/app/frontend/src/components/system/Auth.tsx
@@ -56,3 +56,20 @@ export const Login: React.FC = () => {
         />
     );
 }
+
+export const Logout: React.FC = () => {
+    const authUser: AuthUserContextType = useAuthUserContext();
+    const navigate = useNavigate();
+
+    const handleSignOut = () => {
+        authUser.signOut(() => {
+            navigate("/login");
+        });
+    };
+
+    React.useEffect(() => {
+        handleSignOut();
+    }, []);
+
+    return null;
+};
diff --git a/app/frontend/src/components/system/Logout.tsx b/app/frontend/src/components/system/Logout.tsx
deleted file mode 100644
index f1d5e6bc..00000000
--- a/app/frontend/src/components/system/Logout.tsx
+++ /dev/null
@@ -1,20 +0,0 @@
-import {useNavigate} from "react-router-dom";
-import React from "react";
-import {AuthUserContextType, useAuthUserContext} from "../../providers/AuthUser.tsx";
-
-export const Logout: React.FC = () => {
-    const authUser: AuthUserContextType = useAuthUserContext();
-    const navigate = useNavigate();
-
-    const handleSignOut = () => {
-        authUser.signOut(() => {
-            navigate("/login");
-        });
-    };
-
-    React.useEffect(() => {
-        handleSignOut();
-    }, []);
-
-    return null;
-};

```

## コミット: ea7fbcaf

### メッセージ

```
refactor:関数の抽出
Refactor modal handling for clarity and modularity.

Reorganized modal logic into dedicated functions and returned methods to enhance readability and maintainability. This improves separation of concerns by isolating modal-related state and behavior, making the components easier to extend and debug.
```

### 変更されたファイル

- M	app/frontend/src/components/system/Audit.tsx

### 変更内容

```diff
commit ea7fbcaf432a7b662fde18b4869e530a3847521c
Author: k2works <kakimomokuri@gmail.com>
Date:   Mon Dec 23 18:43:42 2024 +0900

    refactor:関数の抽出
    
    Refactor modal handling for clarity and modularity.
    
    Reorganized modal logic into dedicated functions and returned methods to enhance readability and maintainability. This improves separation of concerns by isolating modal-related state and behavior, making the components easier to extend and debug.

diff --git a/app/frontend/src/components/system/Audit.tsx b/app/frontend/src/components/system/Audit.tsx
index 1d78d877..c408d137 100644
--- a/app/frontend/src/components/system/Audit.tsx
+++ b/app/frontend/src/components/system/Audit.tsx
@@ -41,100 +41,129 @@ export const Audit: React.FC = () => {
             });
         }, []);
 
-        const handleOpenModal = (audit?: AuditType) => {
-            setMessage("");
-            setError("");
-            if (audit) {
-                setNewAudit(audit);
-                setIsEditing(true);
-            } else {
-                setNewAudit(initialAudit);
-                setIsEditing(false);
-            }
-            setModalIsOpen(true);
-        };
 
-        const handleCloseModal = () => {
-            setError("");
-            setModalIsOpen(false);
-            setEditId(null);
-        };
+        const modalView = () => {
+            const editModal = () => {
+                const handleOpenModal = (audit?: AuditType) => {
+                    setMessage("");
+                    setError("");
+                    if (audit) {
+                        setNewAudit(audit);
+                        setIsEditing(true);
+                    } else {
+                        setNewAudit(initialAudit);
+                        setIsEditing(false);
+                    }
+                    setModalIsOpen(true);
+                };
 
-        const handleOpenSearchModal = () => {
-            setSearchModalIsOpen(true);
-        }
+                const handleCloseModal = () => {
+                    setError("");
+                    setModalIsOpen(false);
+                    setEditId(null);
+                };
 
-        const handleCloseSearchModal = () => {
-            setSearchModalIsOpen(false);
-        }
+                const editModalView = () => {
+                    return (
+                        <Modal
+                            isOpen={modalIsOpen}
+                            onRequestClose={handleCloseModal}
+                            contentLabel="アプリケーション実行履歴情報を入力"
+                            className="modal"
+                            overlayClassName="modal-overlay"
+                            bodyOpenClassName="modal-open"
+                        >
+                            {singleView()}
+                        </Modal>
+                    );
+                }
 
-        const modalView = () => {
-            const editModal = () => {
-                return (
-                    <Modal
-                        isOpen={modalIsOpen}
-                        onRequestClose={handleCloseModal}
-                        contentLabel="アプリケーション実行履歴情報を入力"
-                        className="modal"
-                        overlayClassName="modal-overlay"
-                        bodyOpenClassName="modal-open"
-                    >
-                        {singleView()}
-                    </Modal>
-                );
+                return {
+                    editModalView,
+                    handleOpenModal,
+                    handleCloseModal
+                }
             }
 
             const searchModal = () => {
-                return (
-                    <Modal
-                        isOpen={searchModalIsOpen}
-                        onRequestClose={handleCloseSearchModal}
-                        contentLabel="検索情報を入力"
-                        className="modal"
-                        overlayClassName="modal-overlay"
-                        bodyOpenClassName="modal-open"
-                    >
-                        {
-                            <AuditSearchSingleView
-                                condition={searchAuditCondition}
-                                setCondition={setSearchAuditCondition}
-                                handleSelect={async () => {
-                                    if (!searchAuditCondition) {
-                                        return;
-                                    }
-                                    setLoading(true);
-                                    try {
-                                        const fetchedAudit = await auditService.search(searchAuditCondition);
-                                        setAudits(fetchedAudit ? fetchedAudit.list : []);
-                                        if (fetchedAudit.list.length === 0) {
-                                            showErrorMessage(`検索結果は0件です`, setError);
-                                        } else {
-                                            setCondition(searchAuditCondition);
-                                            setPageNation(fetchedAudit);
-                                            setMessage("");
-                                            setError("");
+                const handleOpenSearchModal = () => {
+                    setSearchModalIsOpen(true);
+                }
+
+                const handleCloseSearchModal = () => {
+                    setSearchModalIsOpen(false);
+                }
+
+                const searchModalView = () => {
+                    return (
+                        <Modal
+                            isOpen={searchModalIsOpen}
+                            onRequestClose={handleCloseSearchModal}
+                            contentLabel="検索情報を入力"
+                            className="modal"
+                            overlayClassName="modal-overlay"
+                            bodyOpenClassName="modal-open"
+                        >
+                            {
+                                <AuditSearchSingleView
+                                    condition={searchAuditCondition}
+                                    setCondition={setSearchAuditCondition}
+                                    handleSelect={async () => {
+                                        if (!searchAuditCondition) {
+                                            return;
+                                        }
+                                        setLoading(true);
+                                        try {
+                                            const fetchedAudit = await auditService.search(searchAuditCondition);
+                                            setAudits(fetchedAudit ? fetchedAudit.list : []);
+                                            if (fetchedAudit.list.length === 0) {
+                                                showErrorMessage(`検索結果は0件です`, setError);
+                                            } else {
+                                                setCondition(searchAuditCondition);
+                                                setPageNation(fetchedAudit);
+                                                setMessage("");
+                                                setError("");
+                                            }
+                                        } catch (error: any) {
+                                            showErrorMessage(`実行履歴情報の検索に失敗しました: ${error?.message}`, setError);
+                                        } finally {
+                                            setLoading(false);
                                         }
-                                    } catch (error: any) {
-                                        showErrorMessage(`実行履歴情報の検索に失敗しました: ${error?.message}`, setError);
-                                    } finally {
-                                        setLoading(false);
-                                    }
-                                }}
-                                handleClose={handleCloseSearchModal}
-                            />
-                        }
-                    </Modal>
+                                    }}
+                                    handleClose={handleCloseSearchModal}
+                                />
+                            }
+                        </Modal>
+                    )
+                }
+
+                return {
+                    searchModalView,
+                    handleOpenSearchModal,
+                    handleCloseSearchModal
+                }
+            }
+
+            const init = () => {
+                return (
+                    <>
+                        {editModal().editModalView()}
+                        {searchModal().searchModalView()}
+                    </>
                 )
             }
-            return (
-                <>
-                    {searchModal()}
-                    {editModal()}
-                </>
-            );
+
+            return {
+                editModal,
+                searchModal,
+                init
+            }
         }
 
         const collectionView = () => {
+            const {handleOpenModal} = modalView().editModal();
+            const {handleOpenSearchModal} = modalView().searchModal();
+
             const handleCheckAudit = (audit: AuditType) => {
                 const newAudit = audits.map((d) => {
                     if (d.id === audit.id) {
@@ -216,6 +245,8 @@ export const Audit: React.FC = () => {
         };
 
         const singleView = () => {
+            const {handleCloseModal} = modalView().editModal();
+
             return (
                 <AuditSingleView
                     error={error}
@@ -234,7 +265,7 @@ export const Audit: React.FC = () => {
                     <LoadingIndicator/>
                 ) : (
                     <>
-                        {modalView()}
+                        {modalView().init()}
                         {collectionView()}
                     </>
                 )}

```

## コミット: 6881215a

### メッセージ

```
refactor:型定義
Refactor audit data handling with improved typing.

Updated audit service methods to return a unified `AuditFetchType`, improving type safety and clarity. Removed unused `initialSearchAuditCondition` and refined pagination logic in the `Audit` component.
```

### 変更されたファイル

- M	app/frontend/src/components/system/Audit.tsx
- M	app/frontend/src/models/audit.ts
- M	app/frontend/src/services/audit.ts

### 変更内容

```diff
commit 6881215a01c651de3706c929f578a7aa3e41f57a
Author: k2works <kakimomokuri@gmail.com>
Date:   Mon Dec 23 18:32:30 2024 +0900

    refactor:型定義
    
    Refactor audit data handling with improved typing.
    
    Updated audit service methods to return a unified `AuditFetchType`, improving type safety and clarity. Removed unused `initialSearchAuditCondition` and refined pagination logic in the `Audit` component.

diff --git a/app/frontend/src/components/system/Audit.tsx b/app/frontend/src/components/system/Audit.tsx
index ce6d1df7..1d78d877 100644
--- a/app/frontend/src/components/system/Audit.tsx
+++ b/app/frontend/src/components/system/Audit.tsx
@@ -19,7 +19,6 @@ export const Audit: React.FC = () => {
         const {modalIsOpen: searchModalIsOpen, setModalIsOpen: setSearchModalIsOpen,} = useModal();
         const {
             initialAudit,
-            initialSearchAuditCondition,
             audits,
             setAudits,
             newAudit,
@@ -111,7 +110,7 @@ export const Audit: React.FC = () => {
                                             showErrorMessage(`検索結果は0件です`, setError);
                                         } else {
                                             setCondition(searchAuditCondition);
-                                            setPageNation(fetchedAudit ? fetchedAudit: {});
+                                            setPageNation(fetchedAudit);
                                             setMessage("");
                                             setError("");
                                         }
diff --git a/app/frontend/src/models/audit.ts b/app/frontend/src/models/audit.ts
index 40029816..ed8e295c 100644
--- a/app/frontend/src/models/audit.ts
+++ b/app/frontend/src/models/audit.ts
@@ -1,4 +1,5 @@
 import {UserAccountType} from "./user.ts";
+import {PageNationType} from "../views/application/PageNation.tsx";
 
 export enum ApplicationExecutionProcessType {
     ユーザー登録 = "ユーザー登録",
@@ -53,6 +54,10 @@ export type AuditType = {
     checked: boolean;
 };
 
+export type AuditFetchType = {
+    list: AuditType[];
+} & PageNationType;
+
 export const mapToAuditSearchResource = (condition: SearchAuditConditionType) => {
     const isEmpty = (value: unknown) => value === "" || value === null || value === undefined;
     type Resource = {
diff --git a/app/frontend/src/services/audit.ts b/app/frontend/src/services/audit.ts
index 190b3dc5..6ce876d9 100644
--- a/app/frontend/src/services/audit.ts
+++ b/app/frontend/src/services/audit.ts
@@ -1,12 +1,12 @@
 import Config from "./config";
 import Utils from "./utils";
-import {AuditType, mapToAuditSearchResource, SearchAuditConditionType} from "../models/audit.ts";
+import {AuditFetchType, AuditType, mapToAuditSearchResource, SearchAuditConditionType} from "../models/audit.ts";
 
 export interface AuditServiceType {
-    select: (page?: number, pageSize?: number) => Promise<any>;
+    select: (page?: number, pageSize?: number) => Promise<AuditFetchType>;
     find: (id: number) => Promise<AuditType>;
     destroy: (id: number) => Promise<void>;
-    search: (condition: SearchAuditConditionType, page?: number, pageSize?: number) => Promise<any>;
+    search: (condition: SearchAuditConditionType, page?: number, pageSize?: number) => Promise<AuditFetchType>;
 }
 
 export const AuditService = (): AuditServiceType => {
@@ -21,7 +21,7 @@ export const AuditService = (): AuditServiceType => {
         return params.toString() ? `${baseUrl}?${params.toString()}` : baseUrl;
     };
 
-    const select = (page?: number, pageSize?: number): Promise<AuditType[]> => {
+    const select = (page?: number, pageSize?: number): Promise<AuditFetchType> => {
         const url = buildUrlWithPaging(endPoint, page, pageSize);
         return apiUtils.fetchGet(url);
     };
@@ -34,7 +34,7 @@ export const AuditService = (): AuditServiceType => {
         return apiUtils.fetchDelete(`${endPoint}/${id}`);
     };
 
-    const search = (condition: SearchAuditConditionType, page?: number, pageSize?: number): Promise<AuditType[]> => {
+    const search = (condition: SearchAuditConditionType, page?: number, pageSize?: number): Promise<AuditFetchType> => {
         const url = buildUrlWithPaging(`${endPoint}/search`, page, pageSize);
         return apiUtils.fetchPost(url, mapToAuditSearchResource(condition));
     };

```

## コミット: 001a70ef

### メッセージ

```
Merge branch 'take03' into develop
# Conflicts:
#	app/frontend/src/components/application/RouteConfig.tsx
#	app/frontend/src/views/application/Navigation.tsx
```

### 変更されたファイル


### 変更内容

```diff
commit 001a70efe0da70937b46b7da3fffe970e539e6fd
Merge: dca7b49c 6a79e1fd
Author: k2works <kakimomokuri@gmail.com>
Date:   Mon Dec 23 18:19:17 2024 +0900

    Merge branch 'take03' into develop
    
    # Conflicts:
    #       app/frontend/src/components/application/RouteConfig.tsx
    #       app/frontend/src/views/application/Navigation.tsx

diff --cc app/frontend/src/components/application/RouteConfig.tsx
index eb2587b0,bfd1be85..0384224f
--- a/app/frontend/src/components/application/RouteConfig.tsx
+++ b/app/frontend/src/components/application/RouteConfig.tsx
@@@ -12,9 -12,7 +12,9 @@@ import {Home} from "./Home.tsx"
  import {Product} from "../master/Product.tsx";
  import {SiteLayout} from "../../views/SiteLayout.tsx";
  import {ProductCategory} from "../master/ProductCategory.tsx";
- import {ProductDetail} from "../master/ProductDetail.tsx";
+ import {ProductItem} from "../master/ProductItem.tsx";
 +import {Audit} from "../system/Audit.tsx";
 +import {Download} from "../system/Download.tsx";
  
  export const RouteConfig: React.FC = () => {
      const ProductCategoryPage = () => {
diff --cc app/frontend/src/views/application/Navigation.tsx
index 5ed0acc5,170c43d5..d3524926
--- a/app/frontend/src/views/application/Navigation.tsx
+++ b/app/frontend/src/views/application/Navigation.tsx
@@@ -50,12 -47,10 +50,12 @@@ const NaveItems: React.FC = () => 
                              <SubNavItem id="side-nav-department-nav" to="/department">部門</SubNavItem>
                              <SubNavItem id="side-nav-employee-nav" to="/employee">社員</SubNavItem>
                              <SubNavItem id="side-nav-product-nav" to="/product">商品</SubNavItem>
 -                            <ul className="nav-sub-list">
 -                                <SubNavItem id="side-nav-product-nav" to="/product-category">分類</SubNavItem>
 -                                <SubNavItem id="side-nav-product-nav" to="/product-item">アイテム</SubNavItem>
 -                            </ul>
 +                            { !Env.isProduction() && (
 +                                <ul className="nav-sub-list">
 +                                    <SubNavItem id="side-nav-product-nav" to="/product-category">分類</SubNavItem>
-                                     <SubNavItem id="side-nav-product-nav" to="/product-detail">詳細</SubNavItem>
++                                    <SubNavItem id="side-nav-product-nav" to="/product-item">アイテム</SubNavItem>
 +                                </ul>
 +                            )}
                          </ul>
                      </li>
                  </>

```

## コミット: 6a79e1fd

### メッセージ

```
refactor:パッケージの整理
Moved master view files into logical subfolders (e.g., product, department, employee) to improve code organization and maintainability. Additionally, updated import paths in affected components to reflect the new directory structure.
```

### 変更されたファイル

- M	app/frontend/src/components/master/Department.tsx
- M	app/frontend/src/components/master/Employee.tsx
- M	app/frontend/src/components/master/ProductCategory.tsx
- M	app/frontend/src/components/master/ProductItem.tsx

### 変更内容

```diff
commit 6a79e1fd460ce363864efbedc847335d1ae3a4a8
Author: k2works <kakimomokuri@gmail.com>
Date:   Mon Dec 23 18:06:39 2024 +0900

    refactor:パッケージの整理
    
    Moved master view files into logical subfolders (e.g., product, department, employee) to improve code organization and maintainability. Additionally, updated import paths in affected components to reflect the new directory structure.

diff --git a/app/frontend/src/components/master/Department.tsx b/app/frontend/src/components/master/Department.tsx
index e35d5cce..22e05343 100644
--- a/app/frontend/src/components/master/Department.tsx
+++ b/app/frontend/src/components/master/Department.tsx
@@ -8,9 +8,9 @@ import Modal from "react-modal";
 import {usePageNation} from "../../views/application/PageNation.tsx";
 import {SiteLayout} from "../../views/SiteLayout.tsx";
 import LoadingIndicator from "../../views/application/LoadingIndicatior.tsx";
-import {EmployeeCollectionListView, EmployeeCollectionSelectView} from "../../views/master/EmployeeSelect.tsx";
-import {DepartmentCollectionView} from "../../views/master/DepartmentCollection.tsx";
-import {DepartmentSingleView} from "../../views/master/DepartmentSingle.tsx";
+import {EmployeeCollectionListView, EmployeeCollectionSelectView} from "../../views/master/employee/EmployeeSelect.tsx";
+import {DepartmentCollectionView} from "../../views/master/department/DepartmentCollection.tsx";
+import {DepartmentSingleView} from "../../views/master/department/DepartmentSingle.tsx";
 
 export const Department: React.FC = () => {
     const Content: React.FC = () => {
diff --git a/app/frontend/src/components/master/Employee.tsx b/app/frontend/src/components/master/Employee.tsx
index 2fe35d65..b43f9c15 100644
--- a/app/frontend/src/components/master/Employee.tsx
+++ b/app/frontend/src/components/master/Employee.tsx
@@ -9,10 +9,10 @@ import {usePageNation} from "../../views/application/PageNation.tsx";
 import {SiteLayout} from "../../views/SiteLayout.tsx";
 import LoadingIndicator from "../../views/application/LoadingIndicatior.tsx";
 import {useFetchUsers, useUser} from "../system/hooks.ts";
-import {DepartmentCollectionSelectView, DepartmentSelectView} from "../../views/master/DepartmentSelect.tsx";
+import {DepartmentCollectionSelectView, DepartmentSelectView} from "../../views/master/department/DepartmentSelect.tsx";
 import {UserCollectionSelectView, UserSelectView} from "../../views/system/UserSelect.tsx";
-import {EmployeeCollectionView} from "../../views/master/EmployeeCollection.tsx";
-import {EmployeeSingleView} from "../../views/master/EmployeeSingle.tsx";
+import {EmployeeCollectionView} from "../../views/master/employee/EmployeeCollection.tsx";
+import {EmployeeSingleView} from "../../views/master/employee/EmployeeSingle.tsx";
 
 export const Employee: React.FC = () => {
     const Content: React.FC = () => {
diff --git a/app/frontend/src/components/master/ProductCategory.tsx b/app/frontend/src/components/master/ProductCategory.tsx
index de116a46..b5eb5088 100644
--- a/app/frontend/src/components/master/ProductCategory.tsx
+++ b/app/frontend/src/components/master/ProductCategory.tsx
@@ -8,9 +8,9 @@ import {ProductCategoryType, ProductType} from "../../models";
 
 import {useFetchProductCategories, useFetchProducts, useProduct, useProductCategory} from "./hooks.ts";
 import Modal from "react-modal";
-import {ProductCategoryCollectionView} from "../../views/master/ProductCategoryCollection.tsx";
-import {ProductCategorySingleView} from "../../views/master/ProductCategorySingle.tsx";
-import {ProductCollectionListView, ProductCollectionSelectView} from "../../views/master/ProductSelect.tsx";
+import {ProductCategoryCollectionView} from "../../views/master/product/ProductCategoryCollection.tsx";
+import {ProductCategorySingleView} from "../../views/master/product/ProductCategorySingle.tsx";
+import {ProductCollectionListView, ProductCollectionSelectView} from "../../views/master/product/ProductSelect.tsx";
 
 export const ProductCategory: React.FC = () => {
     const Content: React.FC = () => {
diff --git a/app/frontend/src/components/master/ProductItem.tsx b/app/frontend/src/components/master/ProductItem.tsx
index c87eb421..eb2b2535 100644
--- a/app/frontend/src/components/master/ProductItem.tsx
+++ b/app/frontend/src/components/master/ProductItem.tsx
@@ -7,14 +7,14 @@ import {usePageNation} from "../../views/application/PageNation.tsx";
 import LoadingIndicator from "../../views/application/LoadingIndicatior.tsx";
 import {useFetchBoms, useFetchProducts, useFetchSubstitutes, useProduct} from "./hooks.ts";
 import {ProductType} from "../../models";
-import {ProductCollectionView} from "../../views/master/ProductCollection.tsx";
-import {ProductSingleView} from "../../views/master/ProductSingle.tsx";
-import {ProductCollectionSelectView} from "../../views/master/ProductSelect.tsx";
-import {SubstituteProductCollectionView} from "../../views/master/SubstituteProductCollection.tsx";
-import {BomCollectionView} from "../../views/master/BomCollection.tsx";
+import {ProductCollectionView} from "../../views/master/product/ProductCollection.tsx";
+import {ProductSingleView} from "../../views/master/product/ProductSingle.tsx";
+import {ProductCollectionSelectView} from "../../views/master/product/ProductSelect.tsx";
+import {SubstituteProductCollectionView} from "../../views/master/product/SubstituteProductCollection.tsx";
+import {BomCollectionView} from "../../views/master/product/BomCollection.tsx";
 import {
     CustomerSpecificSellingPriceCollectionView
-} from "../../views/master/CustomerSpecificSellingPriceCollection.tsx";
+} from "../../views/master/product/CustomerSpecificSellingPriceCollection.tsx";
 
 export const ProductItem: React.FC = () => {
     const Content: React.FC = () => {
diff --git a/app/frontend/src/views/master/DepartmentCollection.tsx b/app/frontend/src/views/master/department/DepartmentCollection.tsx
similarity index 97%
rename from app/frontend/src/views/master/DepartmentCollection.tsx
rename to app/frontend/src/views/master/department/DepartmentCollection.tsx
index 777ca092..0d0aa45a 100644
--- a/app/frontend/src/views/master/DepartmentCollection.tsx
+++ b/app/frontend/src/views/master/department/DepartmentCollection.tsx
@@ -1,7 +1,7 @@
 import React from "react";
-import {DepartmentIdType, DepartmentType} from "../../models";
-import {Message} from "../../components/application/Message.tsx";
-import {PageNation} from "../application/PageNation.tsx";
+import {DepartmentIdType, DepartmentType} from "../../../models";
+import {Message} from "../../../components/application/Message.tsx";
+import {PageNation} from "../../application/PageNation.tsx";
 
 interface SearchBarProps {
     searchValue: string;
diff --git a/app/frontend/src/views/master/DepartmentSelect.tsx b/app/frontend/src/views/master/department/DepartmentSelect.tsx
similarity index 96%
rename from app/frontend/src/views/master/DepartmentSelect.tsx
rename to app/frontend/src/views/master/department/DepartmentSelect.tsx
index 05254b63..2a82c4a4 100644
--- a/app/frontend/src/views/master/DepartmentSelect.tsx
+++ b/app/frontend/src/views/master/department/DepartmentSelect.tsx
@@ -1,8 +1,8 @@
 import React from 'react';
 import {FaTimes} from 'react-icons/fa';
-import {DepartmentType} from "../../models";
-import {convertToDateInputFormat} from "../../components/application/utils.ts";
-import {PageNation} from "../application/PageNation.tsx";
+import {DepartmentType} from "../../../models";
+import {convertToDateInputFormat} from "../../../components/application/utils.ts";
+import {PageNation} from "../../application/PageNation.tsx";
 
 interface DepartmentSelectProps {
     handleSelect: () => void;
diff --git a/app/frontend/src/views/master/DepartmentSingle.tsx b/app/frontend/src/views/master/department/DepartmentSingle.tsx
similarity index 97%
rename from app/frontend/src/views/master/DepartmentSingle.tsx
rename to app/frontend/src/views/master/department/DepartmentSingle.tsx
index ff6dfb1e..6427690f 100644
--- a/app/frontend/src/views/master/DepartmentSingle.tsx
+++ b/app/frontend/src/views/master/department/DepartmentSingle.tsx
@@ -1,8 +1,8 @@
 import React from 'react';
-import {Message} from "../../components/application/Message.tsx";
-import {DepartmentType, LowerType, SlitYnType} from "../../models";
-import {convertToDateInputFormat} from "../../components/application/utils.ts";
-import {FormInput, SingleViewHeaderActions, SingleViewHeaderItem} from "../Common.tsx";
+import {Message} from "../../../components/application/Message.tsx";
+import {DepartmentType, LowerType, SlitYnType} from "../../../models";
+import {convertToDateInputFormat} from "../../../components/application/utils.ts";
+import {FormInput, SingleViewHeaderActions, SingleViewHeaderItem} from "../../Common.tsx";
 
 interface HeaderProps {
     title: string;
diff --git a/app/frontend/src/views/master/EmployeeCollection.tsx b/app/frontend/src/views/master/employee/EmployeeCollection.tsx
similarity index 97%
rename from app/frontend/src/views/master/EmployeeCollection.tsx
rename to app/frontend/src/views/master/employee/EmployeeCollection.tsx
index b4efd6a2..617ff5a1 100644
--- a/app/frontend/src/views/master/EmployeeCollection.tsx
+++ b/app/frontend/src/views/master/employee/EmployeeCollection.tsx
@@ -1,7 +1,7 @@
 import React from "react";
-import {EmployeeType} from "../../models";
-import {Message} from "../../components/application/Message.tsx";
-import {PageNation} from "../application/PageNation.tsx";
+import {EmployeeType} from "../../../models";
+import {Message} from "../../../components/application/Message.tsx";
+import {PageNation} from "../../application/PageNation.tsx";
 
 interface SearchBarProps {
     searchValue: string;
diff --git a/app/frontend/src/views/master/EmployeeSelect.tsx b/app/frontend/src/views/master/employee/EmployeeSelect.tsx
similarity index 98%
rename from app/frontend/src/views/master/EmployeeSelect.tsx
rename to app/frontend/src/views/master/employee/EmployeeSelect.tsx
index b616820e..8ab4579a 100644
--- a/app/frontend/src/views/master/EmployeeSelect.tsx
+++ b/app/frontend/src/views/master/employee/EmployeeSelect.tsx
@@ -1,6 +1,6 @@
-import {EmployeeType} from "../../models";
+import {EmployeeType} from "../../../models";
 import React from "react";
-import {PageNation} from "../application/PageNation.tsx";
+import {PageNation} from "../../application/PageNation.tsx";
 import {FaTimes} from "react-icons/fa";
 
 interface EmployeeCollectionProps {
diff --git a/app/frontend/src/views/master/EmployeeSingle.tsx b/app/frontend/src/views/master/employee/EmployeeSingle.tsx
similarity index 97%
rename from app/frontend/src/views/master/EmployeeSingle.tsx
rename to app/frontend/src/views/master/employee/EmployeeSingle.tsx
index f7a5756d..e691e151 100644
--- a/app/frontend/src/views/master/EmployeeSingle.tsx
+++ b/app/frontend/src/views/master/employee/EmployeeSingle.tsx
@@ -1,8 +1,8 @@
-import {Message} from "../../components/application/Message.tsx";
+import {Message} from "../../../components/application/Message.tsx";
 import React from "react";
-import {convertToDateInputFormat} from "../../components/application/utils.ts";
-import {EmployeeType} from "../../models";
-import {FormInput, SingleViewHeaderActions, SingleViewHeaderItem} from "../Common.tsx";
+import {convertToDateInputFormat} from "../../../components/application/utils.ts";
+import {EmployeeType} from "../../../models";
+import {FormInput, SingleViewHeaderActions, SingleViewHeaderItem} from "../../Common.tsx";
 
 interface HeaderProps {
     title: string;
diff --git a/app/frontend/src/views/master/BomCollection.tsx b/app/frontend/src/views/master/product/BomCollection.tsx
similarity index 99%
rename from app/frontend/src/views/master/BomCollection.tsx
rename to app/frontend/src/views/master/product/BomCollection.tsx
index e6fa0410..50b27981 100644
--- a/app/frontend/src/views/master/BomCollection.tsx
+++ b/app/frontend/src/views/master/product/BomCollection.tsx
@@ -1,5 +1,5 @@
 import React, {useState} from "react";
-import {BomType} from "../../models";
+import {BomType} from "../../../models";
 
 interface BomCollectionProps {
     boms: BomType[];
diff --git a/app/frontend/src/views/master/CustomerSpecificSellingPriceCollection.tsx b/app/frontend/src/views/master/product/CustomerSpecificSellingPriceCollection.tsx
similarity index 98%
rename from app/frontend/src/views/master/CustomerSpecificSellingPriceCollection.tsx
rename to app/frontend/src/views/master/product/CustomerSpecificSellingPriceCollection.tsx
index 2abde901..33a1ccfd 100644
--- a/app/frontend/src/views/master/CustomerSpecificSellingPriceCollection.tsx
+++ b/app/frontend/src/views/master/product/CustomerSpecificSellingPriceCollection.tsx
@@ -1,5 +1,5 @@
 import React, {useState} from "react";
-import {CustomerSpecificSellingPriceType} from "../../models";
+import {CustomerSpecificSellingPriceType} from "../../../models";
 
 interface CustomerSpecificSellingPriceCollectionProps {
     prices: CustomerSpecificSellingPriceType[];
diff --git a/app/frontend/src/views/master/ProductCategoryCollection.tsx b/app/frontend/src/views/master/product/ProductCategoryCollection.tsx
similarity index 97%
rename from app/frontend/src/views/master/ProductCategoryCollection.tsx
rename to app/frontend/src/views/master/product/ProductCategoryCollection.tsx
index de91fa79..31c11244 100644
--- a/app/frontend/src/views/master/ProductCategoryCollection.tsx
+++ b/app/frontend/src/views/master/product/ProductCategoryCollection.tsx
@@ -1,7 +1,7 @@
 import React from "react";
-import {ProductCategoryType} from "../../models";
-import {Message} from "../../components/application/Message.tsx";
-import {PageNation} from "../application/PageNation.tsx";
+import {ProductCategoryType} from "../../../models";
+import {Message} from "../../../components/application/Message.tsx";
+import {PageNation} from "../../application/PageNation.tsx";
 
 interface SearchBarProps {
     searchValue: string;
diff --git a/app/frontend/src/views/master/ProductCategorySingle.tsx b/app/frontend/src/views/master/product/ProductCategorySingle.tsx
similarity index 97%
rename from app/frontend/src/views/master/ProductCategorySingle.tsx
rename to app/frontend/src/views/master/product/ProductCategorySingle.tsx
index 971d4015..b7f73c93 100644
--- a/app/frontend/src/views/master/ProductCategorySingle.tsx
+++ b/app/frontend/src/views/master/product/ProductCategorySingle.tsx
@@ -1,7 +1,7 @@
 import React from 'react';
-import {Message} from "../../components/application/Message.tsx";
-import {ProductCategoryType} from "../../models";
-import {FormInput, SingleViewHeaderActions, SingleViewHeaderItem} from "../Common.tsx";
+import {Message} from "../../../components/application/Message.tsx";
+import {ProductCategoryType} from "../../../models";
+import {FormInput, SingleViewHeaderActions, SingleViewHeaderItem} from "../../Common.tsx";
 
 interface HeaderProps {
     title: string;
diff --git a/app/frontend/src/views/master/ProductCollection.tsx b/app/frontend/src/views/master/product/ProductCollection.tsx
similarity index 97%
rename from app/frontend/src/views/master/ProductCollection.tsx
rename to app/frontend/src/views/master/product/ProductCollection.tsx
index 821831da..eda758de 100644
--- a/app/frontend/src/views/master/ProductCollection.tsx
+++ b/app/frontend/src/views/master/product/ProductCollection.tsx
@@ -1,7 +1,7 @@
 import React from 'react';
-import {Message} from "../../components/application/Message.tsx";
-import {PageNation} from "../application/PageNation.tsx";
-import {ProductType} from "../../models";
+import {Message} from "../../../components/application/Message.tsx";
+import {PageNation} from "../../application/PageNation.tsx";
+import {ProductType} from "../../../models";
 
 interface SearchBarProps {
     searchValue: string;
diff --git a/app/frontend/src/views/master/ProductSelect.tsx b/app/frontend/src/views/master/product/ProductSelect.tsx
similarity index 97%
rename from app/frontend/src/views/master/ProductSelect.tsx
rename to app/frontend/src/views/master/product/ProductSelect.tsx
index 70df279c..d51aea45 100644
--- a/app/frontend/src/views/master/ProductSelect.tsx
+++ b/app/frontend/src/views/master/product/ProductSelect.tsx
@@ -1,7 +1,7 @@
 import React from "react";
-import {ProductType} from "../../models"; // 正しいパスに修正してください
+import {ProductType} from "../../../models"; // 正しいパスに修正してください
 import {FaTimes} from "react-icons/fa";
-import {PageNation} from "../application/PageNation.tsx";
+import {PageNation} from "../../application/PageNation.tsx";
 
 interface ProductCollectionProps {
     products: ProductType[];
diff --git a/app/frontend/src/views/master/ProductSingle.tsx b/app/frontend/src/views/master/product/ProductSingle.tsx
similarity index 98%
rename from app/frontend/src/views/master/ProductSingle.tsx
rename to app/frontend/src/views/master/product/ProductSingle.tsx
index b44ff94f..352f2421 100644
--- a/app/frontend/src/views/master/ProductSingle.tsx
+++ b/app/frontend/src/views/master/product/ProductSingle.tsx
@@ -1,4 +1,4 @@
-import {FormInput, FormSelect, SingleViewHeaderActions, SingleViewHeaderItem} from "../Common.tsx";
+import {FormInput, FormSelect, SingleViewHeaderActions, SingleViewHeaderItem} from "../../Common.tsx";
 import {
     MiscellaneousEnumType,
     ProductEnumType,
@@ -6,9 +6,9 @@ import {
     StockAllocationEnumType,
     StockManagementTargetEnumType,
     TaxEnumType
-} from "../../models";
+} from "../../../models";
 import React from "react";
-import {Message} from "../../components/application/Message.tsx";
+import {Message} from "../../../components/application/Message.tsx";
 
 interface HeaderProps {
     title: string;
diff --git a/app/frontend/src/views/master/SubstituteProductCollection.tsx b/app/frontend/src/views/master/product/SubstituteProductCollection.tsx
similarity index 98%
rename from app/frontend/src/views/master/SubstituteProductCollection.tsx
rename to app/frontend/src/views/master/product/SubstituteProductCollection.tsx
index e4469b51..d3ea1388 100644
--- a/app/frontend/src/views/master/SubstituteProductCollection.tsx
+++ b/app/frontend/src/views/master/product/SubstituteProductCollection.tsx
@@ -1,5 +1,5 @@
 import React, {useState} from "react";
-import {SubstituteProductType} from "../../models";
+import {SubstituteProductType} from "../../../models";
 
 interface SubstituteProductCollectionProps {
     substituteProducts: SubstituteProductType[];

```

## コミット: 34853e1d

### メッセージ

```
refactor:型定義
Refactor fetch types for pagination handling.

Replaced generic `any` types with specific fetch types (e.g., `DepartmentFetchType`, `EmployeeFetchType`, etc.) across services and models to enhance type safety and support pagination. This ensures consistent typing for API responses and improves code maintainability.
```

### 変更されたファイル

- M	app/frontend/src/models/department.ts
- M	app/frontend/src/models/employee.ts
- M	app/frontend/src/models/product.ts
- M	app/frontend/src/services/department.ts
- M	app/frontend/src/services/employee.ts
- M	app/frontend/src/services/product.ts

### 変更内容

```diff
commit 34853e1d7b852b6bb58b291003211fe03d584161
Author: k2works <kakimomokuri@gmail.com>
Date:   Mon Dec 23 18:00:27 2024 +0900

    refactor:型定義
    
    Refactor fetch types for pagination handling.
    
    Replaced generic `any` types with specific fetch types (e.g., `DepartmentFetchType`, `EmployeeFetchType`, etc.) across services and models to enhance type safety and support pagination. This ensures consistent typing for API responses and improves code maintainability.

diff --git a/app/frontend/src/models/department.ts b/app/frontend/src/models/department.ts
index c310e228..7d045aaf 100644
--- a/app/frontend/src/models/department.ts
+++ b/app/frontend/src/models/department.ts
@@ -1,5 +1,6 @@
 import {EmployeeResourceType, EmployeeType} from './employee';
 import {toISOStringWithTimezone} from "../components/application/utils.ts";
+import {PageNationType} from "../views/application/PageNation.tsx";
 
 export type DepartmentIdType = {
     deptCode: { value: string };
@@ -18,6 +19,10 @@ export type DepartmentType = {
     checked: boolean;
 }
 
+export type DepartmentFetchType = {
+    list: DepartmentType[];
+} & PageNationType;
+
 export type DepartmentResourceType = {
     departmentCode: string;
     startDate: string;
diff --git a/app/frontend/src/models/employee.ts b/app/frontend/src/models/employee.ts
index e4662174..365a34b8 100644
--- a/app/frontend/src/models/employee.ts
+++ b/app/frontend/src/models/employee.ts
@@ -1,6 +1,7 @@
 import {DepartmentType} from './department';
 import {UserAccountType} from './user';
 import {toISOStringWithTimezone} from "../components/application/utils.ts";
+import {PageNationType} from "../views/application/PageNation.tsx";
 
 export type EmployeeType = {
     empCode: { value: string };
@@ -34,6 +35,10 @@ export type EmployeeType = {
     checked: boolean;
 }
 
+export type EmployeeFetchType = {
+    list: EmployeeType[];
+} & PageNationType;
+
 export type EmployeeResourceType = {
     empCode: string;
     empName: string;
diff --git a/app/frontend/src/models/product.ts b/app/frontend/src/models/product.ts
index 472bd695..442fece9 100644
--- a/app/frontend/src/models/product.ts
+++ b/app/frontend/src/models/product.ts
@@ -1,3 +1,5 @@
+import {PageNationType} from "../views/application/PageNation.tsx";
+
 type ProductCodeType = {
     value: string;
     businessType: string;
@@ -39,6 +41,10 @@ export type BomType = {
     componentQuantity: QuantityType;
 }
 
+export type BomFetchType = {
+    list: BomType[];
+} & PageNationType;
+
 export type CustomerSpecificSellingPriceType = {
     productCode: ProductCodeType;
     customerCode: string;
@@ -86,6 +92,10 @@ export enum StockAllocationEnumType {
     未引当 = "未引当", 引当済 = "引当済"
 }
 
+export type ProductFetchType = {
+    list: ProductType[];
+} & PageNationType;
+
 export type ProductResourceType = {
     productCode: string;
     productFormalName: string;
@@ -123,6 +133,10 @@ export type ProductCategoryType = {
     checked: boolean;
 };
 
+export type ProductCategoryFetchType = {
+    list: ProductCategoryType[];
+} & PageNationType;
+
 export type ProductCategoryResourceType = {
     productCategoryCode: string;
     productCategoryName?: string;
diff --git a/app/frontend/src/services/department.ts b/app/frontend/src/services/department.ts
index d0c22326..f22cdf9b 100644
--- a/app/frontend/src/services/department.ts
+++ b/app/frontend/src/services/department.ts
@@ -1,10 +1,10 @@
 import Config from "./config";
 import Utils from "./utils";
-import {DepartmentType, mapToDepartmentResource} from "../models";
+import {DepartmentFetchType, DepartmentType, mapToDepartmentResource} from "../models";
 import {toISOStringWithTimezone} from "../components/application/utils.ts";
 
 export interface DepartmentServiceType {
-    select: (page?: number, pageSize?: number) => Promise<any>;
+    select: (page?: number, pageSize?: number) => Promise<DepartmentFetchType>;
     find: (deptCode: string, departmentStartDate: string) => Promise<DepartmentType[]>;
     create: (department: DepartmentType) => Promise<void>;
     update: (department: DepartmentType) => Promise<void>;
@@ -16,7 +16,7 @@ export const DepartmentService = () => {
     const apiUtils = Utils.apiUtils;
     const endPoint = `${config.apiUrl}/departments`;
 
-    const select = async (page?: number, pageSize?: number): Promise<any> => {
+    const select = async (page?: number, pageSize?: number): Promise<DepartmentFetchType> => {
         let url = endPoint;
         if (pageSize && page) {
             url = url + "?pageSize=" + pageSize + "&page=" + page;
diff --git a/app/frontend/src/services/employee.ts b/app/frontend/src/services/employee.ts
index 719de84d..5f98037f 100644
--- a/app/frontend/src/services/employee.ts
+++ b/app/frontend/src/services/employee.ts
@@ -1,9 +1,9 @@
 import Config from "./config";
 import Utils from "./utils";
-import {EmployeeType, mapToEmployeeResource} from "../models";
+import {EmployeeFetchType, EmployeeType, mapToEmployeeResource} from "../models";
 
 export interface EmployeeServiceType {
-    select: (page?: number, pageSize?: number) => Promise<any>;
+    select: (page?: number, pageSize?: number) => Promise<EmployeeFetchType>;
     find: (empCode: string) => Promise<EmployeeType>;
     create: (employee: EmployeeType) => Promise<void>;
     update: (employee: EmployeeType) => Promise<void>;
@@ -16,7 +16,7 @@ export const EmployeeService = () => {
     const endPoint = `${config.apiUrl}/employees`;
 
 
-    const select = async (page?: number, pageSize?: number): Promise<any> => {
+    const select = async (page?: number, pageSize?: number): Promise<EmployeeFetchType> => {
         let url = endPoint;
         if (pageSize && page) {
             url = url + "?pageSize=" + pageSize + "&page=" + page;
diff --git a/app/frontend/src/services/product.ts b/app/frontend/src/services/product.ts
index ad7a0451..4f10c0b0 100644
--- a/app/frontend/src/services/product.ts
+++ b/app/frontend/src/services/product.ts
@@ -1,10 +1,10 @@
 import Config from "./config";
 import Utils from "./utils";
-import {mapToProductResource, ProductType} from "../models";
+import {BomFetchType, mapToProductResource, ProductFetchType, ProductType} from "../models";
 
 export interface ProductServiceType {
-    select: (page?: number, pageSize?: number) => Promise<any>;
-    selectBoms: (page?: number, pageSize?: number) => Promise<any>;
+    select: (page?: number, pageSize?: number) => Promise<ProductFetchType>;
+    selectBoms: (page?: number, pageSize?: number) => Promise<BomFetchType>;
     find: (productCode: string) => Promise<ProductType>;
     create: (product: ProductType) => Promise<void>;
     update: (product: ProductType) => Promise<void>;
@@ -17,7 +17,7 @@ export const ProductService = (): ProductServiceType => {
     const apiUtils = Utils.apiUtils;
     const endPoint = `${config.apiUrl}/products`;
 
-    const select = async (page?: number, pageSize?: number): Promise<any> => {
+    const select = async (page?: number, pageSize?: number): Promise<ProductFetchType> => {
         let url = endPoint;
         if (pageSize && page) {
             url = url + "?pageSize=" + pageSize + "&page=" + page;
@@ -29,7 +29,7 @@ export const ProductService = (): ProductServiceType => {
         return await apiUtils.fetchGet(url);
     };
 
-    const selectBoms = async (page?: number, pageSize?: number): Promise<any> => {
+    const selectBoms = async (page?: number, pageSize?: number): Promise<BomFetchType> => {
         let url = endPoint + "/boms";
         if (pageSize && page) {
             url = url + "?pageSize=" + pageSize + "&page=" + page;

```

## コミット: e48a6330

### メッセージ

```
Merge branch 'take02' into take03
# Conflicts:
#	app/frontend/src/models/user.ts
```

### 変更されたファイル


### 変更内容

```diff
commit e48a633008c8075dcd9719d00ba894ee174fef10
Merge: 671962e5 0793b7d1
Author: k2works <kakimomokuri@gmail.com>
Date:   Mon Dec 23 17:33:34 2024 +0900

    Merge branch 'take02' into take03
    
    # Conflicts:
    #       app/frontend/src/models/user.ts

diff --cc app/frontend/src/models/user.ts
index fea79755,47db03f4..1d3e05f3
--- a/app/frontend/src/models/user.ts
+++ b/app/frontend/src/models/user.ts
@@@ -23,11 -25,10 +25,15 @@@ export type UserAccountType = 
      roleName?: string;
  }
  
 +export enum RoleNameEnumType {
 +    ROLE_ADMIN = 'ADMIN',
 +    ROLE_USER = 'USER'
 +}
 +
+ export type UserFetchType = {
+     list: UserAccountType[];
+ } & PageNationType;
+ 
  export type UserAccountResourceType = {
      userId: string;
      password: string | undefined;
diff --cc app/frontend/src/views/system/UserCollection.tsx
index 6fb6915d,f0433208..f320f259
--- a/app/frontend/src/views/system/UserCollection.tsx
+++ b/app/frontend/src/views/system/UserCollection.tsx
@@@ -1,15 -1,15 +1,15 @@@
  import React from "react";
 +import {UserAccountType} from "../../models";
  import {PageNation, PageNationType} from "../application/PageNation.tsx";
  import {Message} from "../../components/application/Message.tsx";
 -import {UserAccountType} from "../../models";
  
- interface SearchInputProps {
-     searchUserId: string;
-     setSearchUserId: (value: string) => void;
-     handleSearchUser: () => void;
+ interface SearchBarProps {
+     searchValue: string;
+     onSearchChange: (e: React.ChangeEvent<HTMLInputElement>) => void;
+     onSearchClick: () => void;
  }
  
- const SearchInput: React.FC<SearchInputProps> = ({searchUserId, setSearchUserId, handleSearchUser}) => {
+ const SearchBar: React.FC<SearchBarProps> = ({searchValue, onSearchChange, onSearchClick}) => {
      return (
          <div className="search-container">
              <input

```

## コミット: 0793b7d1

### メッセージ

```
refactor:型宣言
Refactor UserService and add UserFetchType to models

Updated UserService to define more specific return types, ensuring better type safety and clarity. Introduced `UserFetchType` in models to standardize paginated user data handling. Adjusted type references and imports where necessary.
```

### 変更されたファイル

- M	app/frontend/src/models/user.ts
- M	app/frontend/src/services/user.ts

### 変更内容

```diff
commit 0793b7d1d5d9d4e23733ab4b7e60718e87891ac0
Author: k2works <kakimomokuri@gmail.com>
Date:   Mon Dec 23 17:23:51 2024 +0900

    refactor:型宣言
    
    Refactor UserService and add UserFetchType to models
    
    Updated UserService to define more specific return types, ensuring better type safety and clarity. Introduced `UserFetchType` in models to standardize paginated user data handling. Adjusted type references and imports where necessary.

diff --git a/app/frontend/src/models/user.ts b/app/frontend/src/models/user.ts
index bdf00f94..47db03f4 100644
--- a/app/frontend/src/models/user.ts
+++ b/app/frontend/src/models/user.ts
@@ -1,3 +1,5 @@
+import {PageNationType} from "../views/application/PageNation.tsx";
+
 export type UserType = {
     userId: string;
     token: string;
@@ -23,6 +25,10 @@ export type UserAccountType = {
     roleName?: string;
 }
 
+export type UserFetchType = {
+    list: UserAccountType[];
+} & PageNationType;
+
 export type UserAccountResourceType = {
     userId: string;
     password: string | undefined;
diff --git a/app/frontend/src/services/user.ts b/app/frontend/src/services/user.ts
index 69b41854..6f08813b 100644
--- a/app/frontend/src/services/user.ts
+++ b/app/frontend/src/services/user.ts
@@ -1,14 +1,14 @@
 import Config from "./config.ts";
 import Utils from "./utils.ts";
-import {mapToUserAccountResource, UserAccountType} from "../models";
+import {mapToUserAccountResource, UserAccountType, UserFetchType} from "../models";
 
 export interface UserServiceType {
-    select: (page?: number, pageSize?: number) => Promise<any>;
-    find: (userId: String) => Promise<any>;
-    create: (user: UserAccountType) => Promise<any>;
-    update: (user: UserAccountType) => Promise<any>;
-    destroy: (userId: String) => Promise<any>;
-    search: (pageSize: number, code: string, page: number) => Promise<any>;
+    select: (page?: number, pageSize?: number) => Promise<UserFetchType>;
+    find: (userId: string) => Promise<UserAccountType>;
+    create: (user: UserAccountType) => Promise<string>;
+    update: (user: UserAccountType) => Promise<string>;
+    destroy: (userId: string) => Promise<string>;
+    search: (pageSize: number, code: string, page: number) => Promise<UserFetchType>;
 }
 export const UserService = () => {
     const config = Config();
@@ -28,7 +28,7 @@ export const UserService = () => {
         return await apiUtils.fetchGet(url);
     };
 
-    const find = async (userId: String) => {
+    const find = async (userId: string) => {
         const url = `${endPoint}/${userId}`;
         return await apiUtils.fetchGet(url);
     };
@@ -43,12 +43,12 @@ export const UserService = () => {
     };
 
     const search = async (pageSize = 10, code: string, page = 1): Promise<any> => {
-        let url = `${endPoint}/search?pageSize=${pageSize}&code=${code}&page=${page}`;
+        const url = `${endPoint}/search?pageSize=${pageSize}&code=${code}&page=${page}`;
 
         return await apiUtils.fetchGet(url);
     };
 
-    const destroy = async (userId: String) => {
+    const destroy = async (userId: string) => {
         const url = `${endPoint}/${userId}`;
         return await apiUtils.fetchDelete(url);
     };

```

## コミット: f3a6b601

### メッセージ

```
refactor:関数宣言の変更
Refactor User component to improve props structure.

Grouped related props into cohesive objects for better readability and maintainability. Renamed `SearchInput` to `SearchBar` and updated its props for consistency. These changes enhance code modularity and simplify component prop management.
```

### 変更されたファイル

- M	app/frontend/src/components/system/User.tsx
- M	app/frontend/src/views/system/UserCollection.tsx

### 変更内容

```diff
commit f3a6b6014ac241ec080ba0d775d105859659f713
Author: k2works <kakimomokuri@gmail.com>
Date:   Mon Dec 23 17:13:05 2024 +0900

    refactor:関数宣言の変更
    
    Refactor User component to improve props structure.
    
    Grouped related props into cohesive objects for better readability and maintainability. Renamed `SearchInput` to `SearchBar` and updated its props for consistency. These changes enhance code modularity and simplify component prop management.

diff --git a/app/frontend/src/components/system/User.tsx b/app/frontend/src/components/system/User.tsx
index b440f273..126aba09 100644
--- a/app/frontend/src/components/system/User.tsx
+++ b/app/frontend/src/components/system/User.tsx
@@ -134,14 +134,10 @@ export const User: React.FC = () => {
                     <UserCollectionView
                         error={error}
                         message={message}
-                        searchUserId={searchUserId}
-                        setSearchUserId={setSearchUserId}
-                        handleSearchUser={handleSearchUser}
-                        handleOpenModal={handleOpenModal}
-                        users={users}
-                        handleDeleteUser={handleDeleteUser}
-                        fetchUsers={fetchUsers.load}
-                        pageNation={pageNation}
+                        searchItems={{ searchUserId, setSearchUserId, handleSearchUser }}
+                        headerItems={{ handleOpenModal }}
+                        collectionItems={{ users, handleDeleteUser }}
+                        pageNationItems={{ pageNation, fetchUsers: fetchUsers.load }}
                     />
                 </>
             )
diff --git a/app/frontend/src/views/system/UserCollection.tsx b/app/frontend/src/views/system/UserCollection.tsx
index d1a5e65b..f0433208 100644
--- a/app/frontend/src/views/system/UserCollection.tsx
+++ b/app/frontend/src/views/system/UserCollection.tsx
@@ -3,25 +3,23 @@ import {PageNation, PageNationType} from "../application/PageNation.tsx";
 import {Message} from "../../components/application/Message.tsx";
 import {UserAccountType} from "../../models";
 
-interface SearchInputProps {
-    searchUserId: string;
-    setSearchUserId: (value: string) => void;
-    handleSearchUser: () => void;
+interface SearchBarProps {
+    searchValue: string;
+    onSearchChange: (e: React.ChangeEvent<HTMLInputElement>) => void;
+    onSearchClick: () => void;
 }
 
-const SearchInput: React.FC<SearchInputProps> = ({searchUserId, setSearchUserId, handleSearchUser}) => {
+const SearchBar: React.FC<SearchBarProps> = ({searchValue, onSearchChange, onSearchClick}) => {
     return (
         <div className="search-container">
             <input
-                type="text"
                 id="search-input"
+                type="text"
                 placeholder="ユーザーIDで検索"
-                value={searchUserId}
-                onChange={(e) => setSearchUserId(e.target.value)}
+                value={searchValue}
+                onChange={onSearchChange}
             />
-            <button className="action-button" id="search-all" onClick={handleSearchUser}>
-                検索
-            </button>
+            <button className="action-button" id="search-all" onClick={onSearchClick}>検索</button>
         </div>
     );
 };
@@ -88,27 +86,31 @@ const UserList: React.FC<UserListProps> = ({users, handleOpenModal, handleDelete
 interface UserCollectionViewProps {
     error: string | null;
     message: string | null;
-    searchUserId: string;
-    setSearchUserId: (value: string) => void;
-    handleSearchUser: () => void;
-    handleOpenModal: (user?: UserAccountType) => void;
-    users: UserAccountType[];
-    handleDeleteUser: (userId: string) => void;
-    fetchUsers: () => void;
-    pageNation: PageNationType | null;
+    searchItems: {
+        searchUserId: string;
+        setSearchUserId: (value: string) => void;
+        handleSearchUser: () => void;
+    }
+    headerItems: {
+        handleOpenModal: (user?: UserAccountType) => void;
+    }
+    collectionItems: {
+        users: UserAccountType[];
+        handleDeleteUser: (userId: string) => void;
+    }
+    pageNationItems: {
+        pageNation: PageNationType | null;
+        fetchUsers: (page: number) => void;
+    }
 }
 
 export const UserCollectionView = ({
                                        error,
                                        message,
-                                       searchUserId,
-                                       setSearchUserId,
-                                       handleSearchUser,
-                                       handleOpenModal,
-                                       users,
-                                       handleDeleteUser,
-                                       fetchUsers,
-                                       pageNation
+                                       searchItems: {searchUserId, setSearchUserId, handleSearchUser,},
+                                       headerItems: {handleOpenModal},
+                                       collectionItems: {users, handleDeleteUser},
+                                       pageNationItems: {pageNation, fetchUsers}
                                    }: UserCollectionViewProps) => (
     <div className="collection-view-object-container">
         <Message error={error} message={message}/>
@@ -119,8 +121,11 @@ export const UserCollectionView = ({
                 </div>
             </div>
             <div className="collection-view-content">
-                <SearchInput searchUserId={searchUserId} setSearchUserId={setSearchUserId}
-                             handleSearchUser={handleSearchUser}/>
+                <SearchBar
+                    searchValue={searchUserId}
+                    onSearchChange={(e) => setSearchUserId(e.target.value)}
+                    onSearchClick={handleSearchUser}
+                />
                 <div className="button-container">
                     <button className="action-button" onClick={() => handleOpenModal()} id="new">
                         新規

```

## コミット: 8dd83c16

### メッセージ

```
refactor:関数の抽出
Refactor modal handling into a modular structure

Reorganized modal logic into a distinct and reusable structure within `modalView`. This enhances code readability, promotes maintainability, and separates modal concerns from main component logic. Integrations with `collectionView` and `singleView` were updated accordingly.
```

### 変更されたファイル

- M	app/frontend/src/components/system/User.tsx

### 変更内容

```diff
commit 8dd83c1682f427d1a9743a06f74a99a6cbf0b606
Author: k2works <kakimomokuri@gmail.com>
Date:   Mon Dec 23 16:58:34 2024 +0900

    refactor:関数の抽出
    
    Refactor modal handling into a modular structure
    
    Reorganized modal logic into a distinct and reusable structure within `modalView`. This enhances code readability, promotes maintainability, and separates modal concerns from main component logic. Integrations with `collectionView` and `singleView` were updated accordingly.

diff --git a/app/frontend/src/components/system/User.tsx b/app/frontend/src/components/system/User.tsx
index b5ee8ab5..b440f273 100644
--- a/app/frontend/src/components/system/User.tsx
+++ b/app/frontend/src/components/system/User.tsx
@@ -40,28 +40,67 @@ export const User: React.FC = () => {
             });
         }, []);
 
-        const handleOpenModal = (user?: UserAccountType) => {
-            setMessage("");
-            setError("");
-            if (user) {
-                user.password = {value: ""};
-                setNewUser(user);
-                setEditId(user.userId.value);
-                setIsEditing(true);
-            } else {
-                setNewUser(initialUser);
-                setIsEditing(false);
+        const modalView = () => {
+            const editModal = () => {
+                const handleOpenModal = (user?: UserAccountType) => {
+                    setMessage("");
+                    setError("");
+                    if (user) {
+                        user.password = {value: ""};
+                        setNewUser(user);
+                        setEditId(user.userId.value);
+                        setIsEditing(true);
+                    } else {
+                        setNewUser(initialUser);
+                        setIsEditing(false);
+                    }
+                    setModalIsOpen(true);
+                };
+
+                const handleCloseModal = () => {
+                    setError("");
+                    setModalIsOpen(false);
+                    setEditId(null);
+                };
+
+                const editModalView = () => {
+                    return (
+                        <Modal
+                            isOpen={modalIsOpen}
+                            onRequestClose={handleCloseModal}
+                            contentLabel="ユーザー情報を入力"
+                            className="modal"
+                            overlayClassName="modal-overlay"
+                            bodyOpenClassName="modal-open"
+                        >
+                            {singleView()}
+                        </Modal>
+
+                    )
+                }
+
+                return {
+                    handleOpenModal,
+                    handleCloseModal,
+                    editModalView
+                }
             }
-            setModalIsOpen(true);
-        };
 
-        const handleCloseModal = () => {
-            setError("");
-            setModalIsOpen(false);
-            setEditId(null);
-        };
+            const init = () => (
+                <>
+                    {editModal().editModalView()}
+                </>
+            )
+
+            return{
+                editModal,
+                init
+            }
+        }
 
         const collectionView = () => {
+            const {handleOpenModal} = modalView().editModal();
+
             const handleSearchUser = async () => {
                 if (!searchUserId.trim()) {
                     return;
@@ -92,17 +131,6 @@ export const User: React.FC = () => {
 
             return (
                 <>
-                    <Modal
-                        isOpen={modalIsOpen}
-                        onRequestClose={handleCloseModal}
-                        contentLabel="ユーザー情報を入力"
-                        className="modal"
-                        overlayClassName="modal-overlay"
-                        bodyOpenClassName="modal-open"
-                    >
-                        {singleView()}
-                    </Modal>
-
                     <UserCollectionView
                         error={error}
                         message={message}
@@ -120,6 +148,8 @@ export const User: React.FC = () => {
         }
 
         const singleView = () => {
+            const  {handleCloseModal} = modalView().editModal();
+
             const handleCreateOrUpdateUser = async () => {
                 const validateUser = (): boolean => {
                     if (!newUser.userId.value.trim() || !newUser.name?.firstName?.trim() || !newUser.name?.lastName?.trim() || !newUser.roleName?.trim()) {
@@ -168,7 +198,10 @@ export const User: React.FC = () => {
                 {loading ? (
                     <LoadingIndicator/>
                 ) : (
-                    collectionView()
+                    <>
+                        {modalView().init()}
+                        {collectionView()}
+                    </>
                 )}
             </>
         );

```

## コミット: 671962e5

### メッセージ

```
refactor:関数名の変更
Rename ProductDetail to ProductItem across the codebase

This change updates the component name from `ProductDetail` to `ProductItem` for greater clarity and alignment with its purpose. All references, including imports, routes, navigation, and test files, have been updated accordingly.
```

### 変更されたファイル

- M	app/frontend/src/components/application/RouteConfig.tsx
- M	app/frontend/src/components/master/Product.tsx
- M	app/frontend/src/views/application/Navigation.tsx
- M	app/frontend/src/views/master/ProductCollection.tsx

### 変更内容

```diff
commit 671962e59df007c545126efcaec6caa034fc1bc1
Author: k2works <kakimomokuri@gmail.com>
Date:   Mon Dec 23 16:43:03 2024 +0900

    refactor:関数名の変更
    
    Rename ProductDetail to ProductItem across the codebase
    
    This change updates the component name from `ProductDetail` to `ProductItem` for greater clarity and alignment with its purpose. All references, including imports, routes, navigation, and test files, have been updated accordingly.

diff --git a/app/frontend/cypress/e2e/master/ProductDetail.cy.js b/app/frontend/cypress/e2e/master/ProductItem.cy.js
similarity index 100%
rename from app/frontend/cypress/e2e/master/ProductDetail.cy.js
rename to app/frontend/cypress/e2e/master/ProductItem.cy.js
diff --git a/app/frontend/src/components/application/RouteConfig.tsx b/app/frontend/src/components/application/RouteConfig.tsx
index b354340e..bfd1be85 100644
--- a/app/frontend/src/components/application/RouteConfig.tsx
+++ b/app/frontend/src/components/application/RouteConfig.tsx
@@ -12,7 +12,7 @@ import {Home} from "./Home.tsx";
 import {Product} from "../master/Product.tsx";
 import {SiteLayout} from "../../views/SiteLayout.tsx";
 import {ProductCategory} from "../master/ProductCategory.tsx";
-import {ProductDetail} from "../master/ProductDetail.tsx";
+import {ProductItem} from "../master/ProductItem.tsx";
 
 export const RouteConfig: React.FC = () => {
     const ProductCategoryPage = () => {
@@ -26,7 +26,7 @@ export const RouteConfig: React.FC = () => {
     const ProductDetailPage = () => {
         return (
             <SiteLayout>
-                <ProductDetail/>
+                <ProductItem/>
             </SiteLayout>
         );
     }
@@ -45,7 +45,7 @@ export const RouteConfig: React.FC = () => {
                                                                 allowedRoles={[RoleType.ADMIN]}/>}/>
                 <Route path="/product-category" element={<RouteAuthGuard component={<ProductCategoryPage/>} redirectPath="/"
                                                                 allowedRoles={[RoleType.ADMIN]}/>}/>
-                <Route path="/product-detail" element={<RouteAuthGuard component={<ProductDetailPage/>} redirectPath="/"
+                <Route path="/product-item" element={<RouteAuthGuard component={<ProductDetailPage/>} redirectPath="/"
                                                                 allowedRoles={[RoleType.ADMIN]}/>}/>
                 <Route path="/login" element={<Login/>}/>
                 <Route path="/logout" element={<Logout/>}/>
diff --git a/app/frontend/src/components/master/Product.tsx b/app/frontend/src/components/master/Product.tsx
index 01d61f49..75480dd1 100644
--- a/app/frontend/src/components/master/Product.tsx
+++ b/app/frontend/src/components/master/Product.tsx
@@ -1,7 +1,7 @@
 import React from "react";
 import {useTab} from "../application/hooks.ts";
 import {ProductCategory} from "./ProductCategory.tsx";
-import {ProductDetail} from "./ProductDetail.tsx";
+import {ProductItem} from "./ProductItem.tsx";
 import {SiteLayout} from "../../views/SiteLayout.tsx";
 
 export const Product: React.FC = () => {
@@ -17,13 +17,13 @@ export const Product: React.FC = () => {
             <Tabs>
                 <TabList>
                     <Tab>分類</Tab>
-                    <Tab>詳細</Tab>
+                    <Tab>アイテム</Tab>
                 </TabList>
                 <TabPanel>
                     <ProductCategory/>
                 </TabPanel>
                 <TabPanel>
-                    <ProductDetail/>
+                    <ProductItem/>
                 </TabPanel>
             </Tabs>
         );
diff --git a/app/frontend/src/components/master/ProductDetail.tsx b/app/frontend/src/components/master/ProductItem.tsx
similarity index 99%
rename from app/frontend/src/components/master/ProductDetail.tsx
rename to app/frontend/src/components/master/ProductItem.tsx
index 2fab2b09..c87eb421 100644
--- a/app/frontend/src/components/master/ProductDetail.tsx
+++ b/app/frontend/src/components/master/ProductItem.tsx
@@ -16,7 +16,7 @@ import {
     CustomerSpecificSellingPriceCollectionView
 } from "../../views/master/CustomerSpecificSellingPriceCollection.tsx";
 
-export const ProductDetail: React.FC = () => {
+export const ProductItem: React.FC = () => {
     const Content: React.FC = () => {
         const [loading, setLoading] = useState<boolean>(false);
         const {message, setMessage, error, setError} = useMessage();
diff --git a/app/frontend/src/views/application/Navigation.tsx b/app/frontend/src/views/application/Navigation.tsx
index e4171552..170c43d5 100644
--- a/app/frontend/src/views/application/Navigation.tsx
+++ b/app/frontend/src/views/application/Navigation.tsx
@@ -49,7 +49,7 @@ const NaveItems: React.FC = () => {
                             <SubNavItem id="side-nav-product-nav" to="/product">商品</SubNavItem>
                             <ul className="nav-sub-list">
                                 <SubNavItem id="side-nav-product-nav" to="/product-category">分類</SubNavItem>
-                                <SubNavItem id="side-nav-product-nav" to="/product-detail">詳細</SubNavItem>
+                                <SubNavItem id="side-nav-product-nav" to="/product-item">アイテム</SubNavItem>
                             </ul>
                         </ul>
                     </li>
diff --git a/app/frontend/src/views/master/ProductCollection.tsx b/app/frontend/src/views/master/ProductCollection.tsx
index 76ea948a..821831da 100644
--- a/app/frontend/src/views/master/ProductCollection.tsx
+++ b/app/frontend/src/views/master/ProductCollection.tsx
@@ -133,7 +133,7 @@ export const ProductCollectionView: React.FC<ProductCollectionViewProps> = ({
         <div className="collection-view-container">
             <div className="collection-view-header">
                 <div className="single-view-header-item">
-                    <h1 className="single-view-title">商品</h1>
+                    <h1 className="single-view-title">商品アイテム</h1>
                 </div>
             </div>
             <div className="collection-view-content">

```

## コミット: 24eefde3

### メッセージ

```
refactor:関数宣言の変更
Refactor ProductCollectionView props for better structure

Group related props into nested objects to improve readability and maintainability. This change reduces prop clutter in `ProductCollectionView` and enhances logical grouping of parameters.
```

### 変更されたファイル

- M	app/frontend/src/components/master/ProductDetail.tsx
- M	app/frontend/src/views/master/ProductCollection.tsx

### 変更内容

```diff
commit 24eefde3aca14cb0f4b0eecf07e8628bffa88bd3
Author: k2works <kakimomokuri@gmail.com>
Date:   Mon Dec 23 16:31:02 2024 +0900

    refactor:関数宣言の変更
    
    Refactor ProductCollectionView props for better structure
    
    Group related props into nested objects to improve readability and maintainability. This change reduces prop clutter in `ProductCollectionView` and enhances logical grouping of parameters.

diff --git a/app/frontend/src/components/master/ProductDetail.tsx b/app/frontend/src/components/master/ProductDetail.tsx
index 5b4ed2b4..2fab2b09 100644
--- a/app/frontend/src/components/master/ProductDetail.tsx
+++ b/app/frontend/src/components/master/ProductDetail.tsx
@@ -427,17 +427,10 @@ export const ProductDetail: React.FC = () => {
                     <ProductCollectionView
                         error={error}
                         message={message}
-                        searchProductCode={searchProductCode}
-                        setSearchProductCode={setSearchProductCode}
-                        handleSearchProduct={handleSearchProduct}
-                        handleOpenModal={handleOpenModal}
-                        products={products}
-                        handleDeleteProduct={handleDeleteProduct}
-                        handleCheckProduct={handleCheckProduct}
-                        handleCheckToggleCollection={handleCheckAllProducts}
-                        handleDeleteCheckedCollection={handleDeleteCheckedProducts}
-                        pageNation={pageNation}
-                        fetchProducts={fetchProducts.load}
+                        searchItems={{searchProductCode, setSearchProductCode, handleSearchProduct}}
+                        headerItems={{handleOpenModal, handleCheckToggleCollection: handleCheckAllProducts, handleDeleteCheckedCollection: handleDeleteCheckedProducts}}
+                        contentItems={{products, handleDeleteProduct, handleCheckProduct}}
+                        pageNationItems={{pageNation, fetchProducts: fetchProducts.load}}
                     />
                 </>
             )
diff --git a/app/frontend/src/views/master/ProductCollection.tsx b/app/frontend/src/views/master/ProductCollection.tsx
index 80cd0385..76ea948a 100644
--- a/app/frontend/src/views/master/ProductCollection.tsx
+++ b/app/frontend/src/views/master/ProductCollection.tsx
@@ -99,33 +99,34 @@ const ProductList: React.FC<ProductListProps> = ({products, onEdit, onDelete, on
 interface ProductCollectionViewProps {
     error: string | null;
     message: string | null;
-    searchProductCode: string;
-    setSearchProductCode: React.Dispatch<React.SetStateAction<string>>;
-    handleSearchProduct: () => void;
-    handleOpenModal: (product?: ProductType) => void;
-    products: ProductType[];
-    handleDeleteProduct: (productCode: string) => void;
-    handleCheckProduct: (product: ProductType) => void;
-    handleCheckToggleCollection: () => void;
-    handleDeleteCheckedCollection: () => void;
-    pageNation: any; // 適切な型を使用してください
-    fetchProducts: () => void;
+    searchItems: {
+        searchProductCode: string;
+        setSearchProductCode: React.Dispatch<React.SetStateAction<string>>;
+        handleSearchProduct: () => void;
+    }
+    headerItems: {
+        handleOpenModal: (product?: ProductType) => void;
+        handleCheckToggleCollection: () => void;
+        handleDeleteCheckedCollection: () => void;
+    }
+    contentItems: {
+        products: ProductType[];
+        handleDeleteProduct: (productCode: string) => void;
+        handleCheckProduct: (product: ProductType) => void;
+    }
+    pageNationItems: {
+        pageNation: any; // 適切な型を使用してください
+        fetchProducts: () => void;
+    }
 }
 
 export const ProductCollectionView: React.FC<ProductCollectionViewProps> = ({
                                                                                 error,
                                                                                 message,
-                                                                                searchProductCode,
-                                                                                setSearchProductCode,
-                                                                                handleSearchProduct,
-                                                                                handleOpenModal,
-                                                                                products,
-                                                                                handleDeleteProduct,
-                                                                                handleCheckProduct,
-                                                                                handleCheckToggleCollection,
-                                                                                handleDeleteCheckedCollection,
-                                                                                pageNation,
-                                                                                fetchProducts
+                                                                                searchItems: {searchProductCode, setSearchProductCode, handleSearchProduct,},
+                                                                                headerItems: {handleOpenModal, handleCheckToggleCollection, handleDeleteCheckedCollection},
+                                                                                contentItems: {products, handleDeleteProduct, handleCheckProduct},
+                                                                                pageNationItems: {pageNation, fetchProducts}
                                                                             }) => (
     <div className="collection-view-object-container">
         <Message error={error} message={message}/>

```

## コミット: cca45cf7

### メッセージ

```
refactor:関数の移動
Refactor modal handling in ProductDetail component

Reorganized modal logic into reusable functions for better structure and maintainability. Consolidated modal handling code to reduce duplication and improved separation of concerns. This prepares the component for easier future expansions and updates.
```

### 変更されたファイル

- M	app/frontend/src/components/master/ProductDetail.tsx

### 変更内容

```diff
commit cca45cf7eb90a21106ca2fd798cb0cb366ff4521
Author: k2works <kakimomokuri@gmail.com>
Date:   Mon Dec 23 16:22:28 2024 +0900

    refactor:関数の移動
    
    Refactor modal handling in ProductDetail component
    
    Reorganized modal logic into reusable functions for better structure and maintainability. Consolidated modal handling code to reduce duplication and improved separation of concerns. This prepares the component for easier future expansions and updates.

diff --git a/app/frontend/src/components/master/ProductDetail.tsx b/app/frontend/src/components/master/ProductDetail.tsx
index 06d59329..5b4ed2b4 100644
--- a/app/frontend/src/components/master/ProductDetail.tsx
+++ b/app/frontend/src/components/master/ProductDetail.tsx
@@ -118,27 +118,244 @@ export const ProductDetail: React.FC = () => {
             Tabs,
         } = useTab();
 
-        // TODO:顧客マスタの作成後に実装
-        const collectionView = () => {
-            const handleOpenModal = (product?: ProductType) => {
-                setMessage("");
-                setError("");
-                if (product) {
-                    setNewProduct(product);
-                    setEditId(product.productCode.value);
-                    setIsEditing(true);
-                } else {
-                    setNewProduct(initialProduct);
-                    setIsEditing(false);
+        const modalView = () => {
+            const editModal = () => {
+                const handleOpenModal = (product?: ProductType) => {
+                    setMessage("");
+                    setError("");
+                    if (product) {
+                        setNewProduct(product);
+                        setEditId(product.productCode.value);
+                        setIsEditing(true);
+                    } else {
+                        setNewProduct(initialProduct);
+                        setIsEditing(false);
+                    }
+                    setModalIsOpen(true);
+                };
+
+                const handleCloseModal = () => {
+                    setError("");
+                    setModalIsOpen(false);
+                    setEditId(null);
+                };
+
+                const editModalView = () => {
+                    return (
+                        <Modal
+                            isOpen={modalIsOpen}
+                            onRequestClose={handleCloseModal}
+                            contentLabel="商品情報を入力"
+                            className="modal"
+                            overlayClassName="modal-overlay"
+                            bodyOpenClassName="modal-open"
+                        >
+                            {singleView()}
+
+                            {substituteModal().substituteEditModalView()}
+                            {bomModal().bomEditModalView()}
+
+                            {isEditing && (
+                                <>
+                                    <Tabs>
+                                        <TabList>
+                                            <Tab>代替品</Tab>
+                                            <Tab>部品表</Tab>
+                                            <Tab>顧客別販売単価</Tab>
+                                        </TabList>
+                                        <TabPanel>
+                                            <SubstituteProductCollectionView
+                                                substituteProducts={newProduct.substituteProduct}
+                                                handleAdd={() => {
+                                                    setMessage("");
+                                                    setError("");
+                                                    setProductIsEditing(true);
+                                                    setProductModalIsOpen(true);
+                                                }}
+                                                handleDelete={(product) => {
+                                                    setNewProduct({
+                                                        ...newProduct,
+                                                        substituteProduct: newProduct.substituteProduct.filter(
+                                                            (substituteProduct) => substituteProduct.substituteProductCode !== product.substituteProductCode
+                                                        )
+                                                    });
+                                                }}
+                                            />
+                                        </TabPanel>
+                                        <TabPanel>
+                                            <BomCollectionView
+                                                boms={newProduct.boms}
+                                                handleAdd={() => {
+                                                    setMessage("");
+                                                    setError("");
+                                                    setBomIsEditing(true);
+                                                    setBomModalIsOpen(true);
+                                                }}
+                                                handleDelete={(product) => {
+                                                    setNewProduct({
+                                                        ...newProduct,
+                                                        boms: newProduct.boms.filter(
+                                                            (bomProduct) => bomProduct.componentCode !== product.componentCode
+                                                        )
+                                                    });
+                                                }}
+                                            />
+                                        </TabPanel>
+                                        <TabPanel>
+                                            <CustomerSpecificSellingPriceCollectionView
+                                                prices={newProduct.customerSpecificSellingPrices}
+                                                handleAdd={() => {
+                                                    setNewProduct({
+                                                        ...newProduct,
+                                                        customerSpecificSellingPrices: newProduct.customerSpecificSellingPrices.concat({
+                                                            productCode: newProduct.productCode,
+                                                            customerCode: "XXXX",
+                                                            sellingPrice: {
+                                                                amount: 0,
+                                                                currency: "JPY"
+                                                            }
+                                                        })
+                                                    });
+                                                }}
+                                                handleDelete={(price) => {
+                                                    setNewProduct({
+                                                        ...newProduct,
+                                                        customerSpecificSellingPrices: newProduct.customerSpecificSellingPrices.filter(
+                                                            (sellingPrice) => sellingPrice.customerCode !== price.customerCode
+                                                        )
+                                                    });
+                                                }}
+                                            />
+                                        </TabPanel>
+                                    </Tabs>
+                                </>
+                            )
+                            }
+                        </Modal>
+                    )
                 }
-                setModalIsOpen(true);
-            };
 
-            const handleCloseModal = () => {
-                setError("");
-                setModalIsOpen(false);
-                setEditId(null);
-            };
+                return {
+                    handleOpenModal,
+                    handleCloseModal,
+                    editModalView
+                }
+            }
+
+            const substituteModal = () => {
+                const handleCloseProductModal = () => {
+                    setError("");
+                    setProductModalIsOpen(false);
+                    setProductEditId(null);
+                }
+
+                const substituteEditModalView = () => {
+                    return (
+                        <Modal
+                            isOpen={productModalIsOpen}
+                            onRequestClose={handleCloseProductModal}
+                            contentLabel="代替商品情報を入力"
+                            className="modal"
+                            overlayClassName="modal-overlay"
+                            bodyOpenClassName="modal-open"
+                        >
+                            {
+                                <ProductCollectionSelectView
+                                    products={substitutes}
+                                    handleSelect={(product) => {
+                                        const newProducts = newProduct.substituteProduct.filter((e) => e.substituteProductCode.value !== product.productCode.value);
+                                        newProducts.push({
+                                            productCode: newProduct.productCode,
+                                            substituteProductCode: product.productCode,
+                                            priority: 0
+                                        });
+                                        setNewProduct({
+                                            ...newProduct,
+                                            substituteProduct: newProducts
+                                        });
+                                    }}
+                                    handleClose={handleCloseProductModal}
+                                    pageNation={substitutePageNation}
+                                    fetchProducts={fetchSubstitutes.load}
+                                />
+                            }
+                        </Modal>
+
+                    )
+                }
+
+                return {
+                    substituteEditModalView
+                }
+            }
+
+            const bomModal = () => {
+                const handleCloseBomModal = () => {
+                    setError("");
+                    setBomModalIsOpen(false);
+                    setBomEditId(null);
+                }
+
+                const bomEditModalView = () => {
+                    return (
+                        <Modal
+                            isOpen={bomModalIsOpen}
+                            onRequestClose={handleCloseBomModal}
+                            contentLabel="部品情報を入力"
+                            className="modal"
+                            overlayClassName="modal-overlay"
+                            bodyOpenClassName="modal-open"
+                        >
+                            {
+                                <ProductCollectionSelectView
+                                    products={boms}
+                                    handleSelect={(bom) => {
+                                        const newProducts = newProduct.boms.filter((e) => e.productCode.value !== bom.productCode.value);
+                                        newProducts.push({
+                                            productCode: newProduct.productCode,
+                                            componentCode: bom.productCode,
+                                            componentQuantity: {
+                                                amount: 1,
+                                                unit: "個"
+                                            }
+                                        });
+                                        setNewProduct({
+                                            ...newProduct,
+                                            boms: newProducts
+                                        });
+                                    }}
+                                    handleClose={handleCloseBomModal}
+                                    pageNation={bomPageNation}
+                                    fetchProducts={fetchBoms.load}
+                                />
+                            }
+                        </Modal>
+
+                    )
+                }
+
+                return {
+                    bomEditModalView
+                }
+            }
+
+            const init = () => {
+                return (
+                    <>
+                        {editModal().editModalView()}
+                    </>
+                )
+            }
+
+            return {
+                editModal,
+                init
+            }
+        }
+
+        // TODO:顧客マスタの作成後に実装
+        const collectionView = () => {
+            const {handleOpenModal} = modalView().editModal();
 
             const handleSearchProduct = async () => {
                 if (!searchProductCode.trim()) return;
@@ -207,16 +424,6 @@ export const ProductDetail: React.FC = () => {
 
             return (
                 <>
-                    <Modal
-                        isOpen={modalIsOpen}
-                        onRequestClose={handleCloseModal}
-                        contentLabel="商品情報を入力"
-                        className="modal"
-                        overlayClassName="modal-overlay"
-                        bodyOpenClassName="modal-open"
-                    >
-                        {singleView()}
-                    </Modal>
                     <ProductCollectionView
                         error={error}
                         message={message}
@@ -237,23 +444,7 @@ export const ProductDetail: React.FC = () => {
         };
 
         const singleView = () => {
-            const handleCloseModal = () => {
-                setError("");
-                setModalIsOpen(false);
-                setEditId(null);
-            };
-
-            const handleCloseProductModal = () => {
-                setError("");
-                setProductModalIsOpen(false);
-                setProductEditId(null);
-            }
-
-            const handleCloseBomModal = () => {
-                setError("");
-                setBomModalIsOpen(false);
-                setBomEditId(null);
-            }
+            const {handleCloseModal} = modalView().editModal();
 
             const handleCreateOrUpdateProduct = async () => {
                 const validateProduct = (): boolean => {
@@ -301,70 +492,6 @@ export const ProductDetail: React.FC = () => {
             };
 
             return (
-                <>
-                    <Modal
-                        isOpen={productModalIsOpen}
-                        onRequestClose={handleCloseProductModal}
-                        contentLabel="代替商品情報を入力"
-                        className="modal"
-                        overlayClassName="modal-overlay"
-                        bodyOpenClassName="modal-open"
-                    >
-                        {
-                            <ProductCollectionSelectView
-                                products={substitutes}
-                                handleSelect={(product) => {
-                                    const newProducts = newProduct.substituteProduct.filter((e) => e.substituteProductCode.value !== product.productCode.value);
-                                    newProducts.push({
-                                        productCode: newProduct.productCode,
-                                        substituteProductCode: product.productCode,
-                                        priority: 0
-                                    });
-                                    setNewProduct({
-                                        ...newProduct,
-                                        substituteProduct: newProducts
-                                    });
-                                }}
-                                handleClose={handleCloseProductModal}
-                                pageNation={substitutePageNation}
-                                fetchProducts={fetchSubstitutes.load}
-                            />
-                        }
-                    </Modal>
-
-                    <Modal
-                        isOpen={bomModalIsOpen}
-                        onRequestClose={handleCloseBomModal}
-                        contentLabel="部品情報を入力"
-                        className="modal"
-                        overlayClassName="modal-overlay"
-                        bodyOpenClassName="modal-open"
-                    >
-                        {
-                            <ProductCollectionSelectView
-                                products={boms}
-                                handleSelect={(bom) => {
-                                    const newProducts = newProduct.boms.filter((e) => e.productCode.value !== bom.productCode.value);
-                                    newProducts.push({
-                                        productCode: newProduct.productCode,
-                                        componentCode: bom.productCode,
-                                        componentQuantity: {
-                                            amount: 1,
-                                            unit: "個"
-                                        }
-                                    });
-                                    setNewProduct({
-                                        ...newProduct,
-                                        boms: newProducts
-                                    });
-                                }}
-                                handleClose={handleCloseBomModal}
-                                pageNation={bomPageNation}
-                                fetchProducts={fetchBoms.load}
-                            />
-                        }
-                    </Modal>
-
                     <ProductSingleView
                         error={error}
                         message={message}
@@ -374,84 +501,6 @@ export const ProductDetail: React.FC = () => {
                         handleCreateOrUpdateProduct={handleCreateOrUpdateProduct}
                         handleCloseModal={handleCloseModal}
                     />
-
-                    {isEditing && (
-                        <>
-                            <Tabs>
-                                <TabList>
-                                    <Tab>代替品</Tab>
-                                    <Tab>部品表</Tab>
-                                    <Tab>顧客別販売単価</Tab>
-                                </TabList>
-                                <TabPanel>
-                                    <SubstituteProductCollectionView
-                                        substituteProducts={newProduct.substituteProduct}
-                                        handleAdd={() => {
-                                            setMessage("");
-                                            setError("");
-                                            setProductIsEditing(true);
-                                            setProductModalIsOpen(true);
-                                        }}
-                                        handleDelete={(product) => {
-                                            setNewProduct({
-                                                ...newProduct,
-                                                substituteProduct: newProduct.substituteProduct.filter(
-                                                    (substituteProduct) => substituteProduct.substituteProductCode !== product.substituteProductCode
-                                                )
-                                            });
-                                        }}
-                                    />
-                                </TabPanel>
-                                <TabPanel>
-                                    <BomCollectionView
-                                        boms={newProduct.boms}
-                                        handleAdd={() => {
-                                            setMessage("");
-                                            setError("");
-                                            setBomIsEditing(true);
-                                            setBomModalIsOpen(true);
-                                        }}
-                                        handleDelete={(product) => {
-                                            setNewProduct({
-                                                ...newProduct,
-                                                boms: newProduct.boms.filter(
-                                                    (bomProduct) => bomProduct.componentCode !== product.componentCode
-                                                )
-                                            });
-                                        }}
-                                    />
-                                </TabPanel>
-                                <TabPanel>
-                                    <CustomerSpecificSellingPriceCollectionView
-                                        prices={newProduct.customerSpecificSellingPrices}
-                                        handleAdd={() => {
-                                            setNewProduct({
-                                                ...newProduct,
-                                                customerSpecificSellingPrices: newProduct.customerSpecificSellingPrices.concat({
-                                                    productCode: newProduct.productCode,
-                                                    customerCode: "XXXX",
-                                                    sellingPrice: {
-                                                        amount: 0,
-                                                        currency: "JPY"
-                                                    }
-                                                })
-                                            });
-                                        }}
-                                        handleDelete={(price) => {
-                                            setNewProduct({
-                                                ...newProduct,
-                                                customerSpecificSellingPrices: newProduct.customerSpecificSellingPrices.filter(
-                                                    (sellingPrice) => sellingPrice.customerCode !== price.customerCode
-                                                )
-                                            });
-                                        }}
-                                    />
-                                </TabPanel>
-                            </Tabs>
-                        </>
-                    )
-                    }
-                </>
             );
         };
 
@@ -460,7 +509,10 @@ export const ProductDetail: React.FC = () => {
                 {loading ? (
                     <LoadingIndicator/>
                 ) : (
-                    collectionView()
+                    <>
+                        {modalView().init()}
+                        {collectionView()}
+                    </>
                 )}
             </>
         );

```

## コミット: 8e429936

### メッセージ

```
refactor:デッドコードの削除
Remove unused Tab imports from ProductDetail.tsx

The Tab, TabList, TabPanel, and Tabs imports were unused and have been removed to clean up the code. This improves maintainability and eliminates unnecessary dependencies.
```

### 変更されたファイル

- M	app/frontend/src/components/master/ProductDetail.tsx

### 変更内容

```diff
commit 8e429936dcb7e62970d23b876a2df0909391956a
Author: k2works <kakimomokuri@gmail.com>
Date:   Mon Dec 23 16:00:26 2024 +0900

    refactor:デッドコードの削除
    
    Remove unused Tab imports from ProductDetail.tsx
    
    The Tab, TabList, TabPanel, and Tabs imports were unused and have been removed to clean up the code. This improves maintainability and eliminates unnecessary dependencies.

diff --git a/app/frontend/src/components/master/ProductDetail.tsx b/app/frontend/src/components/master/ProductDetail.tsx
index 619e4a13..06d59329 100644
--- a/app/frontend/src/components/master/ProductDetail.tsx
+++ b/app/frontend/src/components/master/ProductDetail.tsx
@@ -15,7 +15,6 @@ import {BomCollectionView} from "../../views/master/BomCollection.tsx";
 import {
     CustomerSpecificSellingPriceCollectionView
 } from "../../views/master/CustomerSpecificSellingPriceCollection.tsx";
-import {Tab, TabList, TabPanel, Tabs} from "react-tabs";
 
 export const ProductDetail: React.FC = () => {
     const Content: React.FC = () => {

```

## コミット: 9f92f2c1

### メッセージ

```
refactor:関数宣言の変更
Refactor ProductCategory props into grouped objects

Simplify and restructure props in `ProductCategoryCollectionView` by grouping related props into nested objects: `searchItems`, `headerItems`, `collectionItems`, and `pageNationItems`. This improves code readability and enhances maintainability.
```

### 変更されたファイル

- M	app/frontend/src/components/master/ProductCategory.tsx
- M	app/frontend/src/views/master/ProductCategoryCollection.tsx

### 変更内容

```diff
commit 9f92f2c15ca08fe116f8a0e0bbb99fff0cf7d99b
Author: k2works <kakimomokuri@gmail.com>
Date:   Mon Dec 23 15:51:56 2024 +0900

    refactor:関数宣言の変更
    
    Refactor ProductCategory props into grouped objects
    
    Simplify and restructure props in `ProductCategoryCollectionView` by grouping related props into nested objects: `searchItems`, `headerItems`, `collectionItems`, and `pageNationItems`. This improves code readability and enhances maintainability.

diff --git a/app/frontend/src/components/master/ProductCategory.tsx b/app/frontend/src/components/master/ProductCategory.tsx
index b563ee91..de116a46 100644
--- a/app/frontend/src/components/master/ProductCategory.tsx
+++ b/app/frontend/src/components/master/ProductCategory.tsx
@@ -284,17 +284,10 @@ export const ProductCategory: React.FC = () => {
                     <ProductCategoryCollectionView
                         error={error}
                         message={message}
-                        searchProductCategoryCode={searchProductCategoryCode}
-                        setSearchProductCategoryCode={setSearchProductCategoryCode}
-                        handleSearchProductCategory={handleSearchProductCategory}
-                        handleOpenModal={handleOpenModal}
-                        productCategories={productCategories}
-                        handleDeleteProductCategory={handleDeleteProductCategory}
-                        handleCheckProductCategory={handleCheckProductCategory}
-                        handleCheckToggleCollection={handleCheckAllProductCategories}
-                        handleDeleteCheckedCollection={handleDeleteCheckedProductCategories}
-                        pageNation={pageNation}
-                        fetchProductCategories={fetchProductCategories.load}
+                        searchItems={{searchProductCategoryCode, setSearchProductCategoryCode, handleSearchProductCategory}}
+                        headerItems={{handleOpenModal, handleCheckToggleCollection:handleCheckAllProductCategories, handleDeleteCheckedCollection:handleDeleteCheckedProductCategories}}
+                        collectionItems={{productCategories, handleDeleteProductCategory, handleCheckProductCategory}}
+                        pageNationItems={{pageNation, fetchProductCategories: fetchProductCategories.load}}
                     />
                 </>
             )
diff --git a/app/frontend/src/views/master/ProductCategoryCollection.tsx b/app/frontend/src/views/master/ProductCategoryCollection.tsx
index e7d2bdb0..de91fa79 100644
--- a/app/frontend/src/views/master/ProductCategoryCollection.tsx
+++ b/app/frontend/src/views/master/ProductCategoryCollection.tsx
@@ -78,33 +78,34 @@ const ProductCategoryList: React.FC<ProductCategoryListProps> = ({productCategor
 interface ProductCategoryCollectionViewProps {
     error: string | null;
     message: string | null;
-    searchProductCategoryCode: string;
-    setSearchProductCategoryCode: React.Dispatch<React.SetStateAction<string>>;
-    handleSearchProductCategory: () => void;
-    handleOpenModal: (productCategory?: ProductCategoryType) => void;
-    productCategories: ProductCategoryType[];
-    handleDeleteProductCategory: (productCategoryCode: string) => void;
-    handleCheckProductCategory: (productCategory: ProductCategoryType) => void;
-    handleCheckToggleCollection: () => void;
-    handleDeleteCheckedCollection: () => void;
-    pageNation: any; // 適切な型を使用してください
-    fetchProductCategories: () => void;
+    searchItems: {
+        searchProductCategoryCode: string;
+        setSearchProductCategoryCode: React.Dispatch<React.SetStateAction<string>>;
+        handleSearchProductCategory: () => void;
+    }
+    headerItems: {
+        handleOpenModal: (productCategory?: ProductCategoryType) => void;
+        handleCheckToggleCollection: () => void;
+        handleDeleteCheckedCollection: () => void;
+    }
+    collectionItems: {
+        productCategories: ProductCategoryType[];
+        handleDeleteProductCategory: (productCategoryCode: string) => void;
+        handleCheckProductCategory: (productCategory: ProductCategoryType) => void;
+    }
+    pageNationItems: {
+        pageNation: any;
+        fetchProductCategories: () => void;
+    }
 }
 
 export const ProductCategoryCollectionView: React.FC<ProductCategoryCollectionViewProps> = ({
                                                                                                 error,
                                                                                                 message,
-                                                                                                searchProductCategoryCode,
-                                                                                                setSearchProductCategoryCode,
-                                                                                                handleSearchProductCategory,
-                                                                                                handleOpenModal,
-                                                                                                productCategories,
-                                                                                                handleDeleteProductCategory,
-                                                                                                handleCheckProductCategory,
-                                                                                                handleCheckToggleCollection,
-                                                                                                handleDeleteCheckedCollection,
-                                                                                                pageNation,
-                                                                                                fetchProductCategories
+                                                                                                searchItems: {searchProductCategoryCode, setSearchProductCategoryCode, handleSearchProductCategory},
+                                                                                                collectionItems: {productCategories, handleDeleteProductCategory, handleCheckProductCategory},
+                                                                                                headerItems: {handleOpenModal, handleCheckToggleCollection, handleDeleteCheckedCollection},
+                                                                                                pageNationItems: {pageNation, fetchProductCategories}
                                                                                             }) => (
     <div className="collection-view-object-container">
         <Message error={error} message={message}/>

```

## コミット: b70651fa

### メッセージ

```
refactor:関数の抽出
Refactor modal logic into reusable modalView functions.

Modularized modal handling by consolidating repeated logic into `modalView`, simplifying `collectionView` and `singleView`. This improves code readability, maintainability, and avoids redundancy in modal-related components.
```

### 変更されたファイル

- M	app/frontend/src/components/master/ProductCategory.tsx

### 変更内容

```diff
commit b70651fa6be2e27ff923eae9c2e939a0571e9ef6
Author: k2works <kakimomokuri@gmail.com>
Date:   Mon Dec 23 15:41:07 2024 +0900

    refactor:関数の抽出
    
    Refactor modal logic into reusable modalView functions.
    
    Modularized modal handling by consolidating repeated logic into `modalView`, simplifying `collectionView` and `singleView`. This improves code readability, maintainability, and avoids redundancy in modal-related components.

diff --git a/app/frontend/src/components/master/ProductCategory.tsx b/app/frontend/src/components/master/ProductCategory.tsx
index c47786c9..b563ee91 100644
--- a/app/frontend/src/components/master/ProductCategory.tsx
+++ b/app/frontend/src/components/master/ProductCategory.tsx
@@ -75,26 +75,144 @@ export const ProductCategory: React.FC = () => {
             );
         }, []);
 
-        const collectionView = () => {
-            const handleOpenModal = (productCategory?: ProductCategoryType) => {
-                setMessage("");
-                setError("");
-                if (productCategory) {
-                    setNewProductCategory(productCategory);
-                    setEditId(productCategory.productCategoryCode.value);
-                    setIsEditing(true);
-                } else {
-                    setNewProductCategory(initialProductCategory);
-                    setIsEditing(false);
+        const modalView = () => {
+            const editModal = () => {
+                const handleOpenModal = (productCategory?: ProductCategoryType) => {
+                    setMessage("");
+                    setError("");
+                    if (productCategory) {
+                        setNewProductCategory(productCategory);
+                        setEditId(productCategory.productCategoryCode.value);
+                        setIsEditing(true);
+                    } else {
+                        setNewProductCategory(initialProductCategory);
+                        setIsEditing(false);
+                    }
+                    setModalIsOpen(true);
+                };
+
+                const handleCloseModal = () => {
+                    setError("");
+                    setModalIsOpen(false);
+                    setEditId(null);
+                };
+
+                const editModalView = () => {
+                    return (
+                        <Modal
+                            isOpen={modalIsOpen}
+                            onRequestClose={handleCloseModal}
+                            contentLabel="商品分類情報を入力"
+                            className="modal"
+                            overlayClassName="modal-overlay"
+                            bodyOpenClassName="modal-open"
+                        >
+                            {singleView()}
+
+                            {productModal().productModalView()}
+
+                            {isEditing && (
+                                <ProductCollectionListView
+                                    products={newProductCategory.products.filter((e) => !e.deleteFlag)}
+                                    handleAdd={() => {
+                                        setMessage("");
+                                        setError("");
+                                        setProductIsEditing(true);
+                                        setProductModalIsOpen(true);
+                                    }}
+                                    handleDelete={(product: ProductType) => {
+                                        const newProducts = newProductCategory.products.filter((e) => e.productCode.value !== product.productCode.value);
+                                        if (product.productCode.value) {
+                                            newProducts.push({
+                                                ...product,
+                                                addFlag: false,
+                                                deleteFlag: true
+                                            });
+                                        }
+                                        setNewProductCategory({
+                                            ...newProductCategory,
+                                            products: newProducts
+                                        });
+                                    }}
+                                />
+                            )
+                            }
+                        </Modal>
+                    )
                 }
-                setModalIsOpen(true);
-            };
 
-            const handleCloseModal = () => {
-                setError("");
-                setModalIsOpen(false);
-                setEditId(null);
-            };
+                return {
+                    handleOpenModal,
+                    handleCloseModal,
+                    editModalView
+                }
+            }
+
+            const productModal = () => {
+                const handleCloseProductModal = () => {
+                    setError("");
+                    setProductModalIsOpen(false);
+                    setProductEditId(null);
+                }
+
+                const productModalView = () => {
+                    return (
+                        <Modal
+                            isOpen={productModalIsOpen}
+                            onRequestClose={handleCloseProductModal}
+                            contentLabel="商品情報を入力"
+                            className="modal"
+                            overlayClassName="modal-overlay"
+                            bodyOpenClassName="modal-open"
+                        >
+                            {
+                                <ProductCollectionSelectView
+                                    products={products}
+                                    handleSelect={(product) => {
+                                        const newProducts = newProductCategory.products.filter((e) => e.productCode.value !== product.productCode.value);
+                                        if (product.productCode.value) {
+                                            newProducts.push({
+                                                ...product,
+                                                addFlag: true,
+                                                deleteFlag: false
+                                            });
+                                        }
+                                        setNewProductCategory({
+                                            ...newProductCategory,
+                                            products: newProducts
+                                        });
+                                    }}
+                                    handleClose={handleCloseProductModal}
+                                    pageNation={pageNation}
+                                    fetchProducts={fetchProducts.load}
+                                />
+                            }
+                        </Modal>
+
+                    )
+                }
+
+                return {
+                    productModalView,
+                }
+            }
+
+            const init = () => {
+                return (
+                    <>
+                        {editModal().editModalView()}
+                    </>
+                )
+            }
+
+            return {
+                editModal,
+                init
+            }
+        }
+
+        const collectionView = () => {
+            const {handleOpenModal} = modalView().editModal()
 
             const handleSearchProductCategory = async () => {
                 if (!searchProductCategoryCode.trim()) return;
@@ -163,16 +281,6 @@ export const ProductCategory: React.FC = () => {
 
             return (
                 <>
-                    <Modal
-                        isOpen={modalIsOpen}
-                        onRequestClose={handleCloseModal}
-                        contentLabel="商品分類情報を入力"
-                        className="modal"
-                        overlayClassName="modal-overlay"
-                        bodyOpenClassName="modal-open"
-                    >
-                        {singleView()}
-                    </Modal>
                     <ProductCategoryCollectionView
                         error={error}
                         message={message}
@@ -193,17 +301,7 @@ export const ProductCategory: React.FC = () => {
         };
 
         const singleView = () => {
-            const handleCloseModal = () => {
-                setError("");
-                setModalIsOpen(false);
-                setEditId(null);
-            };
-
-            const handleCloseProductModal = () => {
-                setError("");
-                setProductModalIsOpen(false);
-                setProductEditId(null);
-            }
+            const {handleCloseModal} = modalView().editModal();
 
             const handleCreateOrUpdateProductCategory = async () => {
                 const validateProductCategory = (): boolean => {
@@ -234,39 +332,6 @@ export const ProductCategory: React.FC = () => {
             };
 
             return (
-                <>
-                    <Modal
-                        isOpen={productModalIsOpen}
-                        onRequestClose={handleCloseProductModal}
-                        contentLabel="商品情報を入力"
-                        className="modal"
-                        overlayClassName="modal-overlay"
-                        bodyOpenClassName="modal-open"
-                    >
-                        {
-                            <ProductCollectionSelectView
-                                products={products}
-                                handleSelect={(product) => {
-                                    const newProducts = newProductCategory.products.filter((e) => e.productCode.value !== product.productCode.value);
-                                    if (product.productCode.value) {
-                                        newProducts.push({
-                                            ...product,
-                                            addFlag: true,
-                                            deleteFlag: false
-                                        });
-                                    }
-                                    setNewProductCategory({
-                                        ...newProductCategory,
-                                        products: newProducts
-                                    });
-                                }}
-                                handleClose={handleCloseProductModal}
-                                pageNation={pageNation}
-                                fetchProducts={fetchProducts.load}
-                            />
-                        }
-                    </Modal>
-
                     <ProductCategorySingleView
                         error={error}
                         message={message}
@@ -276,34 +341,6 @@ export const ProductCategory: React.FC = () => {
                         handleCreateOrUpdateProductCategory={handleCreateOrUpdateProductCategory}
                         handleCloseModal={handleCloseModal}
                     />
-
-                    {isEditing && (
-                        <ProductCollectionListView
-                            products={newProductCategory.products.filter((e) => !e.deleteFlag)}
-                            handleAdd={() => {
-                                setMessage("");
-                                setError("");
-                                setProductIsEditing(true);
-                                setProductModalIsOpen(true);
-                            }}
-                            handleDelete={(product: ProductType) => {
-                                const newProducts = newProductCategory.products.filter((e) => e.productCode.value !== product.productCode.value);
-                                if (product.productCode.value) {
-                                    newProducts.push({
-                                        ...product,
-                                        addFlag: false,
-                                        deleteFlag: true
-                                    });
-                                }
-                                setNewProductCategory({
-                                    ...newProductCategory,
-                                    products: newProducts
-                                });
-                            }}
-                        />
-                    )
-                    }
-                </>
             );
         };
 
@@ -312,7 +349,11 @@ export const ProductCategory: React.FC = () => {
                 {loading ? (
                     <LoadingIndicator/>
                 ) : (
-                    collectionView()
+                    <>
+                        {modalView().init()}
+                        {collectionView()}
+                    </>
+
                 )}
             </>
         );

```

## コミット: 8483c7cd

### メッセージ

```
refactor:関数宣言の変更
Refactor EmployeeCollectionView props for better organization

Reorganized props in EmployeeCollectionView by grouping related items into logical objects: searchItems, headerItems, collectionItems, and pageNationItems. This improves readability, reduces prop clutter, and enhances maintainability of the component.
```

### 変更されたファイル

- M	app/frontend/src/components/master/Employee.tsx
- M	app/frontend/src/views/master/EmployeeCollection.tsx

### 変更内容

```diff
commit 8483c7cd7576840d75915bfd06a2b6e9702b34b9
Author: k2works <kakimomokuri@gmail.com>
Date:   Mon Dec 23 15:20:46 2024 +0900

    refactor:関数宣言の変更
    
    Refactor EmployeeCollectionView props for better organization
    
    Reorganized props in EmployeeCollectionView by grouping related items into logical objects: searchItems, headerItems, collectionItems, and pageNationItems. This improves readability, reduces prop clutter, and enhances maintainability of the component.

diff --git a/app/frontend/src/components/master/Employee.tsx b/app/frontend/src/components/master/Employee.tsx
index 3d0ee0f9..2fe35d65 100644
--- a/app/frontend/src/components/master/Employee.tsx
+++ b/app/frontend/src/components/master/Employee.tsx
@@ -303,17 +303,10 @@ export const Employee: React.FC = () => {
                     <EmployeeCollectionView
                         error={error}
                         message={message}
-                        searchEmployeeCode={searchEmployeeCode}
-                        setSearchEmployeeCode={setSearchEmployeeCode}
-                        handleSearchEmployee={handleSearchEmployee}
-                        handleOpenModal={handleOpenModal}
-                        employees={employees}
-                        handleDeleteEmployee={handleDeleteEmployee}
-                        handleCheckEmployee={handleCheckEmployee}
-                        handleCheckToggleCollection={handleCheckAllEmployees}
-                        handleDeleteCheckedCollection={handleDeleteCheckedEmployees}
-                        pageNation={pageNation}
-                        fetchEmployees={fetchEmployees.load}
+                        searchItems={{searchEmployeeCode, setSearchEmployeeCode, handleSearchEmployee}}
+                        headerItems={{handleOpenModal, handleCheckToggleCollection:handleCheckAllEmployees, handleDeleteCheckedCollection:handleDeleteCheckedEmployees}}
+                        collectionItems={{employees, handleDeleteEmployee, handleCheckEmployee}}
+                        pageNationItems={{pageNation, fetchEmployees:fetchEmployees.load}}
                     />
             )
         };
diff --git a/app/frontend/src/views/master/EmployeeCollection.tsx b/app/frontend/src/views/master/EmployeeCollection.tsx
index 65f7651a..b4efd6a2 100644
--- a/app/frontend/src/views/master/EmployeeCollection.tsx
+++ b/app/frontend/src/views/master/EmployeeCollection.tsx
@@ -88,33 +88,34 @@ const EmployeeList: React.FC<EmployeeListProps> = ({employees, onEdit, onDelete,
 interface EmployeeCollectionViewProps {
     error: string | null;
     message: string | null;
-    searchEmployeeCode: string;
-    setSearchEmployeeCode: (value: string) => void;
-    handleSearchEmployee: () => void;
-    handleOpenModal: () => void;
-    employees: any[];
-    handleDeleteEmployee: (empCode: string) => void;
-    handleCheckEmployee: (employee: EmployeeType) => void;
-    handleCheckToggleCollection: () => void;
-    handleDeleteCheckedCollection: () => void;
-    pageNation: any;
-    fetchEmployees: (page: number) => void;
+    searchItems: {
+        searchEmployeeCode: string;
+        setSearchEmployeeCode: React.Dispatch<React.SetStateAction<string>>;
+        handleSearchEmployee: () => void;
+    }
+    headerItems: {
+        handleOpenModal: () => void;
+        handleCheckToggleCollection: () => void;
+        handleDeleteCheckedCollection: () => void;
+    }
+    collectionItems: {
+        employees: any[];
+        handleDeleteEmployee: (empCode: string) => void;
+        handleCheckEmployee: (employee: EmployeeType) => void;
+    }
+    pageNationItems: {
+        pageNation: any;
+        fetchEmployees: (page: number) => void;
+    }
 }
 
 export const EmployeeCollectionView: React.FC<EmployeeCollectionViewProps> = ({
                                                                                   error,
                                                                                   message,
-                                                                                  searchEmployeeCode,
-                                                                                  setSearchEmployeeCode,
-                                                                                  handleSearchEmployee,
-                                                                                  handleOpenModal,
-                                                                                  employees,
-                                                                                  handleDeleteEmployee,
-                                                                                  handleCheckEmployee,
-                                                                                  handleCheckToggleCollection,
-                                                                                  handleDeleteCheckedCollection,
-                                                                                  pageNation,
-                                                                                  fetchEmployees
+                                                                                  searchItems: {searchEmployeeCode, setSearchEmployeeCode, handleSearchEmployee},
+                                                                                  headerItems: {handleOpenModal, handleCheckToggleCollection, handleDeleteCheckedCollection},
+                                                                                  collectionItems: {employees, handleDeleteEmployee, handleCheckEmployee},
+                                                                                  pageNationItems: {pageNation, fetchEmployees}
                                                                               }) => {
     return (
         <div className="collection-view-object-container">

```

## コミット: 39f544c5

### メッセージ

```
refactor:関数の移動
Refactor modals in Employee component for improved structure

Simplified modal handling by consolidating related views. Moved department and employee modals inside the main edit modal for better encapsulation. Removed redundant code to enhance maintainability and readability.
```

### 変更されたファイル

- M	app/frontend/src/components/master/Employee.tsx

### 変更内容

```diff
commit 39f544c5edf0a43a68a5bf7c2f290032dd627d29
Author: k2works <kakimomokuri@gmail.com>
Date:   Mon Dec 23 15:09:27 2024 +0900

    refactor:関数の移動
    
    Refactor modals in Employee component for improved structure
    
    Simplified modal handling by consolidating related views. Moved department and employee modals inside the main edit modal for better encapsulation. Removed redundant code to enhance maintainability and readability.

diff --git a/app/frontend/src/components/master/Employee.tsx b/app/frontend/src/components/master/Employee.tsx
index f5568c6c..3d0ee0f9 100644
--- a/app/frontend/src/components/master/Employee.tsx
+++ b/app/frontend/src/components/master/Employee.tsx
@@ -115,16 +115,28 @@ export const Employee: React.FC = () => {
 
                 const editModalView = () => {
                     return(
-                        <Modal
-                            isOpen={modalIsOpen}
-                            onRequestClose={handleCloseModal}
-                            contentLabel="社員情報を入力"
-                            className="modal"
-                            overlayClassName="modal-overlay"
-                            bodyOpenClassName="modal-open"
-                        >
-                            {singleView()}
-                        </Modal>
+                        <>
+                            <Modal
+                                isOpen={modalIsOpen}
+                                onRequestClose={handleCloseModal}
+                                contentLabel="社員情報を入力"
+                                className="modal"
+                                overlayClassName="modal-overlay"
+                                bodyOpenClassName="modal-open"
+                            >
+                                {singleView()}
+
+                                {departmentModal().departmentModalView()}
+                                <DepartmentSelectView
+                                    handleSelect={() => setDepartmentModalIsOpen(true)}
+                                />
+
+                                {employeeModal().employeeModalView()}
+                                <UserSelectView
+                                    handleSelect={() => setUserModalIsOpen(true)}
+                                />
+                            </Modal>
+                        </>
                     )
                 }
 
@@ -208,8 +220,6 @@ export const Employee: React.FC = () => {
             const init = () => (
                 <>
                     {editModal().editModalView()}
-                    {departmentModal().departmentModalView()}
-                    {employeeModal().employeeModalView()}
                 </>
             )
 
@@ -346,7 +356,6 @@ export const Employee: React.FC = () => {
             };
 
             return (
-                <>
                     <EmployeeSingleView
                         error={error}
                         message={message}
@@ -356,15 +365,6 @@ export const Employee: React.FC = () => {
                         handleCreateOrUpdateEmployee={handleCreateOrUpdateEmployee}
                         handleCloseModal={handleCloseModal}
                     />
-
-                    <DepartmentSelectView
-                        handleSelect={() => setDepartmentModalIsOpen(true)}
-                    />
-
-                    <UserSelectView
-                        handleSelect={() => setUserModalIsOpen(true)}
-                    />
-                </>
             )
         };
 

```

