# 作業履歴 2024-11-15

## 概要

2024-11-15の作業内容をまとめています。

## コミット: 39be40b9

### メッセージ

```
chore:タスク
Update dev command to include npm install

Prepending 'npm install' ensures that all required dependencies are installed before running the development server. This change helps prevent runtime errors caused by missing packages.
```

### 変更されたファイル

- M	ops/gulp/tasks/custom.js

### 変更内容

```diff
commit 39be40b965a7238309e4e4f4144194f98c07fd56
Author: k2works <kakimomokuri@gmail.com>
Date:   Fri Nov 15 14:19:15 2024 +0900

    chore:タスク
    
    Update dev command to include npm install
    
    Prepending 'npm install' ensures that all required dependencies are installed before running the development server. This change helps prevent runtime errors caused by missing packages.

diff --git a/ops/gulp/tasks/custom.js b/ops/gulp/tasks/custom.js
index 0eb2e720..d1b66f0a 100644
--- a/ops/gulp/tasks/custom.js
+++ b/ops/gulp/tasks/custom.js
@@ -24,7 +24,7 @@ const api = {
 
 const app = {
     dev: () => {
-        const command = 'npm run dev';
+        const command = 'npm install && npm run dev';
         return src(apiGradlewPath, { read: false })
             .pipe(exec(command, { cwd: appCwd }));
     }

```

