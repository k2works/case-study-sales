# 作業履歴 2024-11-03

## 概要

2024-11-03の作業内容をまとめています。

## コミット: 83e7429f

### メッセージ

```
docs:商品
Add product management features

Implemented product-related functionalities including product listing, detail view, creation, editing, and deletion in both data model and user interfaces. Updated TODO list and UML diagrams to reflect these changes and marked the completion of department and employee related tasks.
```

### 変更されたファイル

- M	index.js

### 変更内容

```diff
commit 83e7429ffbe9124f1181037fa3a3efe10468af53
Author: Katuyuki Kakigi <kakimomokuri@gmail.com>
Date:   Sun Nov 3 16:05:02 2024 +0900

    docs:商品
    
    Add product management features
    
    Implemented product-related functionalities including product listing, detail view, creation, editing, and deletion in both data model and user interfaces. Updated TODO list and UML diagrams to reflect these changes and marked the completion of department and employee related tasks.

diff --git a/index.js b/index.js
index 85c86950..a280a799 100644
--- a/index.js
+++ b/index.js
@@ -18,12 +18,20 @@ const mindmap = `
 ----- 社員新規作成
 ----- 社員編集
 ----- 社員削除
+---- 商品
+----- 商品一覧
+----- 商品詳細
+----- 商品新規作成
+----- 商品編集
+----- 商品削除
 -- ドメインモデル
 --- 部門
 --- 社員
+--- 商品
 -- データモデル
 --- 部門マスタ
 --- 社員マスタ
+--- 商品マスタ
 ** ユーザーインターフェース
 *** ビュー
 **** 部門一覧
@@ -33,6 +41,9 @@ const mindmap = `
 **** 社員
 ***** 部門一覧選択
 ***** ユーザー一覧選択
+**** 商品
+**** 商品一覧
+
 *** モデル
 **** 部門一覧
 **** 部門
@@ -40,11 +51,14 @@ const mindmap = `
 **** 社員
 **** 部門一覧選択
 **** ユーザー一覧選択
+**** 商品
+**** 商品一覧
 *** インタラクション
 **** ログイン
 **** ナビゲーション
 ***** 部門
 ***** 社員
+***** 商品
 ***** ユーザー
 **** ログアウト
 
@@ -61,17 +75,19 @@ const contents = `
 ## TODOリスト
 
 - [ ] マスタ管理APIを作成する
-  - [ ] 部門APIを作成する
+  - [x] 部門APIを作成する
     - [x] 部門一覧が全件selectされない
     - [x] 部門の開始日がずれて更新と登録がうまくいかない
-  - [ ] 社員APIを作成する
+  - [x] 社員APIを作成する
+  - [ ] 商品APIを作成する
 - [ ] マスタ管理画面を作成する
-  - [ ] 部門画面を作成する
+  - [x] 部門画面を作成する
     - [x] 部門に所属する社員を表示する
     - [x] 社員一覧から社員を追加する
-  - [ ] 社員画面を作成する
+  - [x] 社員画面を作成する
     - [x] 部門一覧から部門を選択する
     - [x] ユーザー一覧からユーザーを選択する
+  - [ ] 商品画面を作成する
 - [ ] 初期データの日付がUTCなのでJSTに変換する
 - [x] 削除実行時に確認ダイアログを表示する
 - [x] 一括削除機能を追加する
@@ -98,6 +114,13 @@ rectangle マスタ管理 {
     (社員編集)
     (社員削除)
  }
+ rectangle 商品 {
+    (商品一覧)
+    (商品詳細)
+    (商品新規作成)
+    (商品編集)
+    (商品削除)
+ }
 }
 admin --> (部門一覧)
 admin --> (部門詳細)
@@ -109,6 +132,11 @@ admin --> (社員詳細)
 admin --> (社員新規作成)
 admin --> (社員編集)
 admin --> (社員削除)
+admin --> (商品一覧)
+admin --> (商品詳細)
+admin --> (商品新規作成)
+admin --> (商品編集)
+admin --> (商品削除)
 
 @enduml
 `;
@@ -122,10 +150,14 @@ class 社員一覧
 class 社員
 class ユーザー
 
+class 商品一覧
+class 商品
+
 部門一覧 *-- 部門
 部門 *-- 社員
 社員一覧 *-- 社員
 社員 o- ユーザー
+商品一覧 *-- 商品
 @enduml
 `;
 
@@ -160,6 +192,15 @@ entity "ユーザーマスタ" as user {
     役割 : text
 }
 
+entity "商品マスタ" as product {
+    *商品コード : text
+    --
+    商品名 : text
+    販売単価 : number
+    仕入単価 : number
+    売上原価: number
+}
+
 department ||--o{ employee
 employee ||-o user
 @enduml
@@ -288,6 +329,42 @@ const ui = `
       Uxxxxxx | User3    | [ 選択  ]
     }
   }
+  商品一覧画面（コレクション）
+  {+
+     {
+      ホーム
+      ユーザー
+      マスタ
+      ログアウト
+      } |
+      {
+        {
+          <b>商品一覧</b>
+        }
+        
+        [  新規登録  ] | [ 一括削除  ]
+        ---------------------
+        {
+          1xxxxxx | 商品1    | [  編集  ] | [  削除  ]
+          1xxxxxx | 商品2    | [  編集  ] | [  削除  ]
+          1xxxxxx | 商品3    | [  編集  ] | [  削除  ]
+        }
+      } 
+  }
+  ----------------
+  商品画面（シングル）
+  {+
+      {
+        [  保存   ]
+        ---------------------
+        商品コード    | "1xxxxxx"   "
+        商品名        | "商品1    "
+        販売単価      | 1000
+        仕入単価      | 1000
+        売上原価      | 1000
+      }
+  
+  }
 }
 @endsalt
 `;
@@ -320,6 +397,7 @@ const uiModel = `
   
   class 社員 {
     保存()
+    追加()
   }
   
   class 部門一覧選択 {
@@ -330,6 +408,17 @@ const uiModel = `
     選択()
   }
   
+  class 商品一覧 {
+    新規作成()
+    編集()
+    削除()
+    一括削除()
+  }
+  
+  class 商品 {
+    保存()
+  }
+  
   class ナビゲーション {
     ホーム()
     マスタ()
@@ -344,6 +433,9 @@ const uiModel = `
   社員一覧 *-- 社員
   社員 *-- ユーザー一覧選択
   社員 *-- 部門一覧選択
+  ナビゲーション --* 商品一覧
+  商品一覧 *-- 商品
+    
 @enduml
 `;
 
@@ -364,6 +456,10 @@ const uiInteraction = `
     部門一覧選択_コレクション --> 社員_シングル
     社員_シングル --> ユーザー一覧選択_コレクション
     ユーザー一覧選択_コレクション --> 社員_シングル
+    
+    ナビゲーション_シングル --> 商品_コレクション
+    商品_コレクション --> 商品_シングル
+    商品_シングル --> 商品_コレクション
   ログイン_シングル <-- ナビゲーション_シングル
   
 @enduml

```

## コミット: b1e1a8a5

### メッセージ

```
fix:ドキュメント
Add spacing for readability in index.js

Added a blank line for spacing between the department list header and registration options. This enhances the readability and maintains consistency in the layout.
```

### 変更されたファイル

- M	index.js

### 変更内容

```diff
commit b1e1a8a50894e90e0d200f7499f42c3da6c46b9b
Author: Katuyuki Kakigi <kakimomokuri@gmail.com>
Date:   Sun Nov 3 15:18:53 2024 +0900

    fix:ドキュメント
    
    Add spacing for readability in index.js
    
    Added a blank line for spacing between the department list header and registration options. This enhances the readability and maintains consistency in the layout.

diff --git a/index.js b/index.js
index 710a39a2..85c86950 100644
--- a/index.js
+++ b/index.js
@@ -180,6 +180,7 @@ const ui = `
         {
           <b>部門一覧</b>
         }
+        
         [  新規登録  ] | [ 一括削除  ]
         ---------------------
         {

```

## コミット: a8dab63e

### メッセージ

```
refactor:E2Eテスト
Update navigation element IDs for Cypress tests

Standardize the IDs of navigation elements for improved consistency and accuracy in Cypress tests. This includes updating both the test scripts and the navigation component to ensure correct element identification.
```

### 変更されたファイル

- M	app/cypress/e2e/master/Department.cy.js
- M	app/cypress/e2e/master/Employee.cy.js
- M	app/cypress/e2e/system/User.cy.js
- M	app/src/views/application/Navigation.tsx

### 変更内容

```diff
commit a8dab63e9457bf3a0e8dc25ae51757c1b48682b1
Author: Katuyuki Kakigi <kakimomokuri@gmail.com>
Date:   Sun Nov 3 15:06:21 2024 +0900

    refactor:E2Eテスト
    
    Update navigation element IDs for Cypress tests
    
    Standardize the IDs of navigation elements for improved consistency and accuracy in Cypress tests. This includes updating both the test scripts and the navigation component to ensure correct element identification.

diff --git a/app/cypress/e2e/master/Department.cy.js b/app/cypress/e2e/master/Department.cy.js
index f1b66160..e0bf98f8 100644
--- a/app/cypress/e2e/master/Department.cy.js
+++ b/app/cypress/e2e/master/Department.cy.js
@@ -4,7 +4,7 @@ describe('部門管理', () => {
     })
 
     const userPage = () => {
-        cy.get('#side-nav-menu > :nth-child(1) > :nth-child(3) > .nav-sub-list > :nth-child(1) > a').click();
+        cy.get('#side-nav-menu > :nth-child(1) > :nth-child(3) > .nav-sub-list > :nth-child(1) > #side-nav-department-nav').click();
     }
 
     context('部門一覧', () => {
diff --git a/app/cypress/e2e/master/Employee.cy.js b/app/cypress/e2e/master/Employee.cy.js
index b0fb3ac6..0c4dc97a 100644
--- a/app/cypress/e2e/master/Employee.cy.js
+++ b/app/cypress/e2e/master/Employee.cy.js
@@ -4,7 +4,7 @@ describe('社員管理', () => {
     })
 
     const userPage = () => {
-        cy.get('#side-nav-menu > :nth-child(1) > :nth-child(3) > .nav-sub-list > :nth-child(2) > a').click();
+        cy.get('#side-nav-menu > :nth-child(1) > :nth-child(3) > .nav-sub-list > :nth-child(2) > #side-nav-employee-nav').click();
     }
 
     context('社員一覧', () => {
diff --git a/app/cypress/e2e/system/User.cy.js b/app/cypress/e2e/system/User.cy.js
index c87a20da..d1a29363 100644
--- a/app/cypress/e2e/system/User.cy.js
+++ b/app/cypress/e2e/system/User.cy.js
@@ -4,7 +4,7 @@ describe('ユーザー管理', () => {
     })
 
     const userPage = () => {
-        cy.get('#side-nav-menu > :nth-child(1) > :nth-child(2) > .nav-sub-list > #side-nav-user-nav > a').click();
+        cy.get('#side-nav-menu > :nth-child(1) > :nth-child(2) > .nav-sub-list > .nav-sub-item > #side-nav-user-nav').click();
     }
 
     context('ユーザー一覧', () => {
diff --git a/app/src/views/application/Navigation.tsx b/app/src/views/application/Navigation.tsx
index 5597e88c..5d930c18 100644
--- a/app/src/views/application/Navigation.tsx
+++ b/app/src/views/application/Navigation.tsx
@@ -22,8 +22,8 @@ interface SubNavItemProps {
     children: React.ReactNode;
 }
 const SubNavItem: React.FC<SubNavItemProps> = ({id, to, children}) => (
-    <li className="nav-sub-item" id={id}>
-        <Link to={to}>{children}</Link>
+    <li className="nav-sub-item">
+        <Link to={to} id={id}>{children}</Link>
     </li>
 );
 
@@ -45,7 +45,7 @@ const NaveItems: React.FC = () => {
                         マスタ
                         <ul className="nav-sub-list">
                             <SubNavItem id="side-nav-department-nav" to="/department">部門</SubNavItem>
-                            <SubNavItem id="side-nav-department-nav" to="/employee">社員</SubNavItem>
+                            <SubNavItem id="side-nav-employee-nav" to="/employee">社員</SubNavItem>
                         </ul>
                     </li>
                 </>

```

## コミット: 9eacc734

### メッセージ

```
test:E2Eテスト
Add IDs to form fields and buttons for better accessibility

This commit introduces unique IDs to various input fields and action buttons across the EmployeeSingle, EmployeeCollection, and DepartmentSelect components. These IDs enhance accessibility and facilitate improved automation testing. Additionally, a new Cypress e2e test file for Employee management was added along with ignoring Cypress screenshots in .gitignore.
```

### 変更されたファイル

- M	api/src/main/resources/db/migration/h2/V2.11__employee_data.sql
- M	app/.gitignore
- M	app/cypress/e2e/master/Department.cy.js
- A	app/cypress/e2e/master/Employee.cy.js
- M	app/src/components/master/Employee.tsx
- M	app/src/views/master/DepartmentCollection.tsx
- M	app/src/views/master/DepartmentSelect.tsx
- M	app/src/views/master/EmployeeCollection.tsx
- M	app/src/views/master/EmployeeSelect.tsx
- M	app/src/views/master/EmployeeSingle.tsx
- M	app/src/views/system/UserSelect.tsx

### 変更内容

```diff
commit 9eacc734909944684107d2558aad6900f3bff396
Author: Katuyuki Kakigi <kakimomokuri@gmail.com>
Date:   Sun Nov 3 14:40:47 2024 +0900

    test:E2Eテスト
    
    Add IDs to form fields and buttons for better accessibility
    
    This commit introduces unique IDs to various input fields and action buttons across the EmployeeSingle, EmployeeCollection, and DepartmentSelect components. These IDs enhance accessibility and facilitate improved automation testing. Additionally, a new Cypress e2e test file for Employee management was added along with ignoring Cypress screenshots in .gitignore.

diff --git a/api/src/main/resources/db/migration/h2/V2.11__employee_data.sql b/api/src/main/resources/db/migration/h2/V2.11__employee_data.sql
index a3b5f5d0..e361f54c 100644
--- a/api/src/main/resources/db/migration/h2/V2.11__employee_data.sql
+++ b/api/src/main/resources/db/migration/h2/V2.11__employee_data.sql
@@ -1,10 +1,4 @@
 -- 社員マスタ
-INSERT INTO public."社員マスタ" ("社員コード", "社員名", "社員名カナ", "パスワード", "電話番号", "FAX番号",
-                                 "部門コード", "開始日", "職種コード", "承認権限コード", "作成日時", "作成者名",
-                                 "更新日時", "更新者名")
-VALUES ('EMP999', '伊藤 裕子', 'イトウ ユウコ', 'password', '090-1234-5678', '03-1234-5678', '11101',
-        '2021-01-01 00:00:00.000000', '', '', '2021-01-01 00:00:00.000000', 'admin', '2021-01-01 00:00:00.000000',
-        'admin');
 INSERT INTO public."社員マスタ" ("社員コード", "社員名", "社員名カナ", "パスワード", "電話番号", "FAX番号",
                                  "部門コード", "開始日", "職種コード", "承認権限コード", "作成日時", "作成者名",
                                  "更新日時", "更新者名")
diff --git a/app/.gitignore b/app/.gitignore
index 7a9d1bca..f3f1fcac 100644
--- a/app/.gitignore
+++ b/app/.gitignore
@@ -24,3 +24,5 @@ dist-ssr
 *.sw?
 
 .vercel
+
+cypress/screenshots
diff --git a/app/cypress/e2e/master/Department.cy.js b/app/cypress/e2e/master/Department.cy.js
index 612a94d8..f1b66160 100644
--- a/app/cypress/e2e/master/Department.cy.js
+++ b/app/cypress/e2e/master/Department.cy.js
@@ -54,7 +54,7 @@ describe('部門管理', () => {
             cy.get(':nth-child(7) > .single-view-content-item-form-radios > :nth-child(2)').click();
             cy.get(':nth-child(8) > .single-view-content-item-form-radios > :nth-child(2)').click();
             cy.get('#add').click();
-            cy.get(':nth-child(1) > .collection-object-item-actions > #select').click();
+            cy.get(':nth-child(1) > .collection-object-item-actions > #select-employee').click();
             cy.get('.close-modal-button').click();
             cy.get('#save').click();
 
diff --git a/app/cypress/e2e/master/Employee.cy.js b/app/cypress/e2e/master/Employee.cy.js
new file mode 100644
index 00000000..b0fb3ac6
--- /dev/null
+++ b/app/cypress/e2e/master/Employee.cy.js
@@ -0,0 +1,83 @@
+describe('社員管理', () => {
+    beforeEach(() => {
+        cy.login('U000003', 'a234567Z');
+    })
+
+    const userPage = () => {
+        cy.get('#side-nav-menu > :nth-child(1) > :nth-child(3) > .nav-sub-list > :nth-child(2) > a').click();
+    }
+
+    context('社員一覧', () => {
+        it('社員一覧の表示', () => {
+            userPage();
+            cy.get('.collection-view-container').should('be.visible');
+        });
+    });
+
+    context('社員新規登録', () => {
+        it('新規登録', () => {
+            userPage();
+            cy.get('#new').click();
+            cy.get('#empCode').type('EMP999');
+            cy.get('#firstName').type('山田');
+            cy.get('#lastName').type('太郎');
+            cy.get('#firstNameKana').type('ヤマダ');
+            cy.get('#lastNameKana').type('タロウ');
+            cy.get('#tel').type('090-1234-5678');
+            cy.get('#fax').type('090-1234-5678');
+            cy.get('#select-department').click();
+            cy.get('.collection-object-container-modal > .collection-object-list > :nth-child(1) > .collection-object-item-actions > .action-button').click();
+            cy.get('#save').click();
+
+            cy.get('#message').contains('社員を作成しました。');
+        });
+    });
+
+    context('社員検索', () => {
+        it('検索', () => {
+            userPage();
+            cy.get('#search-input').type('EMP999');
+            cy.get('#search-all').click();
+            cy.wait(1000);
+            cy.get(':nth-child(3) > .collection-object-item-content-name').contains('山田');
+        });
+    })
+
+    context('社員登録情報編集', () => {
+        it('登録情報編集', () => {
+            userPage();
+            cy.get('#search-input').type('EMP999');
+            cy.get('#search-all').click();
+            cy.wait(1000);
+            cy.get('#edit').click();
+            cy.get('#firstName').clear();
+            cy.get('#firstName').type('鈴木');
+            cy.get('#lastName').clear();
+            cy.get('#lastName').type('次郎');
+            cy.get('#firstNameKana').clear();
+            cy.get('#firstNameKana').type('スズキ');
+            cy.get('#lastNameKana').clear();
+            cy.get('#lastNameKana').type('ジロウ');
+            cy.get('#tel').clear();
+            cy.get('#tel').type('09-9999-9999');
+            cy.get('#fax').clear();
+            cy.get('#fax').type('00-0000-0000');
+            cy.get('#select-department').click();
+            cy.get('.collection-object-container-modal > .collection-object-list > :nth-child(2) > .collection-object-item-actions > .action-button').click();
+            cy.get('#save').click();
+
+            cy.get('#message').contains('社員を更新しました。');
+        });
+    });
+
+    context('社員削除', () => {
+        it('削除', () => {
+            userPage();
+            cy.get('#search-input').type('EMP999');
+            cy.get('#search-all').click();
+            cy.wait(1000);
+            cy.get('#delete').click();
+            cy.get('#message').contains('社員を削除しました。');
+        })
+    });
+});
diff --git a/app/src/components/master/Employee.tsx b/app/src/components/master/Employee.tsx
index dec112b4..3b9c8bd7 100644
--- a/app/src/components/master/Employee.tsx
+++ b/app/src/components/master/Employee.tsx
@@ -242,10 +242,14 @@ export const Employee: React.FC = () => {
                     }
                     setNewEmployee({...initialEmployee});
                     await fetchEmployees.load();
-                    setMessage("社員を保存しました。");
+                    if (isEditing) {
+                        setMessage("社員を更新しました。");
+                    } else {
+                        setMessage("社員を作成しました。");
+                    }
                     handleCloseModal();
                 } catch (error: any) {
-                    showErrorMessage(`社員の保存に失敗しました: ${error?.message}`, setError);
+                    showErrorMessage(`社員の作成に失敗しました: ${error?.message}`, setError);
                 }
             };
 
diff --git a/app/src/views/master/DepartmentCollection.tsx b/app/src/views/master/DepartmentCollection.tsx
index 6051f8ab..04c99762 100644
--- a/app/src/views/master/DepartmentCollection.tsx
+++ b/app/src/views/master/DepartmentCollection.tsx
@@ -11,7 +11,9 @@ interface SearchBarProps {
 
 const SearchBar: React.FC<SearchBarProps> = ({searchValue, onChange, onSearch}) => (
     <div className="search-container">
-        <input type="text" id="search-input" placeholder="部門コードで検索"
+        <input id="search-input"
+               type="text"
+               placeholder="部門コードで検索"
                value={searchValue}
                onChange={onChange}/>
         <button className="action-button" id="search-all" onClick={onSearch}>検索</button>
diff --git a/app/src/views/master/DepartmentSelect.tsx b/app/src/views/master/DepartmentSelect.tsx
index 799e4a5b..05254b63 100644
--- a/app/src/views/master/DepartmentSelect.tsx
+++ b/app/src/views/master/DepartmentSelect.tsx
@@ -19,7 +19,7 @@ export const DepartmentSelectView: React.FC<DepartmentSelectProps> = ({handleSel
                 </div>
                 <div className="collection-view-content">
                     <div className="button-container">
-                        <button className="action-button" onClick={handleSelect}>選択
+                        <button className="action-button" onClick={handleSelect} id="select-department">選択
                         </button>
                     </div>
                 </div>
diff --git a/app/src/views/master/EmployeeCollection.tsx b/app/src/views/master/EmployeeCollection.tsx
index 5c5aea51..65f7651a 100644
--- a/app/src/views/master/EmployeeCollection.tsx
+++ b/app/src/views/master/EmployeeCollection.tsx
@@ -13,12 +13,13 @@ const SearchBar: React.FC<SearchBarProps> = ({searchValue, onSearchChange, onSea
     return (
         <div className="search-container">
             <input
+                id="search-input"
                 type="text"
                 placeholder="社員コードで検索"
                 value={searchValue}
                 onChange={onSearchChange}
             />
-            <button className="action-button" onClick={onSearchClick}>検索</button>
+            <button className="action-button" id="search-all" onClick={onSearchClick}>検索</button>
         </div>
     );
 };
@@ -51,10 +52,11 @@ const EmployeeListItem: React.FC<EmployeeListItemProps> = ({employee, onEdit, on
                 </div>
             ))}
             <div className="collection-object-item-actions" data-id={employee.empCode.value}>
-                <button className="action-button" onClick={() => onEdit(employee)}>編集</button>
+                <button className="action-button" onClick={() => onEdit(employee)} id="edit">編集</button>
             </div>
             <div className="collection-object-item-actions" data-id={employee.empCode.value}>
-                <button className="action-button" onClick={() => onDelete(employee.empCode.value)}>削除</button>
+                <button className="action-button" onClick={() => onDelete(employee.empCode.value)} id="delete">削除
+                </button>
             </div>
         </li>
     );
@@ -132,9 +134,13 @@ export const EmployeeCollectionView: React.FC<EmployeeCollectionViewProps> = ({
                         onSearchClick={handleSearchEmployee}
                     />
                     <div className="button-container">
-                        <button className="action-button" onClick={() => handleOpenModal()}>新規</button>
-                        <button className="action-button" onClick={handleCheckToggleCollection}>一括選択</button>
-                        <button className="action-button" onClick={handleDeleteCheckedCollection}>一括削除</button>
+                        <button className="action-button" onClick={() => handleOpenModal()} id="new">新規</button>
+                        <button className="action-button" onClick={handleCheckToggleCollection}
+                                id="selectAll">一括選択
+                        </button>
+                        <button className="action-button" onClick={handleDeleteCheckedCollection}
+                                id="deleteAll">一括削除
+                        </button>
                     </div>
                     <EmployeeList
                         employees={employees}
diff --git a/app/src/views/master/EmployeeSelect.tsx b/app/src/views/master/EmployeeSelect.tsx
index d529fa85..b616820e 100644
--- a/app/src/views/master/EmployeeSelect.tsx
+++ b/app/src/views/master/EmployeeSelect.tsx
@@ -100,7 +100,7 @@ export const EmployeeCollectionSelectView: React.FC<EmployeeCollectionSelectProp
                                     </div>
                                     <div className="collection-object-item-actions" data-id={employee.empCode.value}>
                                         <button className="action-button" onClick={() => handleSelect(employee)}
-                                                id="select">選択
+                                                id="select-employee">選択
                                         </button>
                                     </div>
                                 </li>
diff --git a/app/src/views/master/EmployeeSingle.tsx b/app/src/views/master/EmployeeSingle.tsx
index 953fff21..5d5962d9 100644
--- a/app/src/views/master/EmployeeSingle.tsx
+++ b/app/src/views/master/EmployeeSingle.tsx
@@ -44,42 +44,58 @@ const Form: React.FC<EmployeeFormProps> = ({newEmployee, setNewEmployee, isEditi
                         onChange={(value) => setNewEmployee({
                             ...newEmployee,
                             empCode: {value}
-                        })} disabled={isEditing}/>
+                        })} disabled={isEditing}
+                        id="empCode"
+            />
             <InputField label="姓" value={newEmployee.empName.firstName} placeholder="姓"
                         onChange={(value) => setNewEmployee({
                             ...newEmployee,
                             empName: {...newEmployee.empName, firstName: value}
-                        })}/>
+                        })}
+                        id="firstName"
+            />
             <InputField label="名" value={newEmployee.empName.lastName} placeholder="名"
                         onChange={(value) => setNewEmployee({
                             ...newEmployee,
                             empName: {...newEmployee.empName, lastName: value}
-                        })}/>
+                        })}
+                        id="lastName"
+            />
             <InputField label="姓カナ" value={newEmployee.empName.firstNameKana} placeholder="姓カナ"
                         onChange={(value) => setNewEmployee({
                             ...newEmployee,
                             empName: {...newEmployee.empName, firstNameKana: value}
-                        })}/>
+                        })}
+                        id="firstNameKana"
+            />
             <InputField label="名カナ" value={newEmployee.empName.lastNameKana} placeholder="名カナ"
                         onChange={(value) => setNewEmployee({
                             ...newEmployee,
                             empName: {...newEmployee.empName, lastNameKana: value}
-                        })}/>
+                        })}
+                        id="lastNameKana"
+            />
             <InputField label="電話番号" value={newEmployee.tel.value} placeholder="電話番号"
                         onChange={(value) => setNewEmployee({
                             ...newEmployee,
                             tel: {...newEmployee.tel, value}
-                        })}/>
+                        })}
+                        id="tel"
+            />
             <InputField label="FAX番号" value={newEmployee.fax.value} placeholder="FAX番号"
                         onChange={(value) => setNewEmployee({
                             ...newEmployee,
                             fax: {...newEmployee.fax, value}
-                        })}/>
+                        })}
+                        id="fax"
+            />
             <InputField label="職種コード" value={newEmployee.occuCode.value} placeholder="職種コード"
                         onChange={(value) => setNewEmployee({
                             ...newEmployee,
                             occuCode: {...newEmployee.occuCode, value}
-                        })}/>
+                        })}
+                        id="occuCode"
+            />
             <InputField label="部門コード" value={newEmployee.department?.departmentId.deptCode.value}
                         placeholder="部門コード" onChange={(value) => setNewEmployee({
                 ...newEmployee,
@@ -90,7 +106,9 @@ const Form: React.FC<EmployeeFormProps> = ({newEmployee, setNewEmployee, isEditi
                         deptCode: {value}
                     }
                 }
-            })}/>
+            })}
+                        id="deptCode"
+            />
             <InputField
                 label="開始日"
                 type="date"
@@ -106,12 +124,15 @@ const Form: React.FC<EmployeeFormProps> = ({newEmployee, setNewEmployee, isEditi
                         }
                     }
                 })}
+                id="departmentStartDate"
             />
             <InputField label="ユーザーID" value={newEmployee.user?.userId.value} placeholder="ユーザーID"
                         onChange={(value) => setNewEmployee({
                             ...newEmployee,
                             user: {...newEmployee.user, userId: {value}}
-                        })}/>
+                        })}
+                        id="userId"
+            />
         </div>
     );
 };
@@ -122,14 +143,16 @@ interface InputFieldProps {
     placeholder: string;
     onChange: (value: string) => void;
     disabled?: boolean;
+    id?: string;
     type?: string;
 }
 
-const InputField: React.FC<InputFieldProps> = ({label, value, placeholder, onChange, disabled, type = "text"}) => {
+const InputField: React.FC<InputFieldProps> = ({label, value, placeholder, onChange, disabled, id, type = "text"}) => {
     return (
         <div className="single-view-content-item-form-item">
             <label className="single-view-content-item-form-item-label">{label}</label>
             <input
+                id={id}
                 type={type}
                 className="single-view-content-item-form-item-input"
                 placeholder={placeholder}
diff --git a/app/src/views/system/UserSelect.tsx b/app/src/views/system/UserSelect.tsx
index dc17bf1e..08727374 100644
--- a/app/src/views/system/UserSelect.tsx
+++ b/app/src/views/system/UserSelect.tsx
@@ -18,7 +18,7 @@ export const UserSelectView: React.FC<UserSelectProps> = ({handleSelect}) => {
                 </div>
                 <div className="collection-view-content">
                     <div className="button-container">
-                        <button className="action-button" onClick={handleSelect}>選択</button>
+                        <button className="action-button" onClick={handleSelect} id="select-user">選択</button>
                     </div>
                 </div>
             </div>

```

